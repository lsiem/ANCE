interface:
  display_name: "ANCE"
  short_description: "Repo-specific patterns and workflows for ANCE"
  default_prompt: "Use the ANCE repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true