---
phase: 07-nnue-strength-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - training/data/hf_ingest.py
  - training/data/quiet_filter.py
  - training/run_pipeline.py
  - tests/training/test_hf_ingest.py
  - tests/training/test_quiet_filter.py
  - tests/training/test_run_pipeline_hf.py
autonomous: true
requirements:
  - TOOL-04
must_haves:
  truths:
    - "Official 4-field HF FENs become 6-field with clocks 0 16 in row_to_sample so ply_from_fen is at least 8 and is_quiet_fen does not reject early_ply — D-01, D-03, D-04"
    - "Strength mix accepts min_has_result_rate 0.15 with modest Lichess fill while HF rows stay game_result None — D-02"
    - "--lichess-max-samples 80000 caps PGN fill; default None keeps time-derived cap — D-02, D-04"
    - "filter_quiet_samples stops at max_kept 120000 with truncated plus kept_by_source — D-04, D-15"
    - "Pinned Discretion (copy): hf_max_positions=750000, dump=2013-01, lichess-max-samples=80000, min_has_result_rate=0.15"
    - "Pinned Discretion (copy): λ 1.0→0.75, fen-skip=2, epochs=30, patience=6, elo_probe 5/12"
    - "Pinned Discretion (copy): smoke=16; abort if wins==0 OR score_rate==0.0 OR elo_ci_high < -200"
    - "Pinned Discretion (copy): sidecar 07-NET-SIDECAR.json; closer post_train_close_07.py; evidence 07-GAUNTLET-EVIDENCE.json"
    - "Pinned Discretion (copy): Wave 0 pad 4-field HF FEN with 0 16; add --lichess-max-samples"
    - "AdamW and cosine LR stay at train.py defaults (1e-3 / 1e-4 / eta_min=lr*0.05) — D-06"
    - "Spec-less probe fallback skipped this run: ROADMAP phase_req_ids is TBD/null; TOOL-04 still listed in requirements; no probe predicates generated"
  artifacts:
    - path: training/data/hf_ingest.py
      provides: "4-field FEN pad in row_to_sample"
      contains: "0 16"
    - path: training/run_pipeline.py
      provides: "--lichess-max-samples and max_kept pass-through"
      contains: "lichess-max-samples"
    - path: training/data/quiet_filter.py
      provides: "max_kept early-stop on filter_quiet_samples"
      contains: "max_kept"
    - path: tests/training/test_hf_ingest.py
      provides: "TestFourFieldFenPad official card FEN"
  key_links:
    - from: training/data/hf_ingest.py
      to: training/data/quiet_filter.py
      via: "padded FEN must satisfy ply_from_fen 6-field contract"
      pattern: "0 16"
    - from: training/run_pipeline.py
      to: training/data/quiet_filter.py
      via: "strength_corpus passes max_kept=120000 into filter_quiet_samples"
      pattern: "max_kept"
  prohibitions:
    - statement: "Do not add king-bucketed feature sets or change hidden width"
      status: enforced
    - statement: "Do not start a CPU train on this host"
      status: enforced
    - statement: "Do not complete-phase 6 while TOOL-04 is open"
      status: enforced
    - statement: "Do not start a second in-phase train"
      status: enforced

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
---

<objective>
Wave 0 ingest harness: pad official 4-field Hugging Face FENs, cap Lichess fill, early-stop quiet keep, and prove HF-primary + modest-result mix at 0.15 so D-01/D-04 are not a silent no-op.

Purpose: Phase 6 collapsed to ~20k because 4-field FENs look like ply 0. Without this slice, the M4 sitting retrains the same Lichess-only set.
Output: Production pad + `--lichess-max-samples` + `max_kept` with pytest proving ingest→quiet-ply→mix end-to-end.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-nnue-strength-recovery/07-CONTEXT.md
@.planning/phases/07-nnue-strength-recovery/07-RESEARCH.md
@.planning/phases/07-nnue-strength-recovery/07-PATTERNS.md
@.planning/phases/07-nnue-strength-recovery/07-VALIDATION.md
@training/data/hf_ingest.py
@training/data/quiet_filter.py
@training/run_pipeline.py
@tests/training/test_hf_ingest.py
@tests/training/test_quiet_filter.py
@tests/training/test_run_pipeline_hf.py
</context>

## Artifacts this phase produces

- `training/data/hf_ingest.py` — `row_to_sample` pads 4-field FENs — **Plan 07-01**
- `training/run_pipeline.py` — `--lichess-max-samples`, `lichess_max_samples`, `max_kept=120000` — **Plan 07-01**
- `training/data/quiet_filter.py` — `max_kept`, `QuietFilterStats.truncated`, `kept_by_source` — **Plan 07-01**
- `tests/training/test_hf_ingest.py` — `TestFourFieldFenPad` — **Plan 07-01**
- `tests/training/test_quiet_filter.py` — mix 0.15 + `max_kept` — **Plan 07-01**
- `tests/training/test_run_pipeline_hf.py` — Lichess cap — **Plan 07-01**
- `.planning/phases/07-nnue-strength-recovery/post_train_close_07.py` — **Plan 07-02**
- `tests/training/test_phase7_closer_evidence.py` — **Plan 07-02**
- `.planning/phases/07-nnue-strength-recovery/install_phase7_net.py` — **Plan 07-03**
- `tests/training/test_phase7_sidecar.py` — **Plan 07-03**
- `.planning/phases/07-nnue-strength-recovery/07-NET-SIDECAR.json` — **Plan 07-03**
- `ance/eval/nnue/net.safetensors` — from-scratch overwrite — **Plan 07-03**
- `.planning/phases/07-nnue-strength-recovery/07-GAUNTLET-EVIDENCE.json` — **Plan 07-04**

New symbols/flags: `--lichess-max-samples`, `run_bounded(..., lichess_max_samples=)`, `_lichess_sample_cap`, `filter_quiet_samples(..., max_kept=)`, `QuietFilterStats.truncated`, `QuietFilterStats.kept_by_source`.

## Recorded choice — spec-less probe fallback

`gsd_run query init.plan-phase 7` returned `phase_req_ids: null` (ROADMAP Requirements still TBD). Spec-less probe fallback skipped this run. TOOL-04 still appears in every plan `requirements` field. No probe predicates generated.

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end HF 4-field pad through quiet ply and mix 0.15 — one path only</name>
  <files>training/data/hf_ingest.py, tests/training/test_hf_ingest.py, tests/training/test_quiet_filter.py</files>
  <read_first>.planning/phases/07-nnue-strength-recovery/07-RESEARCH.md Pattern 1 and Pinned Discretion; .planning/phases/07-nnue-strength-recovery/07-PATTERNS.md hf_ingest and test_hf_ingest sections; training/data/hf_ingest.py row_to_sample; training/data/quiet_filter.py ply_from_fen is_quiet_fen enforce_corpus_mix; tests/training/test_hf_ingest.py TestSkipRow TestSignConvention</read_first>
  <behavior>
    - Test 1: official card FEN 2bq1rk1/pr3ppn/1p2p3/7P/2pP1B1P/2P5/PPQ2PB1/R3R1K1 w - - pads to six fields ending in 0 16 (D-01)
    - Test 2: padded FEN has ply_from_fen at least 8 and is_quiet_fen reject reason is not early_ply (D-03)
    - Test 3: existing onlyonefield FEN still returns None from row_to_sample
    - Test 4: crc32 game_id uses the padded FEN string
    - Test 5: 20 samples with exactly 3 game_result set (one padded HF row plus modest Lichess-like fill) pass enforce_corpus_mix at min_has_result_rate 0.15 and strength_corpus True; 2 of 20 still raises (D-02)
    - Test 6: default min_has_result_rate 0.50 still raises on zero-result samples
  </behavior>
  <action>
    Per D-01 and D-03, edit row_to_sample in training/data/hf_ingest.py after the existing len(fields) less-than-2 skip. When len(fields) equals 4, append space-zero-space-sixteen to fen and re-split fields before the black-STM sign flip. crc32 the padded fen so identical 4-field rows share game_id after normalize. Keep game_result None, source lichess-hf, and hf-NNNN game_id. Do not call chess.Board(fen).fen() — that rewrites clocks to 0 1 and still fails early_ply. Do not import training.data.quiet_filter from hf_ingest. Do not change _HF_DEFAULT_REPO, _COLUMNS, OR quality filter, or per-shard hf_hub_download.

    Add class TestFourFieldFenPad in tests/training/test_hf_ingest.py next to TestSkipRow using constant _HF_CARD_FEN from 07-PATTERNS.md. Cover behaviors 1–4. Keep test_short_fen_is_skipped.

    Add test_enforce_strength_allows_015_when_enough_results and test_tracer_padded_hf_plus_modest_fill_passes_mix_015 in tests/training/test_quiet_filter.py per D-02. Keep test_enforce_strength_requires_results at 0.50. Do not change enforce_corpus_mix default 0.50. This tracer does not edit quiet_filter.py production code.
  </action>
  <acceptance_criteria>
    - "Official card FEN sample has six fields and ends with 0 16"
    - "ply_from_fen on that sample is at least 8; is_quiet_fen reason is not early_ply"
    - "enforce_corpus_mix at 0.15 accepts a 15 percent result mix that includes a padded HF row"
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/training/test_hf_ingest.py tests/training/test_quiet_filter.py -q -x</automated>
    <fails_when>pytest exits non-zero, or an assertion that the card FEN still has four fields, or reject reason early_ply, or RuntimeError has_result at rate 0.15</fails_when>
  </verify>
  <done>One production path: HF row_to_sample pad → quiet ply accepts → mix 0.15 accepts modest fill. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: --lichess-max-samples cap (D-02, D-04)</name>
  <files>training/run_pipeline.py, tests/training/test_run_pipeline_hf.py</files>
  <read_first>.planning/phases/07-nnue-strength-recovery/07-PATTERNS.md run_pipeline section; training/run_pipeline.py _ingest_lichess run_bounded main argparse around --lichess-zst and --hf-max-positions; tests/training/test_run_pipeline_hf.py test_ingest_hf_caps_samples_and_forwards_thresholds</read_first>
  <behavior>
    - Test 1: _lichess_sample_cap(time_cap=50000, lichess_max_samples=None) returns 50000
    - Test 2: _lichess_sample_cap(time_cap=50000, lichess_max_samples=80000) returns 50000
    - Test 3: _lichess_sample_cap(time_cap=200000, lichess_max_samples=80000) returns 80000
    - Test 4: argparse dest lichess_max_samples defaults to None; passing --lichess-max-samples 80000 sets 80000
    - Test 5: _ingest_lichess stops at sample_cap when extract_samples would overshoot (monkeypatch iter_games / extract_samples)
  </behavior>
  <action>
    Per D-02 and D-04, add argparse --lichess-max-samples type int default None immediately after --lichess-zst (copy the --hf-max-positions shape). Add helper _lichess_sample_cap(time_cap, lichess_max_samples) returning time_cap when the explicit cap is None else min(time_cap, lichess_max_samples). Thread lichess_max_samples: int | None = None through run_bounded. When ingesting, keep sample_cap = max(1000, int(remaining * 20)) then replace it with _lichess_sample_cap(sample_cap, lichess_max_samples) before _ingest_lichess. Pass args.lichess_max_samples from main into run_bounded. Do not reorder streams (lichess then HF then fresh). Do not change empty-HF cache skip. Do not change AdamW/LR argparse defaults. Do not remove --resume-from-checkpoint; M4 simply must not pass it (D-08).
  </action>
  <acceptance_criteria>
    - "--lichess-max-samples exists and defaults to None"
    - "Explicit 80000 wins when time cap is larger; time cap wins when smaller"
    - "_ingest_lichess still truncates to sample_cap"
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/training/test_run_pipeline_hf.py -q -x</automated>
    <fails_when>pytest exits non-zero, ImportError, or an assertion that lichess_max_samples is missing / sample_cap ignored</fails_when>
  </verify>
  <done>--lichess-max-samples 80000 is a real CLI/kwarg cap; default None preserves today’s time-only behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: quiet max_kept early-stop and per-source stats (D-04, D-15)</name>
  <files>training/data/quiet_filter.py, training/run_pipeline.py, tests/training/test_quiet_filter.py</files>
  <read_first>.planning/phases/07-nnue-strength-recovery/07-PATTERNS.md quiet_filter section; training/data/quiet_filter.py QuietFilterStats filter_quiet_samples; training/run_pipeline.py filter_quiet_samples call; tests/training/test_quiet_filter.py test_filter_quiet_samples_stats</read_first>
  <behavior>
    - Test 1: three keepable late-ply quiet FENs with bestmove_capture_fn false and min_ply 0 yield len(kept) == 2 when max_kept is 2
    - Test 2: returned stats.truncated is True in that case
    - Test 3: kept_by_source counts sample source keys among kept rows
    - Test 4: reject reason strings stay check / early_ply / capture_bestmove / qsearch / illegal
  </behavior>
  <action>
    Per D-04 and D-15, add optional max_kept: int | None = None to filter_quiet_samples. After kept.append(sample), break when max_kept is not None and len(kept) &gt;= max_kept. Add QuietFilterStats fields truncated (default False) and kept_by_source (default empty dict via dataclass field) at the end of the dataclass so existing positional construction stays valid if you switch the constructor to keywords. Increment kept_by_source from sample.get(source) or unknown. Do not change DEFAULT_MIN_PLY, DEFAULT_QSEARCH_MARGIN, DEFAULT_CAPTURE_SKIP_DEPTH, or ply_from_fen returning 0 for fewer than 6 fields.

    In training/run_pipeline.py, when quiet_filter runs, pass max_kept=120000 whenever strength_corpus is True (Phase 7 sitting bound). Leave max_kept None for non-strength runs. Record truncated and kept_by_source on the existing quiet_filter record_event. Keep stderr error: exc / exit 1 on enforce_corpus_mix RuntimeError.
  </action>
  <acceptance_criteria>
    - "max_kept=2 keeps exactly two quiet samples and sets truncated"
    - "strength_corpus quiet_filter call in run_pipeline passes max_kept=120000"
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/training/test_hf_ingest.py tests/training/test_quiet_filter.py tests/training/test_phase6_closer_evidence.py tests/training/test_diagnostics_eval.py tests/training/test_lambda_schedule.py tests/training/test_run_pipeline_hf.py -q -x</automated>
    <fails_when>pytest exits non-zero, or filter_quiet_samples keeps more than max_kept, or quiet_filter call site never mentions max_kept</fails_when>
  </verify>
  <done>Quiet filter can stop at 120k kept; Wave 0 harness is the production ingest path for the M4 recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hugging Face parquet / Lichess PGN → ingest | Untrusted FEN and eval fields enter row_to_sample / extract_samples |
| Operator CLI → run_pipeline | Local path args --lichess-zst --out-dir; no shell interpolation |
| training/ → ance/ | training may import ance; reverse is forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | --lichess-zst / --out-dir | medium | mitigate | Open as files only; no shell=True; existing argparse Path handling |
| T-07-02 | Tampering | HF/PGN FEN in row_to_sample | medium | mitigate | Skip short FENs; chess.Board reject illegal in is_quiet_fen; no eval on comments |
| T-07-04 | Denial of Service | HF shard download | medium | mitigate | Keep per-shard hf_hub_download + max_positions; do not snapshot_download; max_kept=120000 |
| T-07-05 | Information Disclosure | stderr ingest errors | low | accept | Local operator CLI; error: exc is intended |
| T-07-SC | Tampering | pip installs | high | mitigate | No new packages; RESEARCH Package Legitimacy Audit already approved pre-existing extras |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/training/test_hf_ingest.py tests/training/test_quiet_filter.py tests/training/test_phase6_closer_evidence.py tests/training/test_diagnostics_eval.py tests/training/test_lambda_schedule.py tests/training/test_run_pipeline_hf.py -q -x`
- After wave: `.venv/bin/python -m pytest tests/ -q -m 'not slow'`
</verification>

<success_criteria>
- Official 4-field HF FEN survives quiet ply (D-01/D-03)
- Mix 0.15 is legal for HF-primary + modest fill (D-02)
- `--lichess-max-samples` and `max_kept` exist (D-04/D-15)
- TOOL-04 not claimed yet — this plan only unblocks a real corpus
</success_criteria>

<output>
Create `.planning/phases/07-nnue-strength-recovery/07-01-SUMMARY.md` when done
</output>
