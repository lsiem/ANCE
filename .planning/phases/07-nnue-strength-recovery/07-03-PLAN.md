---
phase: 07-nnue-strength-recovery
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - .planning/phases/07-nnue-strength-recovery/install_phase7_net.py
  - tests/training/test_phase7_sidecar.py
  - .planning/phases/07-nnue-strength-recovery/07-NET-SIDECAR.json
  - ance/eval/nnue/net.safetensors
autonomous: false
requirements:
  - TOOL-04
user_setup:
  - service: apple-silicon-m4
    why: "D-13 trains on M4 MPS; this cloud Linux host is CPU-only (mps_avail False)"
    env_vars: []
    dashboard_config:
      - task: "On the M4 sitting, install extras and Stockfish, then run the pinned CLI after 07-01 lands"
        location: "PROJECT M4 Mac with `pip install -e '.[hf-ingest]'` and brew Stockfish"
must_haves:
  truths:
    - "Published arch stays 768x2-256-1 / board768; this plan records the one-way lock and does not change nnue_format.schema — D-05"
    - "Train from scratch on M4 MPS; do not pass --resume-from-checkpoint and do not warm-start ance/eval/nnue/net.safetensors — D-08, D-13"
    - "In-train Elo probes run every 5 epochs for 12 games; export prefers best_elo.safetensors when it exists — D-07"
    - "M4 CLI uses pinned knobs: hf_max_positions 750000, 2013-01 dump, lichess-max-samples 80000, min_has_result_rate 0.15, λ 1.0→0.75, fen-skip 2, epochs 30, patience 6, elo_probe 5/12, fresh-n-games 0, hf_min_depth 20, hf_min_knodes 1000, max-hours 5 — D-06, D-15"
    - "Committed 07-NET-SIDECAR.json has phase 7, from_scratch true, arch_id 768x2-256-1, feature_set board768 — D-16"
    - "This cloud host must not run a reduced CPU train if MPS is absent — D-14"
    - "Pinned Discretion (copy): hf_max_positions=750000, dump=2013-01, lichess-max-samples=80000, min_has_result_rate=0.15"
    - "Pinned Discretion (copy): λ 1.0→0.75, fen-skip=2, epochs=30, patience=6, elo_probe 5/12"
    - "Pinned Discretion (copy): sidecar 07-NET-SIDECAR.json; closer post_train_close_07.py; evidence 07-GAUNTLET-EVIDENCE.json"
  artifacts:
    - path: .planning/phases/07-nnue-strength-recovery/install_phase7_net.py
      provides: "write_sidecar + install_export"
      contains: "write_sidecar"
    - path: tests/training/test_phase7_sidecar.py
      provides: "sidecar identity and arch lock tests"
    - path: .planning/phases/07-nnue-strength-recovery/07-NET-SIDECAR.json
      provides: "Committed identity for the closer (after M4 install)"
  key_links:
    - from: .planning/phases/07-nnue-strength-recovery/install_phase7_net.py
      to: ance/eval/nnue/net.safetensors
      via: "shutil.copy2 export → ENGINE_NET"
      pattern: "install_export"
    - from: .planning/phases/07-nnue-strength-recovery/install_phase7_net.py
      to: nnue_format/schema.py
      via: "arch_id and feature_set copied from ARCH_ID / FEATURE_SET"
      pattern: "ARCH_ID"
  prohibitions:
    - statement: "Do not add king-bucketed feature sets or change hidden width"
      status: enforced
    - statement: "Do not start a CPU train on this host"
      status: enforced
    - statement: "Do not complete-phase 6 while TOOL-04 is open"
      status: enforced
    - statement: "Do not start a second in-phase train"
      status: enforced

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
---

<objective>
Lock `768x2-256-1` / `board768`, ship the sidecar + install helper, and run one from-scratch M4 sitting with the pinned CLI so the closer can identify a Phase 7 net.

Purpose: D-05 is a published contract (EVAL-03 / nnue_format). Changing width or feature-set here would orphan the packaged loader. D-14 forbids measuring the Phase 6 file without a sidecar.
Output: install_phase7_net.py, sidecar tests, and (on M4 resume) committed net + 07-NET-SIDECAR.json. On this host, resume blocked is a valid path into Plan 07-04.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-nnue-strength-recovery/07-CONTEXT.md
@.planning/phases/07-nnue-strength-recovery/07-RESEARCH.md
@.planning/phases/07-nnue-strength-recovery/07-PATTERNS.md
@nnue_format/schema.py
@training/export.py
@training/train.py
@training/run_pipeline.py
</context>

## Artifacts this phase produces

- Wave 0 ingest harness — **Plan 07-01** (required before the M4 CLI)
- Measure closer — **Plan 07-02**
- `.planning/phases/07-nnue-strength-recovery/install_phase7_net.py` — **Plan 07-03**
- `tests/training/test_phase7_sidecar.py` — **Plan 07-03**
- `.planning/phases/07-nnue-strength-recovery/07-NET-SIDECAR.json` — **Plan 07-03**
- `ance/eval/nnue/net.safetensors` — from-scratch overwrite — **Plan 07-03**
- `.planning/phases/07-nnue-strength-recovery/07-GAUNTLET-EVIDENCE.json` — **Plan 07-04**

New symbols/flags: `write_sidecar`, `install_export`, `REQUIRED_SIDECAR_KEYS`, `M4_PIPELINE_ARGV` (tuple of pinned flags; not executed on this host).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Record one-way arch lock (D-05)</name>
  <decision>Keep published architecture 768x2-256-1 / board768 for the Phase 7 export. Do not change hidden width or feature-set.</decision>
  <context>
    D-05 is already locked in 07-CONTEXT.md. nnue_format.schema.ARCH_ID, FEATURE_SET, and EXPECTED_SHAPES are the EVAL-03 packaged-net contract. Widening the hidden layer or switching to a king-bucketed feature-set would require a new milestone and a weights migration for every consumer of ance/eval/nnue/net.safetensors. This checkpoint records the one-way door; it does not reopen research. Auto-mode may select keep-768x2-256-1.
  </context>
  <options>
    <option id="keep-768x2-256-1">
      <name>Keep 768x2-256-1 / board768</name>
      <pros>Honors D-05, EVAL-03, and existing numpy loader; no format migration</pros>
      <cons>Cannot buy Elo by widening the net in this phase</cons>
    </option>
    <option id="change-arch">
      <name>Change hidden width or feature-set</name>
      <pros>None in this milestone — CONTEXT and RESEARCH forbid it</pros>
      <cons>Breaks nnue_format / packaged net; different milestone</cons>
    </option>
  </options>
  <resume-signal>Select keep-768x2-256-1 (required). change-arch is out of scope.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sidecar writer and install helper (D-05, D-16) — do not change arch</name>
  <reversibility rating="one-way">nnue_format / EVAL-03 already publish 768x2-256-1 / board768; a different export is a weights migration, not a refactor.</reversibility>
  <files>.planning/phases/07-nnue-strength-recovery/install_phase7_net.py, tests/training/test_phase7_sidecar.py</files>
  <read_first>.planning/phases/07-nnue-strength-recovery/07-RESEARCH.md Evidence Schema 07-NET-SIDECAR.json and M4 CLI flags; .planning/phases/07-nnue-strength-recovery/07-PATTERNS.md sidecar and net.safetensors sections; nnue_format/schema.py ARCH_ID FEATURE_SET EXPECTED_SHAPES; training/export.py export_checkpoint meta keys</read_first>
  <behavior>
    - Test 1: write_sidecar writes phase 7, from_scratch true, arch_id equal to nnue_format.schema.ARCH_ID, feature_set equal to FEATURE_SET
    - Test 2: JSON is RFC (allow_nan False); required keys match RESEARCH sidecar schema
    - Test 3: install_export copies a temp safetensors-like file to a monkeypatched ENGINE_NET
    - Test 4: sidecar_identity_payload rejects phase != 7 or from_scratch is not True
    - Test 5: M4_PIPELINE_ARGV contains --hf-max-positions 750000, --lichess-max-samples 80000, --min-has-result-rate 0.15, --elo-probe-every 5, --elo-probe-games 12, --random-fen-skipping 2, --epochs 30, --early-stop-patience 6, --fresh-n-games 0, --start-lambda 1.0, --end-lambda 0.75 and does not contain --resume-from-checkpoint
  </behavior>
  <action>
    Per D-05, do not edit nnue_format/schema.py, training/model.py, or training/train.py. Record the lock by reading ARCH_ID and FEATURE_SET from nnue_format.schema when writing sidecar fields.

    Create .planning/phases/07-nnue-strength-recovery/install_phase7_net.py (snake_case, from __future__ import annotations). Define ENGINE_NET, SIDECAR_PATH, PHASE_DIR, write_sidecar(meta: dict) -&gt; None that merges RESEARCH keys (phase 7, from_scratch true, arch_id, feature_set, hf_max_positions 750000, lichess_month 2013-01, min_has_result_rate 0.15, n_merged, has_result_rate, best_elo, best_elo_epoch, best_val_loss, k_scale, installed_utc) and dumps RFC JSON. Define install_export(src: Path) that shutil.copy2 to ENGINE_NET (M4 commit step, not the cloud closer). Define sidecar_identity_payload(data: dict) -&gt; bool for phase 7 and from_scratch true. Define M4_PIPELINE_ARGV as the exact flag list from 07-RESEARCH.md M4 CLI flags (python -m training.run_pipeline …). Do not execute that CLI in this task.

    Write tests/training/test_phase7_sidecar.py covering the five behaviors. Use tmp_path for SIDECAR_PATH / ENGINE_NET via monkeypatch. Do not overwrite ance/eval/nnue/net.safetensors in the unit tests.
  </action>
  <acceptance_criteria>
    - "install_phase7_net.py defines write_sidecar, install_export, M4_PIPELINE_ARGV"
    - "Sidecar arch_id equals schema.ARCH_ID; M4_PIPELINE_ARGV has no resume flag"
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/training/test_phase7_sidecar.py tests/training/test_nnue_format_roundtrip.py -q -x</automated>
    <fails_when>pytest exits non-zero, sidecar arch_id mismatch versus schema.ARCH_ID, or M4_PIPELINE_ARGV contains resume-from-checkpoint</fails_when>
  </verify>
  <done>Sidecar/install contract is tested. Architecture files unchanged. Net not yet overwritten.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>M4 from-scratch train and commit net plus sidecar (D-07, D-08, D-13, D-15, D-16)</name>
  <precondition>07-01 has landed --lichess-max-samples and the 4-field FEN pad; M4 has torch.backends.mps.is_available() True</precondition>
  <action>Run one from-scratch MPS train on the PROJECT M4 using the pinned M4_PIPELINE_ARGV, then commit the export over ance/eval/nnue/net.safetensors plus 07-NET-SIDECAR.json.</action>
  <instructions>
    Claude on this cloud host must not invoke training.run_pipeline or any CPU train (D-14).

    On the M4 sitting:
    1. Confirm `python -c "import torch; assert torch.backends.mps.is_available()"` exits 0.
    2. Confirm extras: `pip install -e '.[hf-ingest]'` if huggingface_hub / pyarrow missing.
    3. Confirm Lichess 2013-01 dump sha256 aa40b3671fa3cf1072eb182892cd90b0e1e003a4a5943492f64b77e7f3fd1635 (re-check database.lichess.org/standard/sha256sums.txt before curl).
    4. Run the exact CLI in 07-RESEARCH.md M4 CLI flags (also M4_PIPELINE_ARGV). Do not pass --resume-from-checkpoint (D-08). Do not change AdamW/LR flags (D-06).
    5. If the first in-train probe exceeds 25 minutes, drop remaining probes to 8 games (still D-07). If kept HF after quiet is under 30000, stop honestly — do not start a second train (D-11).
    6. After export, run `.planning/phases/07-nnue-strength-recovery/install_phase7_net.py` (or call install_export + write_sidecar) so ance/eval/nnue/net.safetensors is the from-scratch net and 07-NET-SIDECAR.json has phase 7, from_scratch true, n_merged, has_result_rate, best_elo, k_scale, installed_utc.
    7. Commit net + sidecar on the working branch (D-16). Push so the cloud closer can see them.

    If the M4 sitting is not available, resume blocked so Plan 07-04 writes blocked evidence (D-14). Do not train on this CPU host as a substitute.
  </instructions>
  <verification>
    Either 07-NET-SIDECAR.json exists with phase 7 and from_scratch true, or the operator resumes blocked. `python -c "import torch; print(torch.backends.mps.is_available())"` on this cloud host stays False and no training-run-output from a local train appears.
  </verification>
  <resume-signal>Type trained after net+sidecar are committed, or blocked to proceed to Plan 07-04 without a Phase 7 net.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| M4 disk export → git-tracked net | Public weights; must match nnue_format shapes |
| Optional .pt on M4 → torch.load | Self-written only; D-08 forbids resume from Phase 6 |
| This CPU host → train CLI | Must not cross; D-14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | resume .pt | medium | mitigate | M4_PIPELINE_ARGV omits --resume-from-checkpoint; train.py already uses weights_only=True |
| T-07-04 | Denial of Service | 42 GB HF snapshot | medium | mitigate | Existing per-shard download + hf_max_positions 750000 + max_kept |
| T-07-07 | Elevation of Privilege | pickle in weights | medium | mitigate | Export safetensors via nnue_format only; closer/engine never torch.load the packaged net |
| T-07-SC | Tampering | pip installs | high | mitigate | No new packages; hf-ingest extras already approved |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/training/test_phase7_sidecar.py tests/training/test_nnue_format_roundtrip.py -q -x`
- After trained resume: sidecar JSON parses with phase 7 and from_scratch true
- After blocked resume: no local train artifacts required
</verification>

<success_criteria>
- D-05 lock recorded; schema files unchanged
- Install helper + tests green
- Either a committed Phase 7 sidecar+net or an explicit blocked resume into 07-04
</success_criteria>

<output>
Create `.planning/phases/07-nnue-strength-recovery/07-03-SUMMARY.md` when done
</output>
