---
phase: 07-nnue-strength-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-nnue-strength-recovery/post_train_close_07.py
  - tests/training/test_phase7_closer_evidence.py
autonomous: true
requirements:
  - TOOL-04
must_haves:
  truths:
    - "Closer requires 07-NET-SIDECAR.json with phase 7 and from_scratch true before any play; missing sidecar writes blocked RFC JSON and does not launch a gauntlet — D-14"
    - "Ladder is diagnostics then 16-game smoke then 200 then 1000, all fixed depth 3 — D-09, D-10, Phase 5 D-10–D-12"
    - "Smoke abort skips 200 when wins==0 OR score_rate==0.0 OR (elo_ci_high is not None AND elo_ci_high < -200) — D-10, D-11"
    - "Smoke or 200 failure writes evidence and does not start 1000 games and does not start a train — D-11"
    - "TOOL-04 pass is Elo point estimate > 0 and 95 percent CI low > 0 at >=1000 games; useful-fail vs Phase 6 0-200 still lists gates_failed D-12 and TOOL-04 — D-12"
    - "Evidence schema_version 1 includes probe_smoke, compare_phase6, blocked; shutouts serialize as JSON null via json_safe_number and allow_nan False"
    - "Pinned Discretion (copy): smoke=16; abort if wins==0 OR score_rate==0.0 OR elo_ci_high < -200"
    - "Pinned Discretion (copy): sidecar 07-NET-SIDECAR.json; closer post_train_close_07.py; evidence 07-GAUNTLET-EVIDENCE.json"
  artifacts:
    - path: .planning/phases/07-nnue-strength-recovery/post_train_close_07.py
      provides: "Phase 7 measure-only closer"
      contains: "SMOKE_GAMES"
    - path: tests/training/test_phase7_closer_evidence.py
      provides: "blocked / smoke abort / schema / shutout nulls"
      contains: "test_blocked_without_sidecar"
  key_links:
    - from: .planning/phases/07-nnue-strength-recovery/post_train_close_07.py
      to: .planning/phases/07-nnue-strength-recovery/07-NET-SIDECAR.json
      via: "sidecar_identity_ok before run_diagnostics"
      pattern: "sidecar_identity_ok"
    - from: .planning/phases/07-nnue-strength-recovery/post_train_close_07.py
      to: training/elo_probe.py
      via: "run_elo_probe n_games=16 then maybe 200"
      pattern: "SMOKE_GAMES"
    - from: .planning/phases/07-nnue-strength-recovery/post_train_close_07.py
      to: ance/tools/gauntlet.py
      via: "ANCE_EVAL nnue vs handcrafted at SEARCH_DEPTH 3"
      pattern: "ANCE_EVAL"
  prohibitions:
    - statement: "Do not add king-bucketed feature sets or change hidden width"
      status: enforced
    - statement: "Do not start a CPU train on this host"
      status: enforced
    - statement: "Do not complete-phase 6 while TOOL-04 is open"
      status: enforced
    - statement: "Do not start a second in-phase train"
      status: enforced

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
---

<objective>
Build the Phase 7 measure-only closer: sidecar identity gate, diagnostics, 16-game smoke, 200-game probe, optional 1000-game TOOL-04, and RFC evidence with blocked / useful-fail paths.

Purpose: File presence of ance/eval/nnue/net.safetensors is not proof of a Phase 7 train (Phase 6 net is already there). Without the sidecar gate the cloud host would re-measure the 0-200 net.
Output: post_train_close_07.py plus tests/training/test_phase7_closer_evidence.py green.
</objective>

<execution_context>
@/workspace/.cursor/gsd-core/workflows/execute-plan.md
@/workspace/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-nnue-strength-recovery/07-CONTEXT.md
@.planning/phases/07-nnue-strength-recovery/07-RESEARCH.md
@.planning/phases/07-nnue-strength-recovery/07-PATTERNS.md
@.planning/phases/07-nnue-strength-recovery/07-VALIDATION.md
@.planning/phases/06-quiet-data-nnue-strength-gap/post_train_close_06.py
@tests/training/test_phase6_closer_evidence.py
@.planning/phases/06-quiet-data-nnue-strength-gap/06-GAUNTLET-EVIDENCE.json
@training/elo_probe.py
@training/diagnostics_eval.py
@ance/tools/gauntlet.py
</context>

## Artifacts this phase produces

- `training/data/hf_ingest.py` — 4-field FEN pad — **Plan 07-01**
- `training/run_pipeline.py` — `--lichess-max-samples`, `max_kept` — **Plan 07-01**
- `training/data/quiet_filter.py` — `max_kept` — **Plan 07-01**
- `.planning/phases/07-nnue-strength-recovery/post_train_close_07.py` — **Plan 07-02**
- `tests/training/test_phase7_closer_evidence.py` — **Plan 07-02**
- `.planning/phases/07-nnue-strength-recovery/install_phase7_net.py` — **Plan 07-03**
- `.planning/phases/07-nnue-strength-recovery/07-NET-SIDECAR.json` — **Plan 07-03**
- `ance/eval/nnue/net.safetensors` — from-scratch overwrite — **Plan 07-03**
- `.planning/phases/07-nnue-strength-recovery/07-GAUNTLET-EVIDENCE.json` — **Plan 07-04**

New symbols/flags: `SMOKE_GAMES`, `SIDECAR`, `PHASE6_BASELINE`, `sidecar_identity_ok`, `smoke_abort`, `write_blocked_evidence`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: post_train_close_07.py closer (D-09–D-14)</name>
  <files>.planning/phases/07-nnue-strength-recovery/post_train_close_07.py</files>
  <read_first>.planning/phases/07-nnue-strength-recovery/07-RESEARCH.md Evidence Schema and Pattern 4; .planning/phases/07-nnue-strength-recovery/07-PATTERNS.md post_train_close_07 section; .planning/phases/06-quiet-data-nnue-strength-gap/post_train_close_06.py entire file; training/elo_probe.py json_safe_number probe_summary run_elo_probe</read_first>
  <behavior>
    - Tested in Task 2: sidecar miss writes blocked JSON; smoke abort skips 200; shutout nulls; schema keys
  </behavior>
  <action>
    Copy .planning/phases/06-quiet-data-nnue-strength-gap/post_train_close_06.py to .planning/phases/07-nnue-strength-recovery/post_train_close_07.py. Keep the path bootstrap (ROOT = parents[3], chdir, sys.path). Keep PROBE_GAMES=200, D12_GAMES=1000, SEARCH_DEPTH=3, ENGINE_ARGV, MAX_HALFMOVES=160, BUDGET_SECONDS=172800, PROBE_BUDGET_SECONDS=64800. Rename PHASE_DIR to 07-nnue-strength-recovery, EVIDENCE to 07-GAUNTLET-EVIDENCE.json, CHECKPOINT / LIVE / LOG / STATE to 07-prefixed names. Add SMOKE_GAMES=16 and SIDECAR = PHASE_DIR / 07-NET-SIDECAR.json.

    Per D-14 do not copy the Phase 6 fallback that installs STRENGTH_NET or ENGINE_NET when a strength-run file is missing. Define sidecar_identity_ok() that returns True only when SIDECAR is a file and json.loads has phase equal to 7 (int or 7) and from_scratch is True. On failure call write_blocked_evidence() and return from main without run_diagnostics, run_elo_probe, or run_gauntlet. Blocked payload matches 07-RESEARCH.md: reason phase7_net_not_installed, detail names missing sidecar or phase mismatch and refusing to measure the prior net, engine_net ance/eval/nnue/net.safetensors, probe_smoke null, probe_200 null, gauntlet status blocked depth 3, gates_failed D-14 and TOOL-04, json.dumps allow_nan False.

    Keep EngineSpec env ANCE_EVAL nnue vs handcrafted and search_depth=SEARCH_DEPTH (D-09, Phase 5 D-11). After sidecar pass: run_diagnostics(str(ENGINE_NET)); on any not ok write evidence and return (D-10). Then run_elo_probe(..., n_games=SMOKE_GAMES, out_dir=PHASE_DIR / probe-smoke). Define smoke_abort(agg) True when wins==0 OR score_rate==0.0 OR (elo_ci_high is not None AND elo_ci_high &lt; -200) per D-10. On abort write evidence with probe_smoke filled, probe_200 null, gates_failed including D-10 or D-11 plus TOOL-04, and return — do not start 200, 1000, or a train (D-11).

    If smoke passes, run the existing 200-game probe gate (elo_ci_low &gt; 0 and n_games &gt;= 200). On fail write evidence and return (D-11). Only then _run_depth_gauntlet(1000). Omit the optional clock gauntlet (Phase 7 budget is smoke/200/1000).

    Extend _write_evidence with blocked and probe_smoke (via probe_summary). Always include compare_phase6 constant PHASE6_BASELINE from 07-RESEARCH.md / 07-PATTERNS.md (wins 0, losses 200, draws 0, elo_ci_high -686.6071411804116, n_merged 19866, best_elo None). D-12 useful-fail (200 better than 0-200 but TOOL-04 unmet) still sets gates_failed to D-12 and TOOL-04. Catch probe exceptions, write evidence, return 2. Never call torch train or subprocess the pipeline from this script (D-14).
  </action>
  <acceptance_criteria>
    - "SIDECAR gate runs before diagnostics"
    - "SMOKE_GAMES is 16; SEARCH_DEPTH is 3; D12_GAMES is 1000"
    - "write_blocked_evidence and smoke_abort are importable helpers"
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "from importlib.machinery import SourceFileLoader; m=SourceFileLoader('c','.planning/phases/07-nnue-strength-recovery/post_train_close_07.py').load_module(); assert m.SMOKE_GAMES==16 and m.SEARCH_DEPTH==3 and m.D12_GAMES==1000 and callable(m.sidecar_identity_ok) and callable(m.smoke_abort)"</automated>
    <fails_when>ImportError, AssertionError, or AttributeError for SMOKE_GAMES / sidecar_identity_ok / smoke_abort</fails_when>
  </verify>
  <done>Closer script exists with identity gate, smoke abort, and RFC writer. Not yet executed against a net.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 7 closer evidence tests (TOOL-04 / D-10 / D-14)</name>
  <files>tests/training/test_phase7_closer_evidence.py</files>
  <read_first>tests/training/test_phase6_closer_evidence.py entire file; .planning/phases/07-nnue-strength-recovery/07-RESEARCH.md Validation Architecture and Evidence Schema; .planning/phases/07-nnue-strength-recovery/07-PATTERNS.md test_phase7_closer_evidence section</read_first>
  <behavior>
    - Test 1: test_write_evidence_schema — schema_version 1 and keys include probe_smoke, compare_phase6, blocked
    - Test 2: test_shutout_probe_serializes_without_nan — elo and elo_ci_low JSON null; raw text has no Infinity or NaN
    - Test 3: test_blocked_without_sidecar — missing sidecar → blocked.reason phase7_net_not_installed, gauntlet.status blocked, gates_failed contains D-14 and TOOL-04; monkeypatched run_elo_probe and run_gauntlet raise if invoked
    - Test 4: test_smoke_abort_skips_200 — wins 0 or score_rate 0.0 or elo_ci_high &lt; -200 → evidence written and 200-game probe not started
    - Test 5: compare_phase6 matches PHASE6_BASELINE values from 06-GAUNTLET-EVIDENCE.json
  </behavior>
  <action>
    Copy the SourceFileLoader pattern from tests/training/test_phase6_closer_evidence.py. Point it at .planning/phases/07-nnue-strength-recovery/post_train_close_07.py. Monkeypatch EVIDENCE, CHECKPOINT, and SIDECAR onto tmp_path. Implement the five behaviors. For test_blocked_without_sidecar and test_smoke_abort_skips_200, invoke main() (or write_blocked_evidence / smoke_abort + a thin main wrapper) with run_elo_probe / run_gauntlet / run_diagnostics patched: diagnostics all ok when the smoke path is under test; raise AssertionError if a 200-game or 1000-game call happens after abort or when sidecar is missing.

    Do not hand-author a passing 07-GAUNTLET-EVIDENCE.json in the phase directory. Do not import ance from a new training module that would reverse the layering rule.
  </action>
  <acceptance_criteria>
    - "All five named tests exist and pass"
    - "Blocked path never calls run_gauntlet"
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/training/test_phase7_closer_evidence.py tests/training/test_phase6_closer_evidence.py tests/test_nnue_gauntlet_depth.py tests/training/test_diagnostics_eval.py -q -x</automated>
    <fails_when>pytest exits non-zero, missing test_blocked_without_sidecar, or a test that invokes run_gauntlet on the blocked path</fails_when>
  </verify>
  <done>Closer contract is unit-tested: blocked sidecar, smoke abort, RFC shutouts, compare_phase6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidecar / net files on disk → closer | Identity check; prior-phase net must not be treated as Phase 7 |
| Operator env → gauntlet | ANCE_EVAL allowlist already in engine; closer only sets nnue / handcrafted |
| Evidence JSON → humans / verify-work | Must be RFC JSON (no NaN/Inf) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | --net / ANCE_NNUE_PATH | medium | mitigate | Closer reads in-tree ENGINE_NET only after sidecar identity; safetensors load via existing nnue_format |
| T-07-03 | Tampering | .pt resume | medium | mitigate | Closer never loads checkpoints; D-08 / D-14 forbid train and resume here |
| T-07-06 | Repudiation | 07-GAUNTLET-EVIDENCE.json | low | mitigate | git_commit + captured_utc + allow_nan False; honest gates_failed |
| T-07-05 | Information Disclosure | closer logs | low | accept | Local measure CLI |
| T-07-SC | Tampering | pip installs | high | mitigate | No new packages this plan |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/training/test_phase7_closer_evidence.py tests/training/test_phase6_closer_evidence.py tests/test_nnue_gauntlet_depth.py -q -x`
- After wave: `.venv/bin/python -m pytest tests/ -q -m 'not slow'`
</verification>

<success_criteria>
- Missing sidecar cannot start a gauntlet (D-14)
- Hopeless smoke cannot start 200 games (D-10/D-11)
- Evidence schema ready for Plan 07-04
</success_criteria>

<output>
Create `.planning/phases/07-nnue-strength-recovery/07-02-SUMMARY.md` when done
</output>
