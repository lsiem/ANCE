---
phase: 05-nnue-swap-in-elo-gauntlet
plan: 04
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - ance/eval/nnue/net.safetensors
  - .planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json
  - tests/test_phase5_elo_evidence.py
autonomous: true
requirements:
  - TOOL-04
must_haves:
  truths:
    - "A new supervised net beats handcrafted at depth 3 with Elo > 0 and elo_ci_low > 0 over ≥1000 games"
    - "Committed 05-GAUNTLET-EVIDENCE.json has gates_passed including D-12 and TOOL-04"
    - "Pre-gauntlet smoke: ≥20-game depth-3 score_rate > 0.45 OR material/startpos goldens pass before overnight run"
  artifacts:
    - path: ance/eval/nnue/net.safetensors
      provides: "Retrained weights that clear D-12"
    - path: .planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json
      provides: "TOOL-04 proof with gates_passed"
---

## Phase Goal

**As a** chess engine developer, **I want to** close the Phase 5 D-12 / TOOL-04 gap left by Plan 05-03, **so that** the milestone's defining Elo payoff is actually proven.

## Context from 05-03

Clean evidence (2026-07-22): **0W–1000L–0D**, Elo −∞, `gates_failed: ["D-12","TOOL-04"]`.

HF-primary training (~250k rows → ~37k unique FENs) produced a net that:

- Mis-scores material-winning positions (e.g. rook-up ≈ −380 cp vs handcrafted ≈ +200)
- Fails startpos exact-0 golden
- Loses every depth-3 game under identical search

Harness is not the bug (incremental NNUE vs fresh eval: 0 mismatches / 2111).

## Objective

Retrain / replace `ance/eval/nnue/net.safetensors` with a stronger supervised net, smoke-test, then re-run the ≥1000-game depth-3 gauntlet until D-12 passes and evidence records `gates_passed: ["D-12","TOOL-04"]`.

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Stronger training data + export</name>
  <files>ance/eval/nnue/net.safetensors, .planning/phases/04-offline-nnue-training-pipeline/</files>
  <action>
    Pick one primary path (prefer the first available):

    1. Resume Mac SF 1M depth-12 labeling from local progress JSON, train, export, install.
    2. Larger HF stream (`Lichess/chess-position-evaluations`) with mate filtering + `|cp|` clip; verify rook-up / startpos goldens before install.
    3. Blend Lichess PGN `[%eval]` (result-bearing for K-fit) + HF + fresh SF.

    Acceptance before gauntlet: either (a) `pytest tests/test_nnue_eval.py -q` goldens that the net is expected to pass, or (b) ≥20-game depth-3 smoke with `score_rate > 0.45` vs handcrafted.
  </action>
  <done>Installed net clears smoke gate; metrics/manifest recorded under Phase 4 run-output.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run D-12 evidence</name>
  <files>.planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json, tests/test_phase5_elo_evidence.py</files>
  <action>
    Restart gauntlet with a fresh checkpoint (or `--restart` if parameters mismatch). Depth 3, 1000 games, same EngineSpec env pattern as 05-03. Finalize evidence; require `gates_passed` includes D-12 and TOOL-04. Slow pytest must exit 0.
  </action>
  <done>TOOL-04 closed; Phase 5 ready for `/gsd-verify-work`.</done>
</task>

</tasks>

## Non-goals

- Changing search to bail out a weak NNUE
- Claiming TOOL-04 with &lt;1000 games or without `elo_ci_low > 0`
- Lowering acceptance depth solely to pass CI without smoke proof

## Success criteria

- `05-GAUNTLET-EVIDENCE.json` has `games ≥ 1000`, `depth == 3`, `elo > 0`, `elo_ci_low > 0`, `gates_passed` includes `D-12` and `TOOL-04`
- `pytest tests/test_phase5_elo_evidence.py -m slow -x` exits 0
- REQUIREMENTS.md TOOL-04 checked; ROADMAP Phase 5 complete

## Output

Create `05-04-SUMMARY.md` when done.
