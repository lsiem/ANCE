---
phase: 05-nnue-swap-in-elo-gauntlet
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - tests/test_phase5_elo_evidence.py
  - .planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json
autonomous: true
requirements:
  - TOOL-04
must_haves:
  truths:
    - "A ≥1000-game fixed-opening-book gauntlet at depth 3 compares NNUE (engine A) vs handcrafted (engine B) with identical argv and only ANCE_EVAL env differing — D-04, D-10, D-11"
    - "Reported Elo point estimate is > 0 and 95% CI lower bound is > 0 — D-12 pass gate"
    - "Evidence JSON committed with games≥1000, mode=fixed_depth, depth=3, runner name, Wilson bounds, logistic Elo, git commit, gates_passed includes TOOL-04"
    - "Gauntlet supports checkpoint resume; interrupted run can continue without replaying completed games"
  artifacts:
    - path: tests/test_phase5_elo_evidence.py
      provides: "slow-marked D-12/TOOL-04 gate + assertion helpers"
      contains: "assert_positive_elo_with_ci"
    - path: .planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json
      provides: "committed machine-readable TOOL-04 proof"
  key_links:
    - from: tests/test_phase5_elo_evidence.py
      to: ance/tools/gauntlet.py
      via: "run_gauntlet(spec_nnue, spec_hc, search_depth=3, n_games=1000)"
      pattern: "run_gauntlet"
    - from: tests/test_phase5_elo_evidence.py
      to: ance/uci/loop.py
      via: "real subprocess engines with ANCE_EVAL env"
      pattern: "ANCE_EVAL"
---

## Phase Goal

**As a** chess engine developer validating the ANCE milestone, **I want to** drop the trained NNUE behind the untouched evaluator seam and measure it against the handcrafted baseline in a rigorous gauntlet, **so that** we prove measurable Elo gain from supervised training.

## Artifacts this phase produces

*(Full phase-wide inventory — identical across all Phase 5 plans.)*

- `ance/eval/nnue/__init__.py`, `features.py`, `inference.py`, `eval.py` — zero-torch NNUE inference — **Plan 05-01**
- `ance/eval/nnue/net.safetensors` — git-tracked default weights copied from Phase 4 run-output — **Plan 05-01**
- `ance/uci/loop.py` — `resolve_evaluator()` + `ANCE_EVAL` / `ANCE_NNUE_PATH` fail-fast — **Plan 05-01**
- `ance/tools/gauntlet.py` — `EngineSpec.env`, `--depth`, logistic Elo fields — **Plan 05-02**
- `tests/test_nnue_eval.py` — parity, goldens, env wiring, structural seam — **Plan 05-01**; search-config diff — **Plan 05-02**
- `tests/nnue_parity_helpers.py` — shared torch/numpy oracle (tests only) — **Plan 05-01**
- `tests/test_nnue_gauntlet_depth.py` — depth mode + env injection contracts — **Plan 05-02**
- `tests/test_phase5_elo_evidence.py` — slow ≥1000-game D-12 gate — **Plan 05-03**
- `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json` — committed TOOL-04 evidence — **Plan 05-03**

<objective>
Run the milestone acceptance measurement: ≥1000-game fixed-depth gauntlet proving NNUE beats handcrafted with positive Elo and 95% CI lower bound > 0, and commit reproducible evidence for `/gsd-verify-work`.

Purpose: TOOL-04 is the milestone payoff — supervised training must show measurable strength gain under identical search.
Output: Green slow test, committed `05-GAUNTLET-EVIDENCE.json`, exact command line in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-CONTEXT.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-RESEARCH.md
@ance/tools/gauntlet.py
@ance/tools/openings.epd
@tests/test_phase3_gauntlet_evidence.py
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-12 assertion helpers + fast contract tests</name>
  <files>tests/test_phase5_elo_evidence.py</files>
  <read_first>tests/test_phase3_gauntlet_evidence.py, 05-CONTEXT.md D-10..D-12, 05-RESEARCH.md Elo formula</read_first>
  <behavior>
    - Test 1: synthetic report with elo=25, elo_ci_low=5 passes `assert_positive_elo_with_ci`; elo_ci_low=0 fails naming D-12
    - Test 2: synthetic report with n_games=999 fails a `assert_minimum_games(report, 1000)` helper naming D-10
    - Test 3: `load_evidence(path)` parses minimal inline evidence JSON fixture (schema_version, git_commit, gauntlet, gates_passed) written to `tmp_path`; missing path raises with actionable message — does **not** require committed `.planning/.../05-GAUNTLET-EVIDENCE.json` (Task 2 creates that artifact)
    - Test 4: slow gauntlet test function carries `@pytest.mark.slow`; `-m "not slow"` excludes it
  </behavior>
  <action>
    Create `tests/test_phase5_elo_evidence.py` (docstring citing TOOL-04, D-10..D-12, D-09). Constants:
    - `D12_GAMES = 1000`
    - `SEARCH_DEPTH = 3` (RESEARCH recommendation — ~4–8h projected)
    - `ENGINE_ARGV = [sys.executable, "-m", "ance"]`
    - `MAX_HALFMOVES = 160`

    Pure helpers: `assert_positive_elo_with_ci(report)`, `assert_minimum_games(report, n=1000)`, `load_evidence(path)`.

    For Test 3, follow `tests/test_phase3_gauntlet_evidence.py::test_load_evidence_parses_schema_and_explains_missing_artifact`: write a minimal inline fixture to `tmp_path / "evidence.json"` with keys `schema_version`, `git_commit`, `gauntlet` (at least `games`), and `gates_passed`; assert round-trip parse. Missing-file test targets a nonexistent path under `tmp_path` only — **do not** read or assert on `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json` in Task 1 (committed artifact + path assertion deferred to Task 2 step 5).

    Fast tests 1–4 only in RED/GREEN for this task. Stub `@pytest.mark.slow` test body with `pytest.skip("Task 2")`.
  </action>
  <acceptance_criteria>
    - "Helper unit tests pass"
    - "`.venv/bin/python -m pytest tests/test_phase5_elo_evidence.py -m 'not slow' -q` exits 0"
    - "`.venv/bin/python -m pytest -m 'not slow' -q` exits 0"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_phase5_elo_evidence.py -m "not slow" -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>D-12 gates encoded in test helpers; slow suite isolated from CI fast lane.</done>
</task>

<task type="auto">
  <name>Task 2: ≥1000-game NNUE vs handcrafted evidence run</name>
  <files>tests/test_phase5_elo_evidence.py, .planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json</files>
  <read_first>ance/tools/gauntlet.py, 05-CONTEXT.md D-04, D-09, D-11, tests/test_phase3_gauntlet_evidence.py Task 2 pattern</read_first>
  <action>
    Implement `@pytest.mark.slow` test `test_phase5_thousand_game_nnue_vs_handcrafted_evidence`:

    1. Preconditions: Plans 05-01 and 05-02 complete; `ance/eval/nnue/net.safetensors` exists; gauntlet accepts `--depth`.

    2. Engine specs (D-04):
       - `spec_nnue = EngineSpec("nnue", ENGINE_ARGV, env={"ANCE_EVAL": "nnue"})` — engine A (score perspective)
       - `spec_hc = EngineSpec("handcrafted", ENGINE_ARGV, env={"ANCE_EVAL": "handcrafted"})`

    3. Run via `gauntlet.run_gauntlet`:
       - `n_games=1000`, `search_depth=3`, openings from `gauntlet.DEFAULT_OPENINGS` (31-line book, cycles per Phase 3 D-16)
       - Checkpoint path: env override `ANCE_PHASE5_GAUNTLET_CHECKPOINT` or pytest `tmp_path` during test; **resume** if interrupted (no `--restart` unless parameters mismatch)
       - Optional `--budget-seconds` 28800 (8h) watchdog via harness deadline parameter
       - Record `detect_runner()` in evidence (arbiter expected on this machine per D-09)

    4. Assert on completion:
       - `assert_minimum_games(report, 1000)`
       - `assert_positive_elo_with_ci(report)` — D-12
       - `report["parameters"]["mode"] == "fixed_depth"` and `search_depth == 3`
       - Only `ANCE_EVAL` differs between engine_a and engine_b env in parameters
       - `status == "completed"`

    5. Write committed `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json` (first time this path exists in the repo — Task 1 schema test used `tmp_path` only):
       - schema_version, git_commit, captured_utc
       - gauntlet: games, mode, depth, wins/losses/draws, score_rate, wilson_low/high, elo, elo_ci_low/high, runner, command_line, elapsed_s
       - gates_passed: ["D-12", "TOOL-04"]

    6. If CI lower bound is marginal, document one rerun in SUMMARY; D-10 forbids SPRT early-stop but allows full restart.

    Wall-clock: expect ~4–8 hours at depth 3 with NNUE overhead (RESEARCH benchmark). Executor may run overnight; checkpoint resume is mandatory if interrupted.
  </action>
  <acceptance_criteria>
    - "Slow test passes locally after gauntlet completes"
    - "Evidence JSON exists with games≥1000 and elo_ci_low > 0"
    - "`.venv/bin/python -m pytest tests/test_phase5_elo_evidence.py -m slow -x` exits 0 (phase gate)"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_phase5_elo_evidence.py -m slow -x</automated>
  </verify>
  <done>TOOL-04 satisfied: measurable positive Elo gain with statistical gate and committed evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence artifact → milestone verification | Committed JSON is trust anchor for TOOL-04 |
| Long-running gauntlet | External process integrity over multi-hour window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-10 | Tampering | False Elo evidence | high | mitigate | Test runs real engines; evidence includes git_commit, command_line, parameters env diff; reproducible rerun |
| T-5-11 | Repudiation | Incomplete game count | medium | mitigate | assert_minimum_games(1000); checkpoint records per-game outcomes |
| T-5-12 | Spoofing | Both engines same eval | high | mitigate | Parameters assert ANCE_EVAL differs; Plan 05-02 env injection tests |
| T-5-13 | Denial of Service | Run exceeds wall-clock | medium | accept | Checkpoint resume + optional budget-seconds; depth 3 chosen for overnight fit |
| T-5-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages in evidence plan |
</threat_model>

<verification>
- Fast lane: `.venv/bin/python -m pytest -m "not slow" -q`
- Phase gate: `.venv/bin/python -m pytest tests/test_phase5_elo_evidence.py -m slow -x`
- Evidence file present: `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json`
- Manual: inspect evidence for runner, depth=3, games=1000, elo_ci_low > 0
</verification>

<success_criteria>
- TOOL-04 complete: ≥1000-game gauntlet, positive Elo, 95% CI lower bound > 0
- D-09..D-12 honored in evidence artifact
- Phase 5 milestone payoff proven — ready for `/gsd-verify-work`
</success_criteria>

<output>
Create `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-03-SUMMARY.md` when done
</output>
