---
phase: 05-nnue-swap-in-elo-gauntlet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ance/eval/nnue/__init__.py
  - ance/eval/nnue/features.py
  - ance/eval/nnue/inference.py
  - ance/eval/nnue/eval.py
  - ance/eval/nnue/net.safetensors
  - ance/uci/loop.py
  - tests/test_nnue_eval.py
  - tests/nnue_parity_helpers.py
autonomous: true
requirements:
  - EVAL-03
must_haves:
  truths:
    - "`ANCE_EVAL=nnue` starts the engine with a numpy `NnueEval` that loads git-tracked weights from `ance/eval/nnue/net.safetensors` (or `ANCE_NNUE_PATH`) and returns side-to-move-relative integer cp from `evaluate(position)` — D-01, D-02, D-05, D-07, D-08"
    - "Invalid `ANCE_EVAL` or missing/invalid weights when `ANCE_EVAL=nnue` exits non-zero with stderr guidance — no silent handcrafted fallback — D-03, D-06"
    - "Torch↔numpy parity is exact integer cp on 40 held-out FENs; king-only symmetric positions score exactly 0; color-mirror+STM-flip scores match; Stockfish sign agrees on 8 won/lost positions when SF is on PATH — D-13, D-14, D-15, D-16"
    - "Search modules never import concrete evaluators; `ance/` never imports `training/` — EVAL-01 seam preserved"
  artifacts:
    - path: ance/eval/nnue/eval.py
      provides: "NnueEval(Evaluator) with strict load_net startup"
      contains: "class NnueEval"
    - path: ance/eval/nnue/net.safetensors
      provides: "git-tracked Phase 4 approved weights (~790 KB)"
    - path: ance/uci/loop.py
      provides: "resolve_evaluator() wired at module init"
      contains: "resolve_evaluator"
    - path: tests/test_nnue_eval.py
      provides: "parity, goldens, env fail-fast, structural seam proofs"
  key_links:
    - from: ance/uci/loop.py
      to: ance/eval/nnue/eval.py
      via: "resolve_evaluator() constructs NnueEval when ANCE_EVAL=nnue"
      pattern: "ANCE_EVAL"
    - from: ance/eval/nnue/eval.py
      to: nnue_format/io.py
      via: "load_net(path) strict arch_id/feature_set/shapes"
      pattern: "load_net"
    - from: tests/test_nnue_eval.py
      to: training/model.py
      via: "torch parity oracle only under tests/ with @pytest.mark.torch"
      pattern: "torch_cp_int"
---

## Phase Goal

**As a** chess engine developer validating the ANCE milestone, **I want to** drop the trained NNUE behind the untouched evaluator seam and measure it against the handcrafted baseline in a rigorous gauntlet, **so that** we prove measurable Elo gain from supervised training.

> Note: ROADMAP Phase 5 `**Goal:**` is capability-shaped; this user story is derived from that goal plus EVAL-03/TOOL-04 success criteria (same precedent as Phase 4 Plan 04-01).

## Artifacts this phase produces

*(Full phase-wide inventory — identical across all Phase 5 plans.)*

- `ance/eval/nnue/__init__.py`, `features.py`, `inference.py`, `eval.py` — zero-torch NNUE inference — **Plan 05-01**
- `ance/eval/nnue/net.safetensors` — git-tracked default weights copied from Phase 4 run-output — **Plan 05-01**
- `ance/uci/loop.py` — `resolve_evaluator()` + `ANCE_EVAL` / `ANCE_NNUE_PATH` fail-fast — **Plan 05-01**
- `ance/tools/gauntlet.py` — `EngineSpec.env`, `--depth`, logistic Elo fields — **Plan 05-02**
- `tests/test_nnue_eval.py` — parity, goldens, env wiring, structural seam — **Plan 05-01**
- `tests/nnue_parity_helpers.py` — shared torch/numpy oracle (tests only) — **Plan 05-01**
- `tests/test_nnue_gauntlet_depth.py` — depth mode + env injection contracts — **Plan 05-02**
- `tests/test_phase5_elo_evidence.py` — slow ≥1000-game D-12 gate — **Plan 05-03**
- `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json` — committed TOOL-04 evidence — **Plan 05-03**

<objective>
Deliver the Phase 5 core vertical slice: numpy `NnueEval` behind the existing `Evaluator` seam, Phase 4 weights copied into the package, `ANCE_EVAL`/`ANCE_NNUE_PATH` wired in the UCI loop with fail-fast semantics, and automated parity + perspective goldens proving correctness before any gauntlet run.

Purpose: EVAL-03 is the milestone's eval swap; without exact parity and goldens, a negative Elo result would be uninterpretable.
Output: Runnable `ANCE_EVAL=nnue` engine path plus `tests/test_nnue_eval.py` green on the fast lane.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-CONTEXT.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-RESEARCH.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-PATTERNS.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-VALIDATION.md
@ance/eval/base.py
@ance/eval/handcrafted.py
@ance/debug.py
@ance/uci/loop.py
@nnue_format/io.py
@nnue_format/schema.py
@training/model.py
@training/data/features.py
@.planning/phases/04-offline-nnue-training-pipeline/run-output/net.safetensors
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing NNUE eval contract tests (RED)</name>
  <files>tests/test_nnue_eval.py, tests/nnue_parity_helpers.py</files>
  <read_first>05-RESEARCH.md Validation Architecture, 05-PATTERNS.md test_nnue_eval section, tests/test_eval_seam.py, tests/training/test_export_pipeline_smoke.py, tests/training/test_features_encoding.py</read_first>
  <behavior>
    - Test 1: `NnueEval()` loads default net and returns an int from `evaluate(Position(startpos))` — fails until module exists
    - Test 2: subprocess with `ANCE_EVAL=bogus` exits non-zero and stderr mentions allowed values (D-03)
    - Test 3: king-only symmetric FEN `4k3/8/8/8/8/8/8/4K3 w - - 0 1` scores exactly 0 for white and black STM (D-14) — not startpos
    - Test 4: `@pytest.mark.torch` parametrized parity on 4 sample FENs — torch int cp equals NnueEval int cp (D-13 stub count; expanded in Task 3)
    - Test 5: `negamax.py` source (non-comment lines) contains neither HandcraftedEval nor NnueEval import strings
  </behavior>
  <action>
    Create `tests/nnue_parity_helpers.py` with `torch_cp_int(model, fen)` and `numpy_cp_int(nnue_eval, fen)` per 05-RESEARCH.md — torch imports allowed here only.

    Create `tests/test_nnue_eval.py` (docstring citing EVAL-03, D-01..D-16). Constants: `SYMMETRIC_FEN`, `ENGINE_MODULE = "ance"`. Implement behaviors 1–5 as failing tests. Mark torch tests `@pytest.mark.torch` with `pytest.importorskip("torch")`. SF sign and color-mirror tests may be stubbed with `pytest.skip("Task 3")` until implementation lands.

    Do not create production modules yet — RED only.
  </action>
  <acceptance_criteria>
    - "New test file collects without import errors for non-torch tests"
    - "At least one test fails because NnueEval or net.safetensors is missing"
    - "`.venv/bin/python -m pytest tests/test_nnue_eval.py -m 'not torch' --collect-only -q` succeeds"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_nnue_eval.py -m "not torch" --collect-only -q</automated>
  </verify>
  <done>Failing test suite defines the EVAL-03 contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NnueEval module + git-tracked weights (GREEN core)</name>
  <files>ance/eval/nnue/__init__.py, ance/eval/nnue/features.py, ance/eval/nnue/inference.py, ance/eval/nnue/eval.py, ance/eval/nnue/net.safetensors</files>
  <read_first>05-PATTERNS.md (eval.py, features.py, inference.py), training/data/features.py, training/model.py, nnue_format/schema.py, .planning/phases/04-offline-nnue-training-pipeline/run-output/net.safetensors</read_first>
  <behavior>
    - Test 1: `NnueEval()` loads and evaluates startpos without exception
    - Test 2: `encode_position` in engine matches `training.data.features.encode_position` on 100 random legal FENs (seed=42)
    - Test 3: copied net passes `load_net` shape/arch assertions (768×256 ft, 512×1 out, arch_id, board768)
  </behavior>
  <action>
    Binary-copy `.planning/phases/04-offline-nnue-training-pipeline/run-output/net.safetensors` to `ance/eval/nnue/net.safetensors` and git-add per D-07.

    Create `ance/eval/nnue/features.py` — copy `training/data/features.py` body verbatim (768 board768 encoder). Create `inference.py` with `forward_cp_float(stm, opp, weights)` using transposed matmul layout from 05-PATTERNS.md and shared `cp_from_nnue_output(raw) -> int(round(raw))` per D-13.

    Create `eval.py`: `NnueEval` loads via `load_net` from `ANCE_NNUE_PATH` or `Path(__file__).with_name("net.safetensors")` (D-05); missing file raises `FileNotFoundError`; schema failures propagate (D-06/D-08). `evaluate(pos)` encodes FEN, runs forward, returns int cp STM-relative with **no extra turn flip** (document in class docstring — Pitfall 3 in RESEARCH).

    Create `__init__.py` exporting `NnueEval`. No `training/` imports anywhere under `ance/`.

    Extend tests: encoder cross-test (behavior 2), net validation (behavior 3). Re-run Task 1 load/symmetric tests — GREEN for module presence.
  </action>
  <acceptance_criteria>
    - "Default net file exists at ance/eval/nnue/net.safetensors"
    - "`.venv/bin/python -m pytest tests/test_nnue_eval.py::test_nnue_loads_default_net tests/test_nnue_eval.py::test_symmetric_positions_score_zero -x -q` passes"
    - "Engine feature encoder matches training encoder on 100 FENs"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_nnue_eval.py::test_nnue_loads_default_net tests/test_nnue_eval.py::test_symmetric_positions_score_zero tests/test_nnue_eval.py::test_engine_features_match_training_encoder -x -q</automated>
  </verify>
  <done>NnueEval evaluates positions with validated weights; encoder bit-identical to training.</done>
</task>

<task type="auto">
  <name>Task 3: ANCE_EVAL wiring + full parity/golden suite</name>
  <files>ance/uci/loop.py, tests/test_nnue_eval.py</files>
  <read_first>ance/debug.py, 05-CONTEXT.md D-01..D-06, 05-RESEARCH.md Claude's Discretion (FEN suite sizes), tests/conftest.py subprocess patterns</read_first>
  <action>
    In `ance/uci/loop.py`, add `resolve_evaluator()` mirroring 05-PATTERNS.md: allowlist `handcrafted|nnue`, default handcrafted (D-02), invalid → stderr + `SystemExit(1)` (D-03). On `nnue`, construct `NnueEval()` inside try/except for `FileNotFoundError`/`ValueError` → stderr message + exit 1 (D-06). Replace module-level `HandcraftedEval()` with `evaluator: Evaluator = resolve_evaluator()`.

    Complete `tests/test_nnue_eval.py`:
    - Expand torch parity to **40 FENs**: 32 from Phase 4 val shard (deterministic seed=42 subsample) + 8 manual tactical FENs (D-13)
    - Color-mirror: **6 FENs** comparing eval vs `board.mirror()` with STM flipped (D-15)
    - SF sign: **8 positions** (4 won / 4 lost), skip if stockfish absent (D-16)
    - Subprocess: `ANCE_EVAL=nnue` accepts isready; missing weights path exits non-zero
    - Structural: extend existing negamax grep test; run `tests/training/test_no_torch_leakage.py` unchanged

    Run full fast lane excluding slow/torch if torch unavailable in CI; torch tests required locally before wave merge.
  </action>
  <acceptance_criteria>
    - "`.venv/bin/python -m pytest tests/test_nnue_eval.py -m 'not torch' -x -q` exits 0"
    - "`.venv/bin/python -m pytest tests/test_nnue_eval.py -m torch -x -q` exits 0 when torch installed"
    - "`.venv/bin/python -m pytest tests/training/test_no_torch_leakage.py -x -q` exits 0"
    - "Subprocess invalid ANCE_EVAL test exits non-zero"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_nnue_eval.py -m "not torch" -x -q &amp;&amp; .venv/bin/python -m pytest tests/test_nnue_eval.py -m torch -x -q &amp;&amp; .venv/bin/python -m pytest tests/training/test_no_torch_leakage.py -x -q</automated>
  </verify>
  <done>EVAL-03 complete: env-selectable NNUE eval with exact parity and perspective goldens; search seam unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process env → UCI startup | Untrusted `ANCE_EVAL` and `ANCE_NNUE_PATH` from parent shell / gauntlet launcher |
| Weight file on disk | `net.safetensors` or override path — public artifact but must not crash parser |
| Test subprocess | pytest spawns `python -m ance` with crafted env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-01 | Spoofing | `resolve_evaluator()` | high | mitigate | Allowlist `ANCE_EVAL`; invalid value → exit 1 + stderr lists allowed (D-03) |
| T-5-02 | Tampering | `ANCE_NNUE_PATH` | high | mitigate | Open only explicit path after `Path.is_file()`; load via safetensors parser + strict schema (D-08) |
| T-5-03 | Tampering | Missing/bad weights | high | mitigate | Fail fast at startup when `ANCE_EVAL=nnue`; never fall back to handcrafted (D-06) |
| T-5-04 | Denial of Service | Corrupt safetensors | medium | mitigate | `load_net` shape/arch validation raises; propagate to exit 1 |
| T-5-05 | Information Disclosure | stderr on failure | low | accept | Error messages name env var and path — acceptable for local engine |
| T-5-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this plan; pre-pinned stack per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_nnue_eval.py -m "not torch" -x -q`
- `.venv/bin/python -m pytest tests/test_nnue_eval.py -m torch -x -q` (local gate)
- `.venv/bin/python -m pytest tests/training/test_no_torch_leakage.py -x -q`
- Manual: `ANCE_EVAL=nnue .venv/bin/python -m ance` then `isready` / `quit` — no hang, clean exit
</verification>

<success_criteria>
- EVAL-03 satisfied: `NnueEval` implements Protocol, loads exported weights, passes D-13..D-16 tests
- D-01..D-08 implemented in engine startup and package layout
- Fast pytest lane green; torch parity green on dev machine
</success_criteria>

<output>
Create `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-01-SUMMARY.md` when done
</output>
