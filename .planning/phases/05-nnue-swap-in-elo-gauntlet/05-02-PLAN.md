---
phase: 05-nnue-swap-in-elo-gauntlet
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - ance/tools/gauntlet.py
  - tests/test_nnue_gauntlet_depth.py
  - tests/test_nnue_eval.py
autonomous: true
requirements:
  - TOOL-04
must_haves:
  truths:
    - "Gauntlet supports fixed-depth mode: both engines receive `chess.engine.Limit(depth=N)` instead of clock limits when `--depth N` is set — D-11"
    - "`EngineSpec` carries optional `env: dict[str,str]` merged at `popen_uci`; two specs with identical argv but `ANCE_EVAL=handcrafted` vs `ANCE_EVAL=nnue` launch distinct eval builds — D-01, D-04"
    - "Checkpoint `parameters` records mode, search_depth, argv, and env for both engines; tests prove only `ANCE_EVAL` differs between NNUE vs HC specs — D-04"
    - "Aggregate report includes logistic Elo point estimate and 95% CI bounds derived from Wilson score-rate interval — D-12 reporting prep"
    - "Search modules and search constants remain identical regardless of eval env — structural proof in tests"
  artifacts:
    - path: ance/tools/gauntlet.py
      provides: "fixed-depth gauntlet + per-engine env + Elo fields"
      contains: "search_depth"
    - path: tests/test_nnue_gauntlet_depth.py
      provides: "mocked depth/env/checkpoint contract tests"
  key_links:
    - from: tests/test_nnue_gauntlet_depth.py
      to: ance/tools/gauntlet.py
      via: "run_gauntlet(..., search_depth=3) with EngineSpec.env"
      pattern: "search_depth"
    - from: ance/tools/gauntlet.py
      to: python -m ance
      via: "popen_uci(argv, env={**os.environ, **spec.env})"
      pattern: "popen_uci"
---

## Phase Goal

**As a** chess engine developer validating the ANCE milestone, **I want to** drop the trained NNUE behind the untouched evaluator seam and measure it against the handcrafted baseline in a rigorous gauntlet, **so that** we prove measurable Elo gain from supervised training.

## Artifacts this phase produces

*(Full phase-wide inventory — identical across all Phase 5 plans.)*

- `ance/eval/nnue/__init__.py`, `features.py`, `inference.py`, `eval.py` — zero-torch NNUE inference — **Plan 05-01**
- `ance/eval/nnue/net.safetensors` — git-tracked default weights copied from Phase 4 run-output — **Plan 05-01**
- `ance/uci/loop.py` — `resolve_evaluator()` + `ANCE_EVAL` / `ANCE_NNUE_PATH` fail-fast — **Plan 05-01**
- `ance/tools/gauntlet.py` — `EngineSpec.env`, `--depth`, logistic Elo fields — **Plan 05-02**
- `tests/test_nnue_eval.py` — parity, goldens, env wiring, structural seam — **Plan 05-01**; search-config diff test — **Plan 05-02**
- `tests/nnue_parity_helpers.py` — shared torch/numpy oracle (tests only) — **Plan 05-01**
- `tests/test_nnue_gauntlet_depth.py` — depth mode + env injection contracts — **Plan 05-02**
- `tests/test_phase5_elo_evidence.py` — slow ≥1000-game D-12 gate — **Plan 05-03**
- `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-GAUNTLET-EVIDENCE.json` — committed TOOL-04 evidence — **Plan 05-03**

<objective>
Extend the Phase 3 gauntlet harness so TOOL-04 can honestly compare NNUE vs handcrafted: fixed search depth on both sides, per-engine env injection for `ANCE_EVAL`, logistic Elo + Wilson CI in the report, and automated proof that search configuration is identical except eval selection.

Purpose: Without depth mode and env injection, the acceptance gauntlet would measure clock noise or run both sides on handcrafted (Pitfalls 4–5 in RESEARCH).
Output: Extended `gauntlet.py` + fast contract tests; ready for Plan 05-03 overnight run at depth 3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-CONTEXT.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-RESEARCH.md
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-PATTERNS.md
@ance/tools/gauntlet.py
@tests/test_gauntlet_harness.py
@tests/test_eval_seam.py
@.planning/phases/05-nnue-swap-in-elo-gauntlet/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing gauntlet depth + env contract tests (RED)</name>
  <files>tests/test_nnue_gauntlet_depth.py</files>
  <read_first>tests/test_gauntlet_harness.py, 05-PATTERNS.md gauntlet section, 05-RESEARCH.md Pattern 3, ance/tools/gauntlet.py</read_first>
  <behavior>
    - Test 1: `play_gauntlet_game(..., search_depth=3)` passes `Limit(depth=3)` to mocked engine.play; clock fields on limit are None
    - Test 2: `run_gauntlet` with two EngineSpecs differing only in `env["ANCE_EVAL"]` captures distinct merged envs at popen_uci
    - Test 3: checkpoint `parameters` includes mode=fixed_depth, search_depth=3, engine_a/env and engine_b/env with only ANCE_EVAL differing
    - Test 4: `_aggregate` on synthetic 60% score from 10 games includes keys elo, elo_ci_low, elo_ci_high with elo_ci_low &lt; elo &lt; elo_ci_high
  </behavior>
  <action>
    Create `tests/test_nnue_gauntlet_depth.py` following 05-PATTERNS.md mocked-engine style (`_ScriptedEngine` recording limits). Use `ENGINE_ARGV = [sys.executable, "-m", "ance"]`. Tests fail against current clock-only gauntlet — RED only.

    Do not modify `gauntlet.py` in this task.
  </action>
  <acceptance_criteria>
    - "Collect succeeds; at least one test fails on unmodified gauntlet.py"
    - "No subprocess / real engine in fast tests"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_nnue_gauntlet_depth.py --collect-only -q</automated>
  </verify>
  <done>Harness extension contract captured in failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gauntlet fixed-depth mode + EngineSpec.env + Elo reporting (GREEN)</name>
  <files>ance/tools/gauntlet.py</files>
  <read_first>ance/tools/gauntlet.py, 05-RESEARCH.md Pattern 3 and Elo formula, 05-CONTEXT.md D-04, D-09, D-11</read_first>
  <behavior>
    - All Task 1 behaviors pass
    - Test 5: CLI `--depth 3` sets search_depth in run_gauntlet parameters; clock tc ignored when depth set
    - Test 6: backward compat — omitting search_depth still uses clock limits (Phase 3 behavior unchanged)
  </behavior>
  <action>
    Extend `EngineSpec` with `env: dict[str, str] = field(default_factory=dict)` (frozen dataclass).

    Add optional `search_depth: int | None` through `play_gauntlet_game` and `run_gauntlet`. When set, use `chess.engine.Limit(depth=search_depth)` and **do not** decrement arbiter clocks (no time_forfeit path in depth mode). When None, preserve existing clock referee behavior exactly.

    At engine launch: `base_env = os.environ.copy()`; `popen_uci(spec.argv, env={**base_env, **spec.env})` for both specs (D-04).

    Extend `_parameters` to record `mode` (`fixed_depth` vs `clock`), `search_depth`, and per-engine `{name, argv, env}` dicts.

    Add `score_rate_to_elo(p)` and extend `_aggregate` with `elo`, `elo_ci_low`, `elo_ci_high` by applying the logistic transform to score_rate and Wilson (low, high) per 05-RESEARCH.md.

    CLI: `--depth N` (int, default None). Document in module docstring that TOOL-04 acceptance uses fixed depth (D-11).

    Preserve checkpoint/resume, opening parity, Wilson CI, cutechess passthrough, and D-09 runner detection unchanged.

    Add behaviors 5–6 to test file; GREEN Task 1 tests.
  </action>
  <acceptance_criteria>
    - "`.venv/bin/python -m pytest tests/test_nnue_gauntlet_depth.py -x -q` exits 0"
    - "`.venv/bin/python -m pytest tests/test_gauntlet_harness.py -x -q` exits 0 (no Phase 3 regression)"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_nnue_gauntlet_depth.py tests/test_gauntlet_harness.py -x -q</automated>
  </verify>
  <done>Harness supports honest NNUE vs HC comparison at fixed depth with env injection and Elo fields.</done>
</task>

<task type="auto">
  <name>Task 3: Search-config diff verification (D-04)</name>
  <files>tests/test_nnue_eval.py</files>
  <read_first>tests/test_eval_seam.py, 05-RESEARCH.md Pattern 4, ance/search/negamax.py, ance/search/types.py</read_first>
  <action>
    Add `test_search_config_unchanged_by_eval_env` to `tests/test_nnue_eval.py`:
    - Assert `ance/search/negamax.py`, `transposition.py`, `ordering.py` (if present) contain no HandcraftedEval/NnueEval imports in non-comment lines
    - Optional: snapshot hash or mtime guard on `ance/search/types.py` constants — document baseline in test docstring if used
    - Assert gauntlet helper: building two EngineSpecs with argv `[sys.executable, "-m", "ance"]` and env `ANCE_EVAL=handcrafted` vs `nnue` — parameters diff shows only ANCE_EVAL key change (call internal parameter builder or run 2-game mocked gauntlet from test_nnue_gauntlet_depth)

    Add a **10-game smoke** `@pytest.mark.slow` optional test stub (skip by default) documenting manual pre-flight: `run_gauntlet` at depth 2, n_games=10 — not required for plan completion; Plan 05-03 owns the 1000-game gate.

    Per RESEARCH discretion: recommend **depth N=3** for acceptance; record in SUMMARY after a quick 2-game depth-3 smoke if run locally.
  </action>
  <acceptance_criteria>
    - "Structural search-config test passes"
    - "`.venv/bin/python -m pytest tests/test_nnue_eval.py::test_search_config_unchanged_by_eval_env -x -q` exits 0"
    - "Full fast lane: `.venv/bin/python -m pytest -m 'not slow' -q` exits 0"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_nnue_eval.py::test_search_config_unchanged_by_eval_env tests/test_nnue_gauntlet_depth.py -x -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>D-04 automated: eval swap cannot alter search code path; harness ready for TOOL-04 evidence run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gauntlet launcher → child UCI processes | Merged OS env + per-spec overrides cross into engine subprocess |
| Checkpoint JSON on disk | Resume state must not silently change parameters mid-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-5-06 | Spoofing | EngineSpec.env | high | mitigate | Only merge explicit dict keys; ANCE_EVAL validated inside child via D-03 |
| T-5-07 | Tampering | Checkpoint resume | medium | mitigate | Existing parameter validation on resume; include env+depth in parameters hash |
| T-5-08 | Repudiation | Wrong runner recorded | low | mitigate | `detect_runner()` + evidence JSON records arbiter vs cutechess (D-09) |
| T-5-09 | Denial of Service | Depth mode + 1000 games | medium | accept | Bounded by max_halfmoves + checkpoint resume; overnight wall-clock expected |
| T-5-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages; existing chess/numpy stack only |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_nnue_gauntlet_depth.py -x -q`
- `.venv/bin/python -m pytest tests/test_gauntlet_harness.py -x -q`
- `.venv/bin/python -m pytest -m "not slow" -q`
- Optional smoke: `.venv/bin/python -m ance.tools.gauntlet --games 2 --depth 3 --engine-a "python -m ance" --engine-b "python -m ance"` with env wrappers documented in SUMMARY
</verification>

<success_criteria>
- TOOL-04 harness prerequisites satisfied: depth mode, env injection, Elo+CI fields, D-04 diff proof
- Phase 3 gauntlet behavior preserved when `--depth` omitted
- Wave 2 merge gate: full non-slow pytest green
</success_criteria>

<output>
Create `.planning/phases/05-nnue-swap-in-elo-gauntlet/05-02-SUMMARY.md` when done
</output>
