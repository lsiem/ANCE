---
phase: 03-search-acceleration-time-management
plan: 06
type: execute
wave: 5
depends_on:
  - 03-02
  - 03-05
files_modified:
  - tests/test_phase3_gauntlet_evidence.py
  - .planning/phases/03-search-acceleration-time-management/03-GAUNTLET-EVIDENCE.json
autonomous: true
requirements:
  - SRCH-08
  - UCI-08
  - TOOL-03
must_haves:
  truths:
    - "One 100-game arbiter-refereed blitz gauntlet at a documented TC (default 30+0.3 per RESEARCH) with identical `[sys.executable, -m, ance]` commands on both sides completes with zero time forfeits AND a Wilson 95% CI on score percentage containing 0.50 — D-14 + D-17 + ROADMAP criteria 3 and 5 in a single run"
    - "The run uses Plan 03-02's harness (opening parity, checkpoint/resume, forfeit counters, Wilson CI) with no new harness code; evidence is committed as 03-GAUNTLET-EVIDENCE.json with the exact command line (D-19)"
    - "Evidence runs are slow-marked only; the fast pytest lane stays green and never schedules the 100-game suites (RESEARCH Pitfall: hours-long CI blowout)"
  artifacts:
    - path: tests/test_phase3_gauntlet_evidence.py
      provides: "slow-marked D-14/D-17 gauntlet gates with checkpoint resume and assertion helpers"
      contains: "test_d14"
    - path: .planning/phases/03-search-acceleration-time-management/03-GAUNTLET-EVIDENCE.json
      provides: "committed machine-readable evidence: forfeit totals, W-L-D, Wilson bounds, command lines, git commit"
  key_links:
    - from: tests/test_phase3_gauntlet_evidence.py
      to: ance/tools/gauntlet.py
      via: "run_gauntlet with real [sys.executable, -m, ance] engines, checkpoint output path, --tc 30+0.3"
      pattern: "run_gauntlet"
    - from: tests/test_phase3_gauntlet_evidence.py
      to: ance/uci/clock.py
      via: "clocked go commands exercised end-to-end through arbiter Limit translation — D-14 only meaningful after Plan 03-05"
      pattern: "compute_clock_budget"
---

## Phase Goal

**As a** chess player running ANCE under a real clock in a GUI, **I want to** play blitz games against an engine that searches efficiently and manages its own time, **so that** games are decided by chess strength rather than shuffling, shallow search, or time forfeits.

<objective>
Run the single phase-end 100-game statistical gauntlet deferred from Plans 03-02 and 03-05: identical-eval self-play that simultaneously proves D-14 (zero forfeits) and D-17 (Wilson CI contains 50%). No new harness features — only a slow-marked test, a committed evidence artifact, and SUMMARY command line.

Purpose: ROADMAP success criteria 3 and 5 are hard acceptance gates; Plans 03-02 (TOOL-03) and 03-05 (clock wiring) make them runnable but intentionally left the multi-hour run to this plan. One identical-eval blitz gauntlet satisfies both gates.
Output: `tests/test_phase3_gauntlet_evidence.py`, committed `03-GAUNTLET-EVIDENCE.json`, and recorded exact arbiter invocation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-acceleration-time-management/03-CONTEXT.md
@.planning/phases/03-search-acceleration-time-management/03-RESEARCH.md
@.planning/phases/03-search-acceleration-time-management/03-02-SUMMARY.md
@.planning/phases/03-search-acceleration-time-management/03-05-SUMMARY.md
@ance/tools/gauntlet.py
@ance/tools/openings.epd
@ance/uci/clock.py

<interfaces>
<!-- Contracts from Plans 03-02 and 03-05 the executor consumes. -->

```python
# ance/tools/gauntlet.py (Plan 03-02):
def run_gauntlet(spec_a, spec_b, openings, n_games, tc_base_s, tc_inc_s,
                 max_halfmoves, output_path, stop_event=None, deadline=None,
                 on_game_complete=None, restart=False) -> dict: ...
# aggregate keys include: wins, losses, draws, score_rate, draw_rate,
# wilson_low, wilson_high, time_forfeits (per engine name), n_games, status

# CLI:
# python -m ance.tools.gauntlet --games 100 --tc 30+0.3 --output <path>
#   --engine-a "python -m ance" --engine-b "python -m ance"  # D-17 identical eval
#   --max-halfmoves 160 --budget-seconds <optional watchdog>

# D-17 assertion (RESEARCH): wilson_low <= 0.50 <= wilson_high — NOT score_rate == 0.50
# D-14 assertion: sum(time_forfeits.values()) == 0 and status == "completed"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evidence test module with fast assertion helpers</name>
  <files>tests/test_phase3_gauntlet_evidence.py</files>
  <read_first>ance/tools/gauntlet.py, tests/test_gauntlet_harness.py, .planning/phases/03-search-acceleration-time-management/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1 (D-17 helper): given a synthetic report dict with score_rate 0.52, n_games 100, wilson_low 0.43, wilson_high 0.61, `assert_sanity_ci_contains_half(report)` passes; wilson_high 0.49 fails with a message naming D-17
    - Test 2 (D-14 helper): given aggregate time_forfeits {"ance": 0, "ance_b": 1}, `assert_zero_forfeits(report)` fails naming the offending engine; all-zero passes
    - Test 3 (schema): `load_evidence(path)` parses 03-GAUNTLET-EVIDENCE.json shape {schema_version, git_commit, gauntlet, gates_passed} and raises FileNotFoundError directing to run this plan when missing
    - Test 4 (not slow): module's D-14/D-17 gauntlet tests are decorated `@pytest.mark.slow` — collecting with `-m "not slow"` excludes them (spy on collected items or assert marker present)
  </behavior>
  <action>
    Create `tests/test_phase3_gauntlet_evidence.py` (house style, docstring citing D-14/D-17/D-19, `from __future__ import annotations`). Module-level constants:
    - `D14_GAMES = 100`, `D14_TC = "30+0.3"` (RESEARCH default blitz TC — exercises clock budgeting without multi-hour per-move costs of 60+1; record override in SUMMARY if changed)
    - `D17_GAMES = 100`, `D17_TC = "30+0.3"` (same TC; identical engine argv both sides)
    - `ENGINE_ARGV = [sys.executable, "-m", "ance"]`
    - `MAX_HALFMOVES = 160`

    Pure helpers (no subprocess):
    - `assert_zero_forfeits(report: dict) -> None`
    - `assert_sanity_ci_contains_half(report: dict) -> None` — uses wilson_low/wilson_high from aggregate
    - `load_evidence(path) -> dict`

    Fast tests 1–4 only in this commit's RED/GREEN cycle.

    RED commit `test(03-06): specify gauntlet evidence assertion helpers (RED)`, GREEN commit `feat(03-06): add D-14/D-17 evidence assertion helpers (GREEN)`.
  </action>
  <acceptance_criteria>
    - "assert_sanity_ci_contains_half and assert_zero_forfeits behave per behaviors 1-2"
    - "load_evidence raises on missing file with actionable message"
    - "D-14/D-17 test functions carry @pytest.mark.slow"
    - "`.venv/bin/python -m pytest tests/test_phase3_gauntlet_evidence.py -m 'not slow' -q` exits 0"
    - "`.venv/bin/python -m pytest -m 'not slow' -q` exits 0 (D-23)"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_phase3_gauntlet_evidence.py -m "not slow" -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>Assertion helpers unit-proven; slow gauntlet tests stubbed with markers but not yet run.</done>
</task>

<task type="auto">
  <name>Task 2: Combined D-14 + D-17 — 100-game identical-eval blitz gauntlet</name>
  <files>tests/test_phase3_gauntlet_evidence.py, .planning/phases/03-search-acceleration-time-management/03-GAUNTLET-EVIDENCE.json</files>
  <read_first>ance/tools/gauntlet.py, tests/test_time_management.py, tests/test_gauntlet_harness.py, .planning/phases/03-search-acceleration-time-management/03-05-SUMMARY.md, .planning/phases/03-search-acceleration-time-management/03-RESEARCH.md</read_first>
  <action>
    Add `@pytest.mark.slow` test `test_phase3_hundred_game_blitz_evidence_d14_d17`:

    1. Precondition: Plan 03-05 complete — `grep -q compute_clock_budget ance/uci/loop.py` and Plan 03-05 slow flag-safety test has passed at least once on this machine.
    2. Run ONE gauntlet via harness API or CLI with checkpoint resume:
       - `spec_a = spec_b = EngineSpec("ance", ENGINE_ARGV)` — identical eval/commands (D-17/D-18)
       - `n_games = 100`, `tc = 30+0.3`, `openings = load_openings(default_epd)`, `max_halfmoves = 160`
       - Output to pytest `tmp_path` during the test; copy final report into the committed evidence artifact
       - Use `--budget-seconds` generously (e.g. 18000 = 5h) or rely on resume — RESEARCH estimates 2.5–4h wall time at this TC
    3. On completion assert ALL of:
       - `assert_zero_forfeits(report)` — D-14
       - `assert_sanity_ci_contains_half(report)` — D-17 (Wilson interval contains 0.50, not point equality)
       - `report["status"] == "completed"` and `report["aggregate"]["n_games"] == 100`
    4. Commit `.planning/phases/03-search-acceleration-time-management/03-GAUNTLET-EVIDENCE.json`:
       - `schema_version`, `git_commit`, `captured_utc`
       - `gauntlet`: {games, tc, wins, losses, draws, score_rate, draw_rate, wilson_low, wilson_high, time_forfeits, command_line, elapsed_s, status}
       - `gates_passed`: ["D-14", "D-17"]

    If interrupted, resume the same output path without `--restart` until 100 games complete — checkpoint contract from Plan 03-02. Do NOT commit a partial evidence JSON.

    Commit test as `test(03-06): add combined D-14/D-17 100-game evidence gate (slow)`; evidence JSON as `docs(03-06): capture D-14/D-17 gauntlet evidence`.
  </action>
  <acceptance_criteria>
    - "100-game identical-eval gauntlet at 30+0.3: total time_forfeits == 0 (D-14)"
    - "Same run: Wilson 95% CI on score rate contains 0.50 (D-17)"
    - "03-GAUNTLET-EVIDENCE.json committed with full aggregate and exact command line"
    - "Test is @pytest.mark.slow; `.venv/bin/python -m pytest -m 'not slow' -q` exits 0 (D-23)"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_phase3_gauntlet_evidence.py::test_phase3_hundred_game_blitz_evidence_d14_d17 -m slow -q &amp;&amp; .venv/bin/python -c "
import json
from tests.test_phase3_gauntlet_evidence import assert_zero_forfeits, assert_sanity_ci_contains_half, load_evidence
d = load_evidence('.planning/phases/03-search-acceleration-time-management/03-GAUNTLET-EVIDENCE.json')
assert_zero_forfeits({'aggregate': d['gauntlet']})
assert_sanity_ci_contains_half({'aggregate': d['gauntlet']})
assert 'D-14' in d['gates_passed'] and 'D-17' in d['gates_passed']
print('evidence ok')
" &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>D-14 and D-17 satisfied in one run; ROADMAP criteria 3 and 5 closed with committed reproducible evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence artifact → phase sign-off | false pass if forfeits miscounted or CI math wrong |
| multi-hour run → interruption | partial JSON must not be committed as evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06a | Repudiation | D-14/D-17 evidence validity | high | mitigate | Commit JSON with git_commit, exact command lines, raw W-L-D and forfeit counters; assertion helpers re-validated on load |
| T-03-06b | Denial of Service | CI suite runtime blowout | high | mitigate | Both gauntlet tests `@pytest.mark.slow` only; fast lane explicitly verified after every task (D-23) |
| T-03-06c | Tampering | partial/torn evidence on kill | medium | mitigate | Harness per-game atomic checkpoint (Plan 03-02); evidence JSON committed only after status==completed for both runs |
| T-03-06d | Spoofing | running D-14 before clock wiring | high | mitigate | depends_on includes 03-05; Task 2 precondition grep on compute_clock_budget in loop.py |
</threat_model>

<verification>
- Fast: `.venv/bin/python -m pytest tests/test_phase3_gauntlet_evidence.py -m "not slow" -q` and full `-m "not slow"` suite green after Task 1.
- Slow (phase-end, dev machine): combined 100-game test green; evidence JSON validates.
</verification>

<success_criteria>
- D-14: 100-game blitz gauntlet, zero time forfeits — ROADMAP criterion 3.
- D-17: same run, Wilson CI contains 50% — ROADMAP criterion 5.
- TOOL-03 harness proven at scale with no further code changes.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `tests/test_phase3_gauntlet_evidence.py` — evidence gates and assertion helpers.
  - `assert_zero_forfeits(report)`, `assert_sanity_ci_contains_half(report)`, `load_evidence(path)`.
  - `test_phase3_hundred_game_blitz_evidence_d14_d17` (`@pytest.mark.slow`).
- `.planning/phases/03-search-acceleration-time-management/03-GAUNTLET-EVIDENCE.json` — committed combined D-14/D-17 results.

<output>
Create `.planning/phases/03-search-acceleration-time-management/03-06-SUMMARY.md` when done. Record exact command line, wall-clock elapsed, forfeits, W-L-D, and Wilson bounds for the combined run.
</output>
