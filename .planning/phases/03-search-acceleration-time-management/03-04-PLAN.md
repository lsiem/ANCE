---
phase: 03-search-acceleration-time-management
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
  - 03-01
files_modified:
  - ance/search/ordering.py
  - ance/search/negamax.py
  - ance/search/types.py
  - ance/uci/loop.py
  - tests/test_move_ordering.py
  - tests/test_phase3_strength_baseline.py
autonomous: true
requirements:
  - SRCH-06
must_haves:
  truths:
    - "Interior-node moves are searched in the order TT hash move → MVV-LVA captures/promotions → killer slot 0 → killer slot 1 → history-scored quiets (D-07); qsearch keeps MVV-LVA only"
    - "Two killer slots per ply shift-update on quiet beta cutoffs and are cleared on ucinewgame (D-08, D-10)"
    - "The butterfly history table ages by halving when any cell would exceed its cap, so quiet scores never outrank killers and the table cannot grow without bound across a game (D-09)"
    - "Deterministic benchmark: TT+ordering searches each FEN to the depth recorded in 03-BASELINE.json, visits strictly fewer nodes per FEN, and totals at most 70% of the baseline nodes (D-21, SRCH-06 success criterion)"
    - "At an equal 2s movetime budget the engine completes depth greater than or equal to the baseline on every baseline FEN (slow-marked wall-clock evidence, D-21)"
  artifacts:
    - path: ance/search/ordering.py
      provides: "score bands, score_move/order_moves, killer + history helpers, relocated MVV-LVA machinery"
      contains: "def order_moves"
    - path: ance/search/negamax.py
      provides: "ordered interior move loop, quiet-cutoff killer/history updates, hash move from TT probe"
      contains: "order_moves"
    - path: ance/uci/loop.py
      provides: "per-game killer/history state cleared on ucinewgame alongside the TT"
      contains: "killer_moves"
    - path: tests/test_move_ordering.py
      provides: "band ordering, killer shift, history aging, qsearch-isolation tests"
    - path: tests/test_phase3_strength_baseline.py
      provides: "D-21 comparison assertions against 03-BASELINE.json"
  key_links:
    - from: ance/search/negamax.py
      to: ance/search/ordering.py
      via: "order_moves(moves, board, hash_move, killers_at_ply, history) in the interior move loop; _mvv_lva_sort imported back for qsearch"
      pattern: "from ance.search.ordering import"
    - from: tests/test_phase3_strength_baseline.py
      to: .planning/phases/03-search-acceleration-time-management/03-BASELINE.json
      via: "load_baseline() comparison at each record's captured deterministic depth (fresh TT/killers/history per search)"
      pattern: "load_baseline"
    - from: ance/uci/loop.py
      to: ance/search/ordering.py
      via: "new_killers()/new_history() module state passed into search_root; reset in handle_ucinewgame"
      pattern: "new_killers"
---

## Phase Goal

**As a** chess player running ANCE under a real clock in a GUI, **I want to** play blitz games against an engine that searches efficiently and manages its own time, **so that** games are decided by chess strength rather than shuffling, shallow search, or time forfeits.

<objective>
Implement SRCH-06: full main-search move ordering (hash move → MVV-LVA → killers → history) threaded through SearchContext, with per-game killer/history state cleared on ucinewgame — then prove D-21 against the committed 03-BASELINE.json snapshot with a deterministic pytest benchmark.

Purpose: ordering is the multiplier on the TT from Plan 03-03; this plan closes the "measurably better than Phase 2 at equal time" success criterion.
Output: `ance/search/ordering.py`, ordered negamax, loop wiring, and the D-21 benchmark evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-acceleration-time-management/03-CONTEXT.md
@.planning/phases/03-search-acceleration-time-management/03-RESEARCH.md
@.planning/phases/03-search-acceleration-time-management/03-PATTERNS.md
@.planning/phases/03-search-acceleration-time-management/03-01-SUMMARY.md
@.planning/phases/03-search-acceleration-time-management/03-03-SUMMARY.md
@ance/search/negamax.py
@ance/search/types.py
@ance/uci/loop.py

<interfaces>
<!-- Contracts established by Plans 03-01/03-03 that this plan consumes. -->

```python
# From Plan 03-03 (ance/search/transposition.py):
class TranspositionTable:
    def probe(self, key: int) -> tuple[int, int, int, chess.Move | None] | None: ...
    # returns (depth, score, flag, best_move); best_move is the hash-move source (D-10)

# From Plan 03-03 (ance/search/negamax.py): negamax already extracts tt_move on probe
# even when tt_depth < depth (kept for this plan). SearchContext already carries `tt`.
# search_root(..., tt=None) keyword-only; _search_at_depth(..., tt) forwards it.

# From Plan 03-01 (tests/test_phase3_strength_baseline.py):
def load_baseline(path=DEFAULT_BASELINE_PATH) -> dict: ...
# baseline['positions'][fen_id]['fixed_depth'] — captured depth and deterministic node count
# baseline['positions'][fen_id]['timed']['completed_depth'] — 2s completed depth

# MVV-LVA machinery currently PRIVATE in negamax.py (relocate, do not duplicate):
_MVV_LVA: dict[int, int]                       # piece-type -> value
def _capture_value(board, move) -> int: ...    # en-passant + promotion aware
def _mvv_lva_sort(moves, board) -> list: ...   # qsearch keeps using exactly this
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ordering module with score bands, killers, and aging history</name>
  <files>ance/search/ordering.py, ance/search/negamax.py, tests/test_move_ordering.py</files>
  <read_first>ance/search/negamax.py, ance/search/transposition.py, .planning/phases/03-search-acceleration-time-management/03-PATTERNS.md, .planning/phases/03-search-acceleration-time-management/03-RESEARCH.md</read_first>
  <behavior>
    - Test 1 (D-07 bands): on a constructed position offering a hash move, several captures, a killer, and quiets, order_moves returns hash move first, then captures in MVV-LVA order (victim descending, attacker ascending within victim), then killer slot 0, then killer slot 1, then quiets by descending history score
    - Test 2 (D-08/D-10 killers): update_killers shifts slot 0 to slot 1 and installs the new move at slot 0; re-storing the current slot-0 move is a no-op; killers from another ply never influence this ply's ordering
    - Test 3 (D-09 history): update_history adds depth*depth to history[side][from][to]; when any cell would exceed HISTORY_CAP the whole table halves; after halving every quiet score is still strictly below KILLER_1_SCORE
    - Test 4 (qsearch isolation, D-07): _qsearch_moves output ordering on a capture-rich FEN is unchanged from Phase 2 — qsearch uses only MVV-LVA (imported from ordering), never killers/history/hash move
    - Test 5 (promotions): a queen promotion scores in the capture band even when it captures nothing
  </behavior>
  <action>
    Create `ance/search/ordering.py` (docstring citing D-07..D-10, `from __future__ import annotations`). Relocate `_MVV_LVA`, `_capture_value`, and `_mvv_lva_sort` from negamax.py into this module verbatim (public-ish module members, same names) and update negamax.py to import them from `ance.search.ordering` so qsearch behavior is byte-identical — relocation, not duplication; ordering.py imports only `chess` and `ance.search.types` (no negamax import, no circularity).

    Add to ordering.py:
    - Score-band constants: `HASH_MOVE_SCORE = 1_000_000`, `CAPTURE_BASE = 100_000`, `KILLER_0_SCORE = 90_000`, `KILLER_1_SCORE = 80_000`, `HISTORY_CAP = 79_000` (bands per RESEARCH Pattern 2; the cap keeps every aged quiet score strictly below KILLER_1_SCORE, D-09).
    - `score_move(move, board, hash_move, killers_at_ply, history) -> int`: hash move → HASH_MOVE_SCORE; capture or any promotion → CAPTURE_BASE + 100 * _capture_value(board, move) - attacker piece value (attacker from board.piece_at(move.from_square), 0 when None); killer slot 0/1 equality → their band scores; otherwise `history[side][move.from_square][move.to_square]` with side = int(board.turn) (the side to move at this node), 0 when history is None. killers_at_ply may be a (None, None)-like pair.
    - `order_moves(moves, board, hash_move, killers_at_ply, history) -> list[chess.Move]`: single `sorted(..., key=score_move..., reverse=True)` — one sort, no list surgery.
    - `new_killers() -> list[list[chess.Move | None]]`: `[[None, None] for _ in range(MAX_PLY + 1)]` (MAX_PLY from ance.search.types).
    - `new_history() -> list[list[list[int]]]`: zeros shaped [2][64][64] (butterfly table, D-09).
    - `update_killers(slots, move) -> None`: if move differs from slots[0], shift 0→1 and install at 0 (D-08).
    - `update_history(history, side, move, depth) -> None`: add depth*depth to the cell; if the cell now exceeds HISTORY_CAP, halve every cell in the whole table with integer division (aging, D-09).

    Create `tests/test_move_ordering.py` (house style, docstring citing D-07..D-10) covering behaviors 1–5.

    RED commit `test(03-04): specify move-ordering bands and tables (RED)`, GREEN commit `feat(03-04): add ordering module with killers and aging history (GREEN)`.
  </action>
  <acceptance_criteria>
    - "ance/search/ordering.py exports order_moves, score_move, new_killers, new_history, update_killers, update_history, _mvv_lva_sort, _capture_value, _MVV_LVA and the five band constants"
    - "negamax.py imports MVV-LVA machinery from ance.search.ordering; the private definitions are removed from negamax.py"
    - "Band ordering, killer shift semantics, history aging cap, and qsearch isolation all pass (behaviors 1-5)"
    - "`.venv/bin/python -m pytest tests/test_move_ordering.py -q` exits 0 and `.venv/bin/python -m pytest -m 'not slow' -q` exits 0 (qsearch relocation regression-free, D-23)"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_move_ordering.py -q &amp;&amp; .venv/bin/python -c "from ance.search.ordering import order_moves, new_killers, new_history, HASH_MOVE_SCORE, HISTORY_CAP, KILLER_1_SCORE; assert HISTORY_CAP &lt; KILLER_1_SCORE" &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>Ordering primitives unit-proven; MVV-LVA relocated with zero qsearch behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ordered negamax with killer/history updates and per-game state</name>
  <files>ance/search/negamax.py, ance/search/types.py, ance/uci/loop.py, tests/test_move_ordering.py</files>
  <read_first>ance/search/negamax.py, ance/search/types.py, ance/uci/loop.py, ance/search/ordering.py, tests/test_transposition_table.py</read_first>
  <behavior>
    - Test 6 (hash-move first): with a pre-seeded TT entry whose best_move is a known quiet move, a spy on the child-search order confirms that move is searched first at the matching node
    - Test 7 (killer/history updates): after a search where a quiet move causes a beta cutoff, that move occupies killer slot 0 at that ply and its history cell increased by depth*depth; a capture causing a cutoff updates neither
    - Test 8 (shared tables, Pitfall 5): killers/history objects observed inside deep child contexts are the identical objects passed into search_root (identity assertion via spy), across both _child_ctx clones and per-root-move contexts
    - Test 9 (node reduction from ordering alone): nodes-to-depth-4 on the italian FEN with tt=None but killers/history/ordering active is strictly lower than the Phase 2 unordered count recorded in 03-BASELINE.json for that FEN
    - Test 10 (ucinewgame): after loop.handle_ucinewgame, loop-level killer slots are all None and every history cell is 0
  </behavior>
  <action>
    Extend `ance/search/types.py`: SearchContext gains `killers: list[list[chess.Move | None]] | None = None` and `history: list[list[list[int]]] | None = None` (same shared-reference pattern as counter/path_keys; annotate with strings or TYPE_CHECKING as needed).

    Modify `ance/search/negamax.py`:
    1. In `negamax()`, keep the tt_move extracted by the Plan 03-03 probe as `hash_move` (None when no TT/hit). Replace the plain `for move in moves:` interior loop with `for move in order_moves(moves, board, hash_move, killers_at_ply, ctx.history):` where `killers_at_ply = ctx.killers[ctx.ply] if ctx.killers is not None else (None, None)` — guard `ctx.ply` <= MAX_PLY (killers list length is MAX_PLY + 1).
    2. On a beta cutoff (after the pop, where board.turn is again the side that made the move): when the cutoff move is quiet (`not board.is_capture(move) and move.promotion is None`) and the tables exist, call `update_killers(ctx.killers[ctx.ply], move)` and `update_history(ctx.history, int(board.turn), move, depth)` (D-10: killer updates on beta cutoffs; exact-policy discretion resolved as: update on every quiet beta cutoff). Then `break` (the Plan 03-03 store still runs).
    3. Forward `killers=ctx.killers, history=ctx.history` in `_child_ctx`. Add `killers`/`history` parameters to `_search_at_depth` and keyword-only `killers=None, history=None` to `search_root`, forwarded into every per-root-move SearchContext (Pitfall 5). When search_root receives None for either, it creates fresh `new_killers()`/`new_history()` once per search_root call so direct callers (tests, tools, baseline collector) always get ordering benefits with per-search lifetime. Qsearch remains untouched (D-07).
    4. `ance/uci/loop.py`: module-level `killer_moves = new_killers()` and `history_table = new_history()` next to `transposition_table`; pass both through `handle_go`'s args tuple and `_run_search` into `search_root` so they persist across go commands within a game (D-08/D-09 lifetime); in `handle_ucinewgame` reassign both to fresh `new_killers()`/`new_history()` immediately after `transposition_table.clear()` (reassignment is safe: the worker was stopped/invalidated first, and a bounded-join survivor holding old references only writes to orphaned tables).

    Extend `tests/test_move_ordering.py` with behaviors 6–10.

    RED commit `test(03-04): specify ordered search integration (RED)`, GREEN commit `feat(03-04): wire hash-move/killer/history ordering into negamax (GREEN)`.
  </action>
  <acceptance_criteria>
    - "SearchContext carries killers/history; _child_ctx and _search_at_depth forward them; search_root creates fresh tables when passed None"
    - "Quiet beta cutoffs update killer slot 0 and the mover-side history cell by depth*depth; capture cutoffs update neither (behavior 7)"
    - "handle_ucinewgame resets killer_moves and history_table alongside transposition_table.clear() (behavior 10)"
    - "Ordering alone (tt=None) beats the recorded unordered baseline node count on the italian FEN at depth 4 (behavior 9)"
    - "`.venv/bin/python -m pytest tests/test_move_ordering.py tests/test_transposition_table.py -q` exits 0 and `.venv/bin/python -m pytest -m 'not slow' -q` exits 0 (D-23)"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_move_ordering.py tests/test_transposition_table.py -q &amp;&amp; grep -q "killer_moves" ance/uci/loop.py &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>D-07 order live at every interior node with per-game killer/history lifetime and ucinewgame reset.</done>
</task>

<task type="auto">
  <name>Task 3: D-21 strength benchmark against the committed baseline</name>
  <files>tests/test_phase3_strength_baseline.py</files>
  <read_first>tests/test_phase3_strength_baseline.py, ance/tools/phase3_baseline.py, .planning/phases/03-search-acceleration-time-management/03-BASELINE.json, ance/search/negamax.py</read_first>
  <action>
    Extend `tests/test_phase3_strength_baseline.py` with the D-21 comparison (docstring section citing D-20/D-21, SRCH-06 success criterion):

    1. Fast deterministic gate `test_phase3_fixed_depth_nodes_beat_baseline` (NOT slow-marked — deterministic node counts, CI-safe): for every FEN id in `load_baseline()['positions']`, read `captured = record['fixed_depth']` and run `search_root(pos, max_depth=captured['depth'], evaluator=HandcraftedEval(), stop_flag=fresh_event, tt=TranspositionTable(), killers=new_killers(), history=new_history())` — fresh tables per search (TT-cold, Pitfall 9). Assert per-FEN `result.nodes` is strictly less than `captured['nodes']`, and that the sum across all six FENs is at most 70% of the baseline sum. This intentionally honors the artifact's documented Kiwipete depth-2 override; all other records remain depth 4.
    2. Slow wall-clock gate `test_phase3_depth_at_2s_meets_baseline` (`@pytest.mark.slow` — machine/load sensitive, dev-machine evidence per Phase 2 precedent): for every baseline FEN run search_root with `deadline = time.monotonic() + 2.0` and fresh tables; assert completed depth >= the baseline `timed.completed_depth` for every FEN, and strictly greater on at least two of the four non-mate FENs (startpos, kiwipete, italian, rook_endgame) — D-21's "greater completed depth at equal budget". Record actual depths in the assertion message for the SUMMARY.
    3. Run the fast suite plus this file; then run the slow gate once and capture its output for the SUMMARY (expected slow-gate runtime: 6 FENs x 2s = ~12s of search plus overhead; budget generously at 2 minutes).

    Commit as `test(03-04): prove D-21 node and depth gains over Phase 2 baseline`.
  </action>
  <acceptance_criteria>
    - "Fast gate: per-FEN nodes at each captured depth strictly below baseline and total &lt;= 0.70 x baseline total, with fresh TT/killers/history per search"
    - "Slow gate: 2s completed depth >= baseline on all six FENs and > baseline on >= 2 of the 4 non-mate FENs"
    - "`.venv/bin/python -m pytest tests/test_phase3_strength_baseline.py -q` exits 0 (fast gate)"
    - "`.venv/bin/python -m pytest tests/test_phase3_strength_baseline.py -m slow -q` exits 0 (wall-clock gate, run once on this machine)"
    - "`.venv/bin/python -m pytest -m 'not slow' -q` exits 0 (D-23)"
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_phase3_strength_baseline.py -q &amp;&amp; .venv/bin/python -m pytest tests/test_phase3_strength_baseline.py -m slow -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>SRCH-06's "measurably better at equal time" criterion is pytest-proven against the pre-change artifact, not hand-waved (D-21).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-game tables ↔ ucinewgame | killers/history must not leak preferences across games |
| benchmark ↔ baseline artifact | D-21 comparison is only honest against the committed pre-change snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04a | Repudiation | D-21 strength claim | high | mitigate | Compare against committed 03-BASELINE.json (with its recorded git commit) using deterministic TT-cold node counts; thresholds asserted in CI-safe pytest, wall-clock gate slow-marked |
| T-03-04b | Tampering | silent table reset (Pitfall 5) | high | mitigate | Identity assertions prove killers/history are shared across _child_ctx clones and root-move contexts; node-reduction test would fail on regression |
| T-03-04c | Denial of Service | unbounded history growth | medium | mitigate | HISTORY_CAP + halve-all aging (tested); table is fixed-shape [2][64][64] |
| T-03-04d | Tampering | ordering corrupts search results | high | mitigate | Ordering changes which move is searched first, never which score is returned; Plan 03-03's oracle-invariance tests and the full fast suite re-run after every task (D-23) |
| T-03-04e | Information Disclosure | cross-game heuristic leakage | medium | mitigate | ucinewgame reassigns fresh killers/history (tested behavior 10) per D-08/D-09 |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_move_ordering.py -q` and full fast suite green after every task (D-23).
- D-21 fast gate green in CI; slow gate green once on this machine with depths recorded in the SUMMARY.
</verification>

<success_criteria>
- SRCH-06 complete: D-07 ordering at every interior node, D-08 killers, D-09 aging history, D-10 hash-move feed — with per-game lifetime and ucinewgame reset.
- ROADMAP criterion 2 proven: measurably fewer nodes to equal depth AND greater/equal completed depth at equal 2s time versus the Phase 2 baseline.
</success_criteria>

## Artifacts this phase produces (this plan's share)

- `ance/search/ordering.py` — new module.
  - `order_moves(moves, board, hash_move, killers_at_ply, history) -> list`.
  - `score_move(...) -> int`; constants `HASH_MOVE_SCORE`, `CAPTURE_BASE`, `KILLER_0_SCORE`, `KILLER_1_SCORE`, `HISTORY_CAP`.
  - `new_killers()`, `new_history()`, `update_killers(slots, move)`, `update_history(history, side, move, depth)`.
  - Relocated `_MVV_LVA`, `_capture_value`, `_mvv_lva_sort` (imported back by negamax/qsearch).
- `ance/search/types.py` — `SearchContext.killers` / `SearchContext.history` fields.
- `ance/search/negamax.py` — ordered interior loop; `search_root(..., killers=None, history=None)` keywords.
- `ance/uci/loop.py` — module globals `killer_moves`, `history_table`; ucinewgame reset.
- `tests/test_move_ordering.py` — ordering test module.
- `tests/test_phase3_strength_baseline.py` — D-21 comparison gates (fast + slow).

<output>
Create `.planning/phases/03-search-acceleration-time-management/03-04-SUMMARY.md` when done. Record per-FEN node counts vs baseline and the 2s completed depths (D-21 evidence).
</output>
