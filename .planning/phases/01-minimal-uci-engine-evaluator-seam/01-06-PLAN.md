---
phase: 01-minimal-uci-engine-evaluator-seam
plan: 06
type: execute
wave: 5
depends_on: ["01-04"]
files_modified: []
autonomous: false
requirements: [TOOL-01]
must_haves:
  truths:
    - "In Cute Chess or Arena, ANCE completes the uci/isready handshake and plays a full legal game to a natural result without hanging or being disqualified"
  artifacts: []
  key_links:
    - "GUI registers <repo>/.venv/bin/python -m ance as a UCI engine and drives it exactly as any other UCI engine, with no ANCE-specific GUI configuration"
---

## Phase Goal

**As a** chess GUI or UCI test harness driving ANCE over stdin/stdout, **I want to** complete the handshake and receive exactly one legal `bestmove` for every `go` — even through a swappable evaluator — **so that** I can play a complete, legal game and confirm ANCE reliably beats a random-mover opponent.

## Artifacts this phase produces

*(Full phase-wide artifact inventory — reproduced identically in every plan in this phase. This plan modifies no files — it is a manual validation gate over the artifacts built by Plans 01-01 through 01-04.)*

- `pyproject.toml`, `ance/__init__.py`, `ance/__main__.py` — Plan 01-01 — the exact entry point this checkpoint launches
- `ance/debug.py`, `ance/board/position.py` — Plan 01-02
- `ance/eval/base.py`, `ance/eval/material.py`, `ance/search/negamax.py`, `ance/uci/loop.py`, `ance/uci/parser.py`, `ance/uci/protocol.py` — Plan 01-03
- `ance/eval/tables.py`, `ance/eval/handcrafted.py` — Plan 01-04 — the evaluator this checkpoint's game is actually played with
- `ance/tools/random_mover_gauntlet.py` — Plan 01-05 (not exercised by this checkpoint; TOOL-01 and TOOL-02 are independent proof points per the ROADMAP)
- `tests/*` — Plans 01-01 through 01-05 (all automated coverage this checkpoint complements, not replaces)

<objective>
Manually verify, in a real UCI GUI, what the automated subprocess tests
in Plans 01-01 through 01-04 can only approximate: that ANCE is
compatible with an actual arbiter's expectations end to end — handshake,
a full game to a natural result, no disqualification, no hang.

Purpose: TOOL-01 is inherently a human-observation requirement (GUIs
aren't scriptable in this stack, per 01-RESEARCH.md's Environment
Availability table) and one of the milestone's two hard proof points
alongside TOOL-02.

Output: A human-confirmed full legal game played end to end in Cute Chess
or Arena.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-minimal-uci-engine-evaluator-seam/01-CONTEXT.md
@.planning/phases/01-minimal-uci-engine-evaluator-seam/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual GUI validation of a full legal game (TOOL-01)</name>
  <action>No code changes in this task — present the verification steps below to the human and wait for explicit confirmation that a full game completed cleanly in Cute Chess or Arena.</action>
  <what-built>
    A complete UCI engine (`<repo>/.venv/bin/python -m ance`) that
    completes the `uci`/`isready` handshake, accepts `position`/`go`/
    `stop`/`quit`, plays legal moves chosen by a fixed-depth negamax
    through the full Simplified-Evaluation-based `HandcraftedEval`, and
    rejects malformed input without crashing — all verified automatically
    by the pytest suites in Plans 01-01 through 01-05.
  </what-built>
  <how-to-verify>
    1. Install Cute Chess (prebuilt macOS binary from
       `https://github.com/cutechess/cutechess/releases`) or Arena, if
       not already installed.
    2. Register a new UCI engine pointing at
       `<repo>/.venv/bin/python` with the argument `-m ance` (or the
       equivalent "command + arguments" fields your GUI exposes).
    3. Start a new game with ANCE on one or both sides (ANCE vs. itself,
       or ANCE vs. a built-in weak opponent), at any time control.
    4. Confirm: (a) the engine shows as "ready"/loaded with no error
       dialog during the handshake; (b) the engine responds with a legal
       move every turn without the GUI ever reporting "no response",
       "timeout", or disqualifying the engine; (c) the game reaches a
       natural result (checkmate, stalemate, draw, or resignation-
       equivalent) rather than hanging or crashing mid-game.
  </how-to-verify>
  <resume-signal>Type "approved" once a full game completes cleanly in the GUI, or describe exactly what went wrong (e.g. "hung after move 12", "GUI disqualified on time", "handshake never completed").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI process <-> ANCE subprocess | Identical trust boundary to every automated test in this phase (stdin/stdout pipe) — this checkpoint is a human observation of behavior already covered by Plans 01-01 through 01-05's threat mitigations, not a new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Denial of Service (undetected by automated tests) | GUI-driven full game (timing/edge cases automated tests may not reach) | medium | accept | Manual human observation is the deliberate mitigation here — this is exactly why TOOL-01 is a checkpoint rather than an automated assertion; any hang found here is routed back to the relevant plan (01-03 threading, most likely) as a gap, not accepted silently |

</threat_model>

<verification>
A full game completes in Cute Chess or Arena without hanging, crashing, or the engine being disqualified.
</verification>

<success_criteria>
In a real UCI GUI, ANCE completes the handshake and plays a full legal
game to a natural result without hanging or being disqualified —
confirmed by direct human observation.
</success_criteria>

<output>
Create `.planning/phases/01-minimal-uci-engine-evaluator-seam/01-06-SUMMARY.md` when done
</output>
