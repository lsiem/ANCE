---
phase: 01-minimal-uci-engine-evaluator-seam
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified:
  - ance/tools/__init__.py
  - ance/tools/random_mover_gauntlet.py
  - tests/test_random_mover_gauntlet.py
autonomous: true
requirements: [TOOL-02]
must_haves:
  truths:
    - "ANCE (search_root + HandcraftedEval) beats a uniformly-random legal-move opponent 100 games out of 100, alternating colors, referereed by python-chess"
  artifacts:
    - ance/tools/random_mover_gauntlet.py
    - tests/test_random_mover_gauntlet.py
  key_links:
    - "run_gauntlet() drives play_game() in-process against search_root(pos, DEFAULT_DEPTH, HandcraftedEval(), ...) for every game, alternating which side ANCE plays"
---

## Phase Goal

**As a** chess GUI or UCI test harness driving ANCE over stdin/stdout, **I want to** complete the handshake and receive exactly one legal `bestmove` for every `go` — even through a swappable evaluator — **so that** I can play a complete, legal game and confirm ANCE reliably beats a random-mover opponent.

## Artifacts this phase produces

*(Full phase-wide artifact inventory — reproduced identically in every plan in this phase.)*

- `pyproject.toml`, `ance/__init__.py`, `ance/__main__.py` — built in Plan 01-01
- `ance/debug.py`, `ance/board/position.py` — built in Plan 01-02
- `ance/eval/base.py`, `ance/eval/material.py`, `ance/search/negamax.py`, `ance/uci/*` — built in Plan 01-03
- `ance/eval/tables.py`, `ance/eval/handcrafted.py` — built in Plan 01-04 (this plan's engine-under-test)
- `ance/tools/__init__.py`, `ance/tools/random_mover_gauntlet.py` — **built in this plan** — `RandomMover`, `play_game()`, `run_gauntlet()`
- `tests/conftest.py`, `tests/test_uci_handshake.py`, `tests/test_position_command.py`, `tests/test_go_bestmove.py`, `tests/test_eval_seam.py` — built in prior plans
- `tests/test_random_mover_gauntlet.py` — **built in this plan**

<objective>
Build the self-play harness that pits ANCE's real search+eval (in-process,
not over a UCI pipe — this is a measurement tool, not a protocol test)
against a uniformly-random legal-move opponent, and prove ANCE wins all
100 games in a fixed, seeded 100-game run.

Purpose: TOOL-02 is one of the milestone's two hard proof points (with
TOOL-01). A correct engine with a full evaluator should never need a draw
against a purely random opponent — this plan encodes "beats" as
wins == 100, losses == 0, per 01-RESEARCH.md's Wave 0 Gaps guidance.

Output: `ance/tools/random_mover_gauntlet.py` plus a `slow`-marked pytest
that asserts the 100/100 result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-minimal-uci-engine-evaluator-seam/01-CONTEXT.md
@.planning/phases/01-minimal-uci-engine-evaluator-seam/01-RESEARCH.md
@.planning/phases/01-minimal-uci-engine-evaluator-seam/01-04-SUMMARY.md
@ance/search/negamax.py
@ance/eval/handcrafted.py
@ance/board/position.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Random mover and single-game referee</name>
  <files>ance/tools/random_mover_gauntlet.py, tests/test_random_mover_gauntlet.py</files>
  <read_first>
    01-RESEARCH.md "Wave 0 Gaps" (random mover: uniformly pick from
    `board.legal_moves` with a seeded RNG; referee via python-chess) and
    "Validation Architecture" > Phase Requirements → Test Map row for
    TOOL-02; `.planning/research/PITFALLS.md` Pitfall 15 (opening
    diversity / fixed-seed variety — relevant here since ANCE vs. a
    uniformly-random opponent needs seed variety across the 100 games to
    avoid repeating the identical game, even though this is not a
    fixed-opening-book gauntlet in the Phase 3 sense).
  </read_first>
  <behavior>
    - Test `test_random_mover_picks_legal_move`: `RandomMover(seed=0).choose(board)`
      for the startpos returns a move that is a member of
      `list(board.legal_moves)`.
    - Test `test_random_mover_is_deterministic_per_seed`: two
      `RandomMover(seed=7)` instances given the same board return the
      same move; a `RandomMover(seed=8)` may return a different one.
    - Test `test_play_game_terminates_with_a_valid_result`: a single
      `play_game(ance_depth=2, ance_evaluator=MaterialEval(), ance_plays_white=True, seed=0, max_halfmoves=300)`
      call returns one of `{"1-0", "0-1", "1/2-1/2"}` within the halfmove
      cap (using `MaterialEval` here only to keep this unit test fast;
      the slow 100-game test in Task 2 uses the real `HandcraftedEval`).
  </behavior>
  <action>
    `ance/tools/random_mover_gauntlet.py` defines
    `class RandomMover: def __init__(self, seed: int): self._rng = random.Random(seed)`
    and `def choose(self, board: chess.Board) -> chess.Move` returning
    `self._rng.choice(list(board.legal_moves))`. Defines
    `def play_game(ance_depth: int, ance_evaluator: Evaluator, ance_plays_white: bool, seed: int, max_halfmoves: int = 300) -> str`
    that alternates calling `search_root(pos, ance_depth, ance_evaluator, threading.Event(), random.Random(seed))`
    for ANCE's turns and `RandomMover(seed).choose(pos.board)` for the
    opponent's turns (a fresh, un-set `threading.Event()` per ANCE move
    since this harness never needs to interrupt a search), applying
    whichever move comes back via `pos.board.push(move)`, and returning
    `pos.board.result()` once `pos.board.is_game_over()` or the
    `max_halfmoves` safety cap is hit (treat a cap-hit as `"1/2-1/2"`
    to keep the function total).
  </action>
  <acceptance_criteria>
    `ance/tools/random_mover_gauntlet.py` contains `class RandomMover`
    and `def play_game(`; `python -m pytest tests/test_random_mover_gauntlet.py -q -m "not slow"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_random_mover_gauntlet.py -q -m "not slow" -x</automated>
  </verify>
  <done>All three fast tests pass; play_game always terminates with a valid result string.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 100-game gauntlet proving 100/100 against the random mover</name>
  <files>ance/tools/random_mover_gauntlet.py, tests/test_random_mover_gauntlet.py</files>
  <read_first>
    01-RESEARCH.md "Wave 0 Gaps" (100-game harness, marked `slow`) and
    "Validation Architecture" > "Sampling Rate" (slow suite runs once per
    wave, not per commit); `ance/eval/handcrafted.py` (the real evaluator
    this test must use, per EVAL-02 being the M3 baseline TOOL-02 must
    beat with).
  </read_first>
  <behavior>
    - Test `test_ance_beats_random_mover_100_of_100` (marked
      `@pytest.mark.slow`): `run_gauntlet(n_games=100, ance_depth=ance.search.negamax.DEFAULT_DEPTH, evaluator=HandcraftedEval())`
      returns a result where `wins == 100` and `losses == 0` (draws, if
      any, fail this assertion — TOOL-02's literal "beats 100 games out
      of 100" is encoded as a strict win requirement, not win-or-draw).
  </behavior>
  <action>
    `ance/tools/random_mover_gauntlet.py` defines
    `def run_gauntlet(n_games: int, ance_depth: int, evaluator: Evaluator) -> dict`
    that loops `game_index in range(n_games)`, alternates
    `ance_plays_white = game_index % 2 == 0` for color fairness, calls
    `play_game(ance_depth, evaluator, ance_plays_white, seed=game_index)`,
    and tallies the result into `{"wins": int, "losses": int, "draws": int}`
    from ANCE's perspective (a `"1-0"` result counts as a win when ANCE
    played White and a loss when ANCE played Black, and symmetrically for
    `"0-1"`; `"1/2-1/2"` always counts as a draw). Register the test with
    `@pytest.mark.slow` (the marker already exists in `pyproject.toml`
    from Plan 01-01).
  </action>
  <acceptance_criteria>
    `ance/tools/random_mover_gauntlet.py` contains `def run_gauntlet(`;
    `tests/test_random_mover_gauntlet.py` contains
    `@pytest.mark.slow` immediately above
    `def test_ance_beats_random_mover_100_of_100`;
    `.venv/bin/python -m pytest tests/test_random_mover_gauntlet.py -q -m slow` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_random_mover_gauntlet.py -q -m slow -x</automated>
  </verify>
  <done>The 100-game gauntlet passes with wins == 100 and losses == 0 against the seeded random mover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Internal test tooling only; no external or untrusted input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Denial of Service (test-suite hang) | ance/tools/random_mover_gauntlet.py play_game | low | mitigate | `max_halfmoves` safety cap guarantees `play_game` always terminates even if a bug produced a pathological non-terminating line, keeping the `slow` test itself bounded |

</threat_model>

<verification>
Run `.venv/bin/python -m pytest tests/test_random_mover_gauntlet.py -q -m slow` (must be green; estimated 1-3 minutes per 01-VALIDATION.md).
</verification>

<success_criteria>
ANCE, playing through `search_root` + `HandcraftedEval`, beats the seeded
random-mover opponent 100 games out of 100, with colors alternated for
fairness.
</success_criteria>

<output>
Create `.planning/phases/01-minimal-uci-engine-evaluator-seam/01-05-SUMMARY.md` when done
</output>
