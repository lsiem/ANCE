---
phase: 04-offline-nnue-training-pipeline
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-04", "04-05"]
files_modified:
  - training/data/shards.py
  - training/train.py
  - tests/training/test_shards.py
  - tests/training/test_run_training_smoke.py
  - tests/training/test_pipeline_end_to_end_smoke.py
autonomous: true
requirements: [TRN-02, TRN-03, TRN-04, TRN-05]
must_haves:
  truths:
    - "Real FEN samples encode into an on-disk shard and load back into a torch Dataset with the exact tensor shapes/dtypes the model expects"
    - "A real ShardDataset-driven training run tracks per-epoch validation loss and checkpoints/reloads safely (weights_only=True, T-4-02)"
    - "The full mechanical pipeline (synthetic scores, no real labeling cost) runs end to end in seconds on tiny data and produces a validated exported net"
  artifacts:
    - training/data/shards.py
    - training/train.py
    - tests/training/test_pipeline_end_to_end_smoke.py
  key_links:
    - "training/data/shards.py ShardDataset -> torch.utils.data.DataLoader -> training/train.py run_training() -> training/export.py export_checkpoint()"
    - "training/data/features.py encode_position() -> training/data/shards.py build_shard() -> training/model.py NNUE.forward() (closes the loop started in Plan 04-05)"
  prohibitions:
    - statement: "torch.load is only ever called on this pipeline's own just-written checkpoint, always with weights_only=True"
      status: enforced
      verification: "grep -n 'weights_only=True' training/train.py"
---

## Phase Goal

**As a** developer preparing Phase 5's NNUE swap-in, **I want to** turn Stockfish-labeled chess positions into a validated, exported `(768→256)×2→1` weights file via an offline PyTorch/MPS pipeline, **so that** Phase 5 can load real trained weights behind the untouched `evaluate()` seam and prove a measurable Elo gain over the handcrafted baseline.

> Note: see Plan 04-01 for the full discrepancy note on why this user-story framing is derived rather than quoted verbatim from ROADMAP.md.

> MVP vertical-slice note: this is the phase's capstone thickening step — it wires every module built in Plans 04-01 through 04-05 into one real, composed pipeline and proves the full "tiny labeled sample set → encode features → train a handful of steps → export → numpy-only roundtrip-load and validate" slice using the actual modules, not isolated smoke stand-ins.

## Artifacts this phase produces

*(Full phase-wide artifact inventory — identical across every plan in this phase. See Plan 04-01 for the complete list and attribution. This plan builds: `training/data/shards.py`, extends `training/train.py` with `run_training`/`save_checkpoint`/`load_checkpoint`, and adds three test files.)*

<objective>
Build the on-disk shard format and `Dataset` wrapper (Claude's discretion, dense float32 `.npz` per 04-RESEARCH.md's recommendation), extend `training/train.py` with a real `DataLoader`-driven training loop that tracks per-epoch validation loss and checkpoints safely, and write the phase's capstone mechanical proof: every module from Plans 04-01 through 04-05 composed into one working pipeline on tiny synthetic data.

Purpose: Plan 04-02 proved the model/train/export mechanism works on `torch.randn` stand-ins. This plan proves the *real* modules (features, split, kfit, shards) compose correctly before Plan 04-07 spends real wall-clock hours on the actual overnight run.
Output: `training/data/shards.py`, an extended `training/train.py`, and a full pipeline smoke test — all passing in well under 30 seconds on synthetic data.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-offline-nnue-training-pipeline/04-CONTEXT.md
@.planning/phases/04-offline-nnue-training-pipeline/04-RESEARCH.md
@.planning/phases/04-offline-nnue-training-pipeline/04-PATTERNS.md
@.planning/phases/04-offline-nnue-training-pipeline/04-VALIDATION.md
@training/model.py
@training/train.py
@training/export.py
@training/mps_gate.py
@training/data/features.py
@training/data/split.py
@training/data/kfit.py
@training/label/position_source.py
@nnue_format/schema.py
@nnue_format/io.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: On-disk shard format + DataLoader-compatible dataset</name>
  <files>training/data/shards.py, tests/training/test_shards.py</files>
  <read_first>04-RESEARCH.md Open Question #3 ("Checkpoint cadence and DataLoader shard format") — recommends starting with dense float32 `.npy`/`.npz`; training/data/features.py (Plan 04-05, for `encode_position`'s exact signature).</read_first>
  <behavior>
    - Test 1: building a shard from a tiny synthetic sample list and loading it back yields arrays with the expected `(N, 768)`/`(N,)` shapes.
    - Test 2: wrapping the loaded shard in the Dataset class yields float32 tensors of the correct dtype.
  </behavior>
  <action>
Create `training/data/shards.py`, docstring noting the dense-float32 `.npz` format choice is Claude's Discretion per 04-CONTEXT.md, following 04-RESEARCH.md Open Question #3's recommendation to start simple and only move to sparse-index encoding if a measured throughput bottleneck later appears. `build_shard(samples: list[dict], out_path: str) -> None`: for each `sample` (a dict with `fen`, `cp`, `game_result` (nullable), `game_id`), calls `training.data.features.encode_position(sample["fen"])` to get `(stm, opp)`, stacks all samples into float32 numpy arrays — `stm_features (N, 768)`, `opp_features (N, 768)`, `cp (N,)`, `game_result (N,)` (using `np.nan` where absent), `has_result (N,)` boolean — and writes them via `np.savez(out_path, stm_features=..., opp_features=..., cp=..., game_result=..., has_result=...)`. `class ShardDataset(torch.utils.data.Dataset)` wraps a loaded `.npz` path, implementing `__len__` and `__getitem__(idx)` returning `(stm, opp, cp, game_result, has_result)` as `torch.from_numpy(...)`-wrapped float32 tensors (bool tensor for `has_result`).

Write `tests/training/test_shards.py` (`pytest.importorskip("torch")` at top): `test_build_and_load_shard_roundtrip()` builds a tiny sample list from `training.label.position_source.generate_position_set(n_games=1)` (assigning a placeholder deterministic `cp`/`game_result` to each), calls `build_shard`, loads the `.npz` back directly via `np.load`, and asserts `stm_features.shape == (N, 768)` and `cp.shape == (N,)` for the correct `N`. `test_shard_dataset_yields_correct_tensor_dtypes()` wraps the same `.npz` in `ShardDataset` and asserts `dataset[0][0].dtype == torch.float32`.
  </action>
  <acceptance_criteria>`training/data/shards.py` defines `build_shard` and `class ShardDataset`; the test file passes.</acceptance_criteria>
  <verify>
    <automated>pytest tests/training/test_shards.py -x -q</automated>
  </verify>
  <done>Real FEN samples encode into a `.npz` shard and load back into a `torch.utils.data.Dataset` with the exact tensor shapes/dtypes `training.model.NNUE.forward` expects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real DataLoader-driven training loop + safe checkpointing (TRN-03, T-4-02)</name>
  <files>training/train.py, tests/training/test_run_training_smoke.py</files>
  <read_first>training/train.py (Plan 04-02's existing `wdl_loss`/`preflight_mps_gate`/`train_smoke` — extend, do not remove); training/data/shards.py (this plan's Task 1); 04-RESEARCH.md Pitfall 6 ("Unattended 8-12h run with no resumability") and §Security Domain's T-4-02 threat pattern (`torch.load` pickle risk).</read_first>
  <behavior>
    - Test 1: running a few epochs on two tiny synthetic shards returns a per-epoch validation-loss list of the correct length, every entry a finite float.
    - Test 2: saving then loading a checkpoint reproduces the exact same model weights.
  </behavior>
  <action>
Extend `training/train.py` (keep `wdl_loss`, `preflight_mps_gate`, `train_smoke` unchanged — those remain the isolated smoke-test entry points from Plan 04-02) by adding `run_training(train_shard_path: str, val_shard_path: str, k: float, epochs: int, checkpoint_dir: str, checkpoint_every_n_steps: int = 500) -> dict`: builds a `training.data.shards.ShardDataset` + `torch.utils.data.DataLoader` for each of `train_shard_path`/`val_shard_path`; calls `preflight_mps_gate()` once at entry to select the device; runs the training loop on that device using `wdl_loss` with the passed `k`; at the end of every epoch, runs a no-gradient pass (`model.eval()`, `torch.no_grad()`) over the val loader and records the mean `wdl_loss`; returns `{"train_losses": [...], "val_losses": [...], "device": device}` — the per-epoch `val_losses` list is TRN-03's "decreasing, trustworthy validation loss" evidence.

Add `save_checkpoint(model, optimizer, epoch: int, path: str) -> None` using `torch.save({"model_state_dict": model.state_dict(), "optimizer_state_dict": optimizer.state_dict(), "epoch": epoch}, path)`, and `load_checkpoint(model, optimizer, path: str) -> int` using `torch.load(path, weights_only=True, map_location="cpu")` and returning the saved epoch number. `weights_only=True` is mandatory in `load_checkpoint` and is this plan's T-4-02 mitigation: document in the function's docstring that this pipeline only ever loads a checkpoint file it itself just wrote to `checkpoint_dir` — never a downloaded or externally-sourced `.pt` file — and `weights_only=True` blocks arbitrary pickle deserialization as defense in depth even against that self-written file.

Write `tests/training/test_run_training_smoke.py::test_run_training_val_loss_tracked_and_checkpoint_roundtrips`: build two tiny shards (5-10 samples each) via Task 1's `build_shard`, call `run_training(..., epochs=3, checkpoint_dir=str(tmp_path))`, assert the returned `val_losses` has length 3 and every entry is a finite float (`math.isfinite`); then call `save_checkpoint`/`load_checkpoint` on the resulting model/optimizer and assert every tensor in the reloaded model's `state_dict()` matches the saved one via `torch.allclose`.
  </action>
  <acceptance_criteria>`training/train.py` defines `run_training`, `save_checkpoint`, `load_checkpoint`, with `weights_only=True` present in the `torch.load` call; the test passes.</acceptance_criteria>
  <verify>
    <automated>pytest tests/training/test_run_training_smoke.py -x -q && grep -n "weights_only=True" training/train.py</automated>
  </verify>
  <done>A real `ShardDataset`-driven training run tracks per-epoch validation loss and checkpoints/reloads safely with `weights_only=True` enforced — T-4-02's mitigation is present and testable, not just documented.</done>
</task>

<task type="auto">
  <name>Task 3: Full mechanical pipeline smoke test (capstone vertical slice)</name>
  <files>tests/training/test_pipeline_end_to_end_smoke.py</files>
  <read_first>Every module built in Plans 04-01 through 04-06's Tasks 1-2: training/mps_gate.py, nnue_format/schema.py + io.py, training/model.py, training/train.py, training/export.py, training/label/position_source.py, training/data/features.py, training/data/split.py, training/data/kfit.py, training/data/shards.py.</read_first>
  <action>
Write `tests/training/test_pipeline_end_to_end_smoke.py::test_full_pipeline_smoke_tiny_dataset` — the phase's capstone mechanical proof, composing every real module (no isolated stand-ins): call `training.label.position_source.generate_position_set(n_games=8, seed=7)` to get roughly 8 synthetic games' worth of FENs; assign each sample a deterministic synthetic `cp` (e.g. derived from a simple material-count difference on the parsed board — no real Stockfish/Lichess dependency, keeping this test fast and hermetic) and a `game_result` computed from a fixed sigmoid-like rule so every sample has `has_result=True`; run the sample list through `training.data.split.split_by_game` and `training.data.split.assert_no_fen_leakage`; call `training.data.kfit.fit_k_from_samples` on the train split to get a fitted `k`; call `training.data.shards.build_shard` for both splits into `tmp_path`; call `training.train.run_training(..., epochs=2, k=fitted_k, checkpoint_dir=str(tmp_path))`; call `training.export.export_checkpoint(model, k_scale=fitted_k, path=str(tmp_path / "net.safetensors"))`; call `nnue_format.io.load_net(...)`. Assert the whole chain completes without raising, the returned `val_losses` list has length 2, and the loaded weights' shapes exactly match `nnue_format.schema.EXPECTED_SHAPES`.
  </action>
  <acceptance_criteria>The test completes in well under 30 seconds using tiny synthetic data on CPU and exits 0.</acceptance_criteria>
  <verify>
    <automated>pytest tests/training/test_pipeline_end_to_end_smoke.py -x -q</automated>
  </verify>
  <done>The complete real-module pipeline (labeling-free, using synthetic scores) runs end to end in seconds and produces a validated exported net — the mechanical proof that every prior plan's module composes correctly before Plan 04-07 spends real wall-clock hours on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Checkpoint files on disk → `torch.load` in `training/train.py::load_checkpoint` | Self-written-only trust boundary; would be a Tampering/RCE risk if ever pointed at an untrusted file |
| Real shard/dataset files on disk → `training/data/shards.py::ShardDataset` | Internal artifacts produced by this pipeline's own earlier stages; no external trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-02 | Tampering | `training/train.py` (`save_checkpoint`/`load_checkpoint`) | high | mitigate | This plan's `load_checkpoint` enforces `weights_only=True` and only ever reads a path this pipeline's own `save_checkpoint` just wrote; the final shipped artifact remains safetensors (zero pickle), per Plan 04-01/04-02 |
| T-4-01 | Denial of Service | `training/data/lichess_ingest.py` (Plan 04-04, not this plan) | high | mitigate | Handled in Plan 04-04 |
| T-4-03 | Tampering | `training/label/stockfish_labeler.py` subprocess (Plan 04-03, not this plan) | low | accept | Handled in Plan 04-03 |

</threat_model>

<verification>
Run `pytest tests/training/ -x -q` after this plan completes. Run `pytest tests/ tests/training/ -q` (full suite) to confirm the torch-only addition still never leaks into `ance/`.
</verification>

<success_criteria>
- Real FEN samples shard and load correctly into a torch `Dataset`.
- A real training run tracks per-epoch validation loss and checkpoints/reloads with `weights_only=True` enforced.
- The full real-module pipeline (synthetic scores) runs end to end in seconds and produces a validated exported net.
</success_criteria>

<output>
Create `.planning/phases/04-offline-nnue-training-pipeline/04-06-SUMMARY.md` when done.
</output>
