---
phase: 02-core-alpha-beta-search
plan: 09
type: execute
wave: 7
depends_on:
  - 02-06
files_modified:
  - ance/tools/depth_vs_depth_match.py
  - tests/test_depth_vs_depth.py
autonomous: true
gap_closure: true
requirements:
  - SRCH-02
  - SRCH-03
  - SRCH-04
must_haves:
  truths:
    - "A game result has one convention only: play_depth_match_game returns win/draw/loss from the deeper side's perspective, and run_depth_match tallies that value without applying color interpretation again."
    - "Deeper-white and deeper-black wins, losses, and draws each map to the correct counter; two synthetic deeper-side wins tally 2W/0L rather than 1W/1L."
    - "The seed deterministically selects and applies a legal opening line before search; equal seeds reproduce the same start and different pinned seeds select different openings."
    - "Color assignment and opening selection are independent: run_depth_match alternates deeper-side color while cycling a reproducible opening set."
  artifacts:
    - path: ance/tools/depth_vs_depth_match.py
      provides: "single-convention DepthMatchOutcome and deterministic seeded opening selection"
      contains: "DepthMatchOutcome"
    - path: tests/test_depth_vs_depth.py
      provides: "six result-classification cases plus seeded-opening determinism tests"
  key_links:
    - from: ance/tools/depth_vs_depth_match.py::play_depth_match_game
      to: ance/tools/depth_vs_depth_match.py::run_depth_match
      via: "DepthMatchOutcome returned and tallied directly"
      pattern: "DepthMatchOutcome"
    - from: ance/tools/depth_vs_depth_match.py::_opening_for_seed
      to: chess.Board.push_uci
      via: "validated predefined legal UCI opening line"
      pattern: "seed % len"
---

<objective>
Make the depth-vs-depth harness trustworthy before collecting any more strength evidence by fixing result inversion and turning the inert seed into reproducible opening variation.

Purpose: Close the D-14 classification blocker with fast table-driven tests; do not repeat the prior 2h25m five-game run in this plan.
Output: Correct result semantics, deterministic opening sampling, and bounded unit/integration regressions.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exhaust result and seed semantics (RED)</name>
  <files>tests/test_depth_vs_depth.py</files>
  <read_first>ance/tools/depth_vs_depth_match.py, tests/test_depth_vs_depth.py, ance/tools/random_mover_gauntlet.py, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md</read_first>
  <action>
    Replace the current slow-test-only coverage with a fast table-driven contract plus the retained slow marker.

    Parameterize all six decisive combinations and both draw combinations: deeper white with board results `1-0`, `0-1`, `1/2-1/2`; deeper black with those same results. Stub game termination/search so each case is deterministic and assert `play_depth_match_game` returns exactly `"win"`, `"loss"`, or `"draw"` from the deeper side's perspective. Stub two games where deeper white and deeper black both win, call `run_depth_match`, and assert `2W/0D/0L` and `score_rate == 1.0`.

    Add seed tests against the planned predefined opening helper: seed 0 is reproducible, seed 1 selects a different pinned line, every configured line can be pushed legally from startpos, and the board after setup has exactly the configured number of opening plies. Add a `run_depth_match` spy asserting game indices alternate `deep_plays_white` while seeds advance independently from a `seed` base argument.

    Keep the statistical slow test but do not run it here; Plan 02-10 owns the correctly sized evidence run.
  </action>
  <acceptance_criteria>
    - "Fast tests cover deeper-white/deeper-black × white-win/black-win/draw and assert deeper-perspective outcomes."
    - "A two-game synthetic match of one deeper-white win and one deeper-black win asserts exactly `wins=2`, `losses=0`, `draws=0`."
    - "Seed tests prove same-seed reproducibility, pinned seed variation, and legal application of every opening line."
    - "The fast tests complete in under 2 seconds and do not invoke a real depth-4 search."
    - "`.venv/bin/python -m pytest tests/test_depth_vs_depth.py -m 'not slow' -q` exits non-zero before implementation."
    - "Commit the test-only RED state atomically as `test(02-09): expose depth match inversion and inert seeds (RED)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_depth_vs_depth.py -m "not slow" -q; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize once and apply deterministic openings (GREEN)</name>
  <files>ance/tools/depth_vs_depth_match.py, tests/test_depth_vs_depth.py</files>
  <read_first>ance/tools/depth_vs_depth_match.py, tests/test_depth_vs_depth.py, ance/board/position.py</read_first>
  <action>
    Define `DepthMatchOutcome = Literal["win", "draw", "loss"]`. Keep python-chess's raw `"1-0"|"0-1"|"1/2-1/2"` only inside `play_depth_match_game`; convert it exactly once there using `deep_color`, then have `run_depth_match` increment counters directly from the returned deeper-perspective outcome with no color-dependent second pass.

    Add `OPENING_LINES` as a tuple of at least eight short, legal, four-ply UCI sequences spanning distinct first moves (for example e-pawn, d-pawn, c-pawn, English, Réti, and flank openings). Add `_opening_for_seed(seed) -> tuple[str, ...]` using `seed % len(OPENING_LINES)` and `_apply_opening(pos, line)` that validates/pushes each UCI move before the game loop. Apply the chosen opening before deciding whose search depth is used. The same seed must always select the same line.

    Extend `run_depth_match(..., seed: int = 0, max_halfmoves: int = 300)` so game `i` uses `seed + i`, alternates deeper color from `i % 2`, and forwards `max_halfmoves`. Reject `n_games <= 0` with `ValueError` so score-rate division cannot be undefined. Update the slow test declaration to 30 games, shallow depth 2, deep depth 3, `max_halfmoves=80`, and a documented 45-minute standalone budget; mark it for Plan 02-10 execution rather than running it now.
  </action>
  <acceptance_criteria>
    - "`play_depth_match_game` returns `DepthMatchOutcome`; `run_depth_match` contains no branch combining raw result strings with `deep_white`."
    - "`OPENING_LINES` contains at least eight legal four-ply lines and `_opening_for_seed` reads its seed."
    - "`run_depth_match` exposes keyword-compatible `seed: int = 0` and `max_halfmoves: int = 300` and rejects non-positive game counts."
    - "The slow test requests exactly 30 games at depth 2 versus depth 3 with `max_halfmoves=80`; it is not executed by this plan."
    - "`.venv/bin/python -m pytest tests/test_depth_vs_depth.py -m 'not slow' -q` and the full fast suite exit 0."
    - "Commit the production fix atomically as `fix(02-09): normalize depth outcomes and seed opening variation (GREEN)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_depth_vs_depth.py -m "not slow" -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
</task>

</tasks>

<verification>
- Eight table-driven color/result cases pass.
- Two normalized deeper-side wins tally 2W/0L with score rate 1.0.
- Seed 0 reproduces exactly; pinned distinct seeds choose distinct legal opening lines.
- Full fast suite passes; no multi-hour match is rerun.
</verification>

<success_criteria>
- The depth-match result can no longer be color-normalized twice.
- Every game has reproducible opening variation controlled by the exposed seed.
- The harness is ready for the sized, budgeted evidence run in 02-10.
- RED and GREEN are separate atomic commits.
</success_criteria>

<threat_model>
| Threat ID | Category | Component | Severity | Mitigation |
|---|---|---|---|---|
| T-02-09a | Tampering | result normalization | blocker | Deeper-perspective outcome type and exhaustive color/result table. |
| T-02-09b | Repudiation | opening randomness | high | Fixed checked-in opening lines and deterministic seed mapping. |
| T-02-09c | Denial of Service | match length | medium | `max_halfmoves` is forwarded and bounded evidence parameters are explicit. |
| T-02-09d | Tampering | invalid openings | medium | Every configured UCI move is legality-checked and all lines are covered by tests. |
</threat_model>

## Artifacts this phase produces

- `DepthMatchOutcome` type alias — `ance/tools/depth_vs_depth_match.py`.
- `OPENING_LINES` constant — `ance/tools/depth_vs_depth_match.py`.
- `_opening_for_seed(seed)` helper — `ance/tools/depth_vs_depth_match.py`.
- `_apply_opening(pos, line)` helper — `ance/tools/depth_vs_depth_match.py`.
- New `seed` and `max_halfmoves` options on `run_depth_match`.
- No new file or external dependency.

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-09-SUMMARY.md` after execution.
</output>
