---
phase: 02-core-alpha-beta-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ance/search/types.py
  - ance/search/negamax.py
  - tests/test_alpha_beta.py
  - tests/test_eval_seam.py
autonomous: true
requirements:
  - SRCH-02
must_haves:
  truths:
    - "Negamax uses fail-soft alpha-beta pruning (beta cutoffs return beta, not fail-hard)"
    - "Checkmate scores propagate as ±(MATE − ply) with correct negamax sign"
    - "Stalemate terminal nodes score 0"
    - "Root move selection is deterministic — no RNG tie-break (D-10)"
  artifacts:
    - path: ance/search/types.py
      provides: SearchContext and SearchResult dataclasses
      contains: "class SearchResult"
    - path: ance/search/negamax.py
      provides: fail-soft alpha-beta negamax with ply-adjusted mate
      contains: "def negamax"
    - path: tests/test_alpha_beta.py
      provides: alpha-beta and mate scoring regression tests
  key_links:
    - from: ance/search/negamax.py
      to: ance/eval/base.py
      via: Evaluator Protocol only (no concrete eval imports)
      pattern: "from ance.eval.base import"
---

## Phase Goal

**As a** developer running ANCE in a UCI GUI, **I want to** search with alpha-beta pruning and correct mate scoring, **so that** the engine finds tactics faster than brute-force negamax and reports accurate mate distances.

<objective>
Implement fail-soft alpha-beta negamax with ply-adjusted mate propagation and deterministic root selection on the Phase 1 skeleton.

Purpose: SRCH-02 is the pruning foundation every later search feature builds on; mate scoring must be correct before quiescence and ID layer on.
Output: `SearchContext`/`SearchResult` types, refactored `negamax`/`search_root` with α-β and D-10 deterministic ties.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-alpha-beta-search/02-CONTEXT.md
@.planning/phases/02-core-alpha-beta-search/02-RESEARCH.md
@ance/search/negamax.py
@ance/eval/base.py
@tests/test_eval_seam.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing alpha-beta and mate-scoring tests</name>
  <files>tests/test_alpha_beta.py, tests/test_eval_seam.py</files>
  <read_first>ance/search/negamax.py, ance/eval/base.py, tests/test_eval_seam.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Mate-in-1 FEN (a1a8) at depth 2 returns score >= MATE-2 after search (ply-adjusted mate propagation per D-00a)
    - Test 2: Stalemate FEN scores 0 at terminal node
    - Test 3: Alpha-beta visits fewer nodes than unpruned on a tactical FEN (e.g. queen hanging — use node counter from SearchContext; unpruned baseline can be simulated at high alpha/beta window or compare against pre-refactor count upper bound)
    - Test 4: Equal-scoring root moves return the first legal move in move list order (deterministic D-10) — replaces test_search_root_tie_break_uses_seeded_rng
    - Test 5: search_root signature no longer accepts rng parameter
  </behavior>
  <acceptance_criteria>
    - tests/test_alpha_beta.py exists with at least 4 test functions
    - pytest tests/test_alpha_beta.py exits non-zero (RED) before implementation
    - tests/test_eval_seam.py removes or rewrites test_search_root_tie_break_uses_seeded_rng to assert deterministic first-move tie-break instead of seeded RNG
  </acceptance_criteria>
  <action>
    Create tests/test_alpha_beta.py with fast unit tests driving search_root/negamax via Position + MaterialEval or HandcraftedEval. Use chess.Board FENs from existing test_eval_seam mate-in-one case. Add SearchContext placeholder imports expecting ance/search/types.py (create minimal stub dataclasses if needed for RED). Remove rng= arguments from test_eval_seam search_root calls throughout the file. Delete test_search_root_tie_break_uses_seeded_rng or replace with test_search_root_deterministic_tie_break_first_move per D-10.
  </action>
  <verify>
    <automated>pytest tests/test_alpha_beta.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED tests exist; eval_seam no longer asserts RNG tie-break behavior</done>
</task>

<task type="auto">
  <name>Task 2: Fail-soft alpha-beta + ply-adjusted mate + SearchResult types</name>
  <files>ance/search/types.py, ance/search/negamax.py</files>
  <read_first>ance/search/negamax.py, ance/eval/base.py, tests/test_alpha_beta.py, .planning/phases/02-core-alpha-beta-search/02-RESEARCH.md</read_first>
  <acceptance_criteria>
    - ance/search/types.py defines SearchContext (stop_flag, counter, evaluator, ply, path_keys, game_history_keys, deadline, max_depth fields) and SearchResult (best_move, score, depth, pv, nodes)
    - negamax accepts alpha, beta, SearchContext; returns fail-soft scores (beta cutoff returns beta)
    - Terminal checkmate returns -(MATE - ctx.ply); stalemate returns 0
    - search_root removes rng parameter; breaks ties by first-found best move; returns SearchResult with depth equal to requested fixed depth
    - tests/test_alpha_beta.py exits 0
    - pytest -m "not slow" -q exits 0 (fix any broken callers passing rng)
  </acceptance_criteria>
  <action>
    Create ance/search/types.py with SearchContext and SearchResult dataclasses per 02-RESEARCH.md Pattern 2. Refactor negamax to fail-soft alpha-beta per D-00a mate convention (search is sole mate scorer with ply adjustment per CONTEXT D-00a). Remove random import and all rng usage from negamax.py per D-10. Update search_root to build SearchContext, run fixed-depth search (no ID yet — Plan 03), return SearchResult. Temporarily update ance/uci/loop.py and ance/tools/random_mover_gauntlet.py call sites to use SearchResult.best_move and drop rng argument so the fast suite stays green — minimal glue only, no UCI info changes in this plan.
  </action>
  <verify>
    <automated>pytest tests/test_alpha_beta.py tests/test_eval_seam.py -q</automated>
  </verify>
  <done>SRCH-02 satisfied at fixed depth; deterministic root; all fast tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search→eval | Evaluator receives Position; must not import concrete eval in negamax |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | negamax.py eval import | mitigate | Keep Evaluator Protocol-only import; test_eval_seam structural test still passes |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
pytest -m "not slow" -q passes. Node counter proves alpha-beta reduces visits vs wide-window search on tactical FEN.
</verification>

<success_criteria>
Fail-soft alpha-beta active; mate scores use ±(MATE−ply); root selection deterministic; SearchResult types exported.
</success_criteria>

## Artifacts this phase produces

- `SearchContext` dataclass — `ance/search/types.py`
- `SearchResult` dataclass — `ance/search/types.py`
- `negamax(pos, depth, alpha, beta, ctx)` — `ance/search/negamax.py`
- `search_root(pos, max_depth, evaluator, stop_flag, *, deadline=None) -> SearchResult` — `ance/search/negamax.py`

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-01-SUMMARY.md` when done
</output>
