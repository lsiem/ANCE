---
phase: 02-core-alpha-beta-search
plan: 07
type: execute
wave: 7
depends_on:
  - 02-06
files_modified:
  - ance/search/negamax.py
  - tests/test_iterative_deepening.py
  - tests/test_search_deadline.py
  - tests/test_search_telemetry.py
autonomous: true
gap_closure: true
requirements:
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-07
  - UCI-11
must_haves:
  truths:
    - "Game-history repetition is detected from the real python-chess move stack; a root child returning to an earlier game position scores 0 (SRCH-07, D-06/D-07)."
    - "Stop and monotonic deadline expiry are both polled inside negamax and quiescence at NODE_POLL_INTERVAL, so an expired subtree raises SearchAborted and search_root retains the last completed depth (D-09/D-17)."
    - "Each iterative-deepening iteration starts from a zero node counter; completed callbacks report exact cumulative totals 10, 20, 30 for three synthetic 10-node iterations and the final SearchResult reports 30, not 70."
    - "NPS is derived from corrected cumulative nodes and total root-search elapsed time; aborted partial-iteration nodes are not advertised in a completed-depth info callback."
  artifacts:
    - path: ance/search/negamax.py
      provides: "full-stack game-history reconstruction, in-tree deadline polling, and exact cumulative iterative node accounting"
      contains: "board.copy(stack=True)"
    - path: tests/test_iterative_deepening.py
      provides: "real-history repetition regression using a board populated by legal pushes"
    - path: tests/test_search_deadline.py
      provides: "deterministic in-tree deadline and last-completed-depth retention tests"
    - path: tests/test_search_telemetry.py
      provides: "deterministic cumulative node/NPS accounting tests"
  key_links:
    - from: ance/search/negamax.py::_build_game_history_keys
      to: chess.Board.move_stack
      via: "stack-preserving board copy followed by pop/hash reconstruction"
      pattern: "copy\\(stack=True\\)"
    - from: ance/search/negamax.py::_poll_stop
      to: SearchContext.deadline
      via: "time.monotonic comparison on the same bounded polling cadence as stop_flag"
      pattern: "ctx.deadline"
    - from: ance/search/negamax.py::search_root
      to: info_callback
      via: "callback after adding the completed iteration delta to total_nodes"
      pattern: "info_callback"
---

<objective>
Repair the three search-core correctness gaps found by verification: reconstruct prior game positions from the real move stack, enforce deadlines inside both search trees, and make iterative node/NPS telemetry mathematically exact.

Purpose: Close SRCH-07's functional repetition failure and restore the timing/accounting guarantees consumed by SRCH-03, SRCH-04, and UCI-11 without changing search features or move ordering.
Output: Corrected `ance/search/negamax.py` plus bounded deterministic regression tests; no multi-hour strength run.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin history, deadline, and telemetry defects (RED)</name>
  <files>tests/test_iterative_deepening.py, tests/test_search_deadline.py, tests/test_search_telemetry.py</files>
  <read_first>ance/search/negamax.py, ance/search/types.py, tests/test_iterative_deepening.py, tests/test_uci_info.py, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md</read_first>
  <action>
    Add three deterministic regression groups before changing production code.

    In `tests/test_iterative_deepening.py`, build a `chess.Board` exclusively with legal pushes for `g1f3 g8f6 f3g1 f6g8 g1f3`, then verify `_build_game_history_keys()` contains the hashes for every prior state rather than only the current state. Add a root-to-interior test that pushes `g8f6` from that real-history root and calls `negamax` with the builder's output (never a manually injected history set); the returned child score must be 0 because the resulting position occurred earlier in the game.

    Create `tests/test_search_deadline.py`. Pin `_poll_stop` at its actual boundary by constructing a `SearchContext` with `counter=[NODE_POLL_INTERVAL - 1]`, an already-expired monotonic deadline, and an unset stop event, then entering both `negamax` and `quiescence_search`; each must raise `SearchAborted` after the node increment. Add an integration test with `NODE_POLL_INTERVAL` monkeypatched to 1 and a controlled evaluator/clock proving an expiry during depth N returns the fully completed depth N-1 result and emits no callback for N.

    Create `tests/test_search_telemetry.py`. Monkeypatch `_search_at_depth` to return exactly 10 iteration-local nodes at depths 1, 2, and 3 and patch monotonic timestamps deterministically. Assert callback node totals are `[10, 20, 30]`, callback NPS values use those cumulative totals and root elapsed time, and final `SearchResult.nodes == 30`. Also assert an aborted synthetic fourth iteration does not emit a fourth callback.
  </action>
  <acceptance_criteria>
    - "The real-history test uses `board.push(...)` for all five setup plies and does not construct `game_history_keys` manually."
    - "The deadline tests exercise both `negamax` and `quiescence_search` with an unset stop event and an expired `deadline`."
    - "The telemetry test asserts callback totals exactly `[10, 20, 30]` and final nodes exactly `30`."
    - "`.venv/bin/python -m pytest tests/test_iterative_deepening.py tests/test_search_deadline.py tests/test_search_telemetry.py -q` exits non-zero before implementation."
    - "Commit the test-only RED state atomically as `test(02-07): expose history deadline and node accounting gaps (RED)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_iterative_deepening.py tests/test_search_deadline.py tests/test_search_telemetry.py -q; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconstruct history and correct search polling/accounting (GREEN)</name>
  <files>ance/search/negamax.py</files>
  <read_first>ance/search/negamax.py, ance/search/types.py, tests/test_iterative_deepening.py, tests/test_search_deadline.py, tests/test_search_telemetry.py</read_first>
  <action>
    Make the smallest search-core changes that satisfy the RED tests.

    Change `_build_game_history_keys(board)` to copy with `stack=True`, hash the current board, then repeatedly `pop()` and hash each predecessor until the copied stack is empty. Do not mutate the caller's board and do not replace the existing Zobrist set contract.

    Change `_poll_stop(ctx)` so the existing `ctx.counter[0] % NODE_POLL_INTERVAL == 0` branch raises `SearchAborted` when either `ctx.stop_flag.is_set()` or (`ctx.deadline is not None` and `time.monotonic() >= ctx.deadline`). Keep one shared poll helper used by both `negamax` and `quiescence_search`; do not add per-node clock calls outside the bounded interval.

    Make `_search_at_depth` iteration-local: initialize `counter=[0]`, return that iteration's delta in `SearchResult.nodes`, and remove `nodes_at_start`, `start_time`, and callback emission from this helper. In `search_root`, after a depth completes, add its delta once to `total_nodes`, copy the cumulative total into the completed result, compute NPS from `total_nodes` and elapsed time since the one root `start_time`, and invoke `info_callback` there. Preserve the rule that `last_completed` changes only after a full iteration and that `SearchAborted` emits no partial-depth callback.
  </action>
  <acceptance_criteria>
    - "`_build_game_history_keys` contains `board.copy(stack=True)` and leaves the original board FEN and move-stack length unchanged."
    - "`_poll_stop` checks both `stop_flag` and `deadline` only on the `NODE_POLL_INTERVAL` cadence."
    - "`_search_at_depth` starts `counter = [0]` and no longer accepts `nodes_at_start`, `start_time`, or `info_callback`."
    - "`search_root` adds each completed iteration's nodes exactly once and emits cumulative node/NPS data after that addition."
    - "The focused regression command exits 0, and `.venv/bin/python -m pytest -m 'not slow' -q` exits 0."
    - "Commit the production fix atomically as `fix(02-07): restore repetition deadlines and exact search telemetry (GREEN)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_iterative_deepening.py tests/test_search_deadline.py tests/test_search_telemetry.py tests/test_uci_info.py -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
</task>

</tasks>

<verification>
- The five-ply history probe retains all predecessor hashes and the real-history repeated child scores 0.
- Expired-deadline probes abort from both negamax and qsearch at the configured poll boundary.
- Synthetic 10-node iterations report cumulative nodes 10/20/30 and final nodes 30 with no partial-depth callback.
- Full fast suite remains green; no slow gauntlet is run in this plan.
</verification>

<success_criteria>
- SRCH-07 no longer relies on discarded history.
- Timed search cannot remain indefinitely inside a root subtree; overrun is bounded by `NODE_POLL_INTERVAL` nodes plus one node's work.
- UCI node and NPS fields represent actual cumulative completed work without double-counting.
- RED and GREEN are separate atomic commits.
</success_criteria>

<threat_model>
| Threat ID | Category | Component | Severity | Mitigation |
|---|---|---|---|---|
| T-02-07a | Tampering | repetition key reconstruction | high | Reconstruct on a stack-preserving copy and prove the caller board is unchanged. |
| T-02-07b | Denial of Service | negamax/qsearch deadline | high | Poll monotonic deadline on the existing bounded node cadence in both trees. |
| T-02-07c | Repudiation | nodes/NPS telemetry | medium | Exact synthetic deltas and timestamps make cumulative accounting independently reproducible. |
| T-02-07d | Tampering | dependencies | low | No new package or external input is introduced. |
</threat_model>

## Artifacts this phase produces

- `tests/test_search_deadline.py` — new deterministic deadline regression module.
- `tests/test_search_telemetry.py` — new deterministic cumulative node/NPS regression module.
- No new production symbol, field, file, or CLI option; existing `_build_game_history_keys`, `_poll_stop`, `_search_at_depth`, and `search_root` are modified.

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-07-SUMMARY.md` after execution.
</output>
