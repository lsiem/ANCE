---
phase: 02-core-alpha-beta-search
plan: 08
type: execute
wave: 7
depends_on:
  - 02-06
files_modified:
  - ance/uci/loop.py
  - tests/test_go_bestmove.py
  - tests/test_uci_generation.py
autonomous: true
gap_closure: true
requirements:
  - UCI-11
must_haves:
  truths:
    - "Every search generation owns a distinct cancellation Event and movetime Timer; a token set for a stale live worker is never cleared or reused by a newer search."
    - "A new go invalidates the prior generation before preemption, and both info and bestmove emission check generation under one lock so no stale output can appear after the new generation begins."
    - "stop signals the active generation without invalidating it, allowing exactly one current bestmove; position/ucinewgame preserve the normal joined-worker flush but invalidate a worker that survives the bounded join."
    - "A forced join-timeout regression proves the stale worker remains cancelled while the replacement worker has a fresh unset token and is the only generation allowed to emit info/bestmove."
  artifacts:
    - path: ance/uci/loop.py
      provides: "per-generation SearchJob ownership and generation-locked output gates"
      contains: "class SearchJob"
    - path: tests/test_uci_generation.py
      provides: "forced stale-worker cancellation and info/bestmove race regressions"
    - path: tests/test_go_bestmove.py
      provides: "current stop still returns exactly one bestmove regression"
  key_links:
    - from: ance/uci/loop.py::handle_go
      to: SearchJob.stop_event
      via: "fresh Event allocation for every generation"
      pattern: "threading.Event\\(\\)"
    - from: ance/uci/loop.py::_run_search
      to: ance/uci/loop.py::_emit_info
      via: "generation-capturing callback"
      pattern: "my_generation"
    - from: ance/uci/loop.py::_emit_info
      to: search_generation
      via: "generation lock held across equality check and stdout emission"
      pattern: "generation_lock"
---

<objective>
Eliminate the stale-worker cancellation and output-generation race by giving each search its own cancellation/timer state and applying the existing generation rule to both `info` and `bestmove`.

Purpose: Restore the non-blocking worker model's safety under overlapping `go` commands and join timeouts, closing UCI-11 stream-integrity failures without serializing the reader thread.
Output: Per-generation worker ownership in `ance/uci/loop.py` and deterministic concurrency regressions.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce stale token reuse and ungated info (RED)</name>
  <files>tests/test_uci_generation.py, tests/test_go_bestmove.py</files>
  <read_first>ance/uci/loop.py, tests/test_go_bestmove.py, tests/test_uci_info.py, tests/conftest.py, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md</read_first>
  <action>
    Create `tests/test_uci_generation.py` as an in-process deterministic concurrency suite using monkeypatches and synchronization Events rather than real deep searches.

    Add a forced-timeout test whose fake first `search_root` blocks past `_stop_active_worker(timeout=0.01)`. Start generation 1, issue generation 2, and capture the events supplied to both fake searches. Assert the two event objects are distinct, generation 1's event remains set after generation 2 starts, and generation 2's event begins unset.

    Patch `send_info_depth` and `send_bestmove` to append tagged records. Let generation 1 finish a synthetic completed depth only after generation 2 has begun, then finish generation 2. Assert no generation-1 `info` or `bestmove` record is appended after generation 2 starts, while generation 2 emits its info and exactly one bestmove.

    Add a lock-boundary race test: pause generation 1 immediately before its output gate, advance `search_generation`, release it, and assert the stale callback is dropped. Extend `tests/test_go_bestmove.py` only with the current-generation invariant: `stop` must signal the active token and still yield exactly one legal bestmove.
  </action>
  <acceptance_criteria>
    - "All concurrency tests use synchronization Events/barriers and complete in under 2 seconds; none uses `time.sleep` as the primary ordering mechanism."
    - "The forced-timeout test asserts `old_event is not new_event`, `old_event.is_set() is True`, and `new_event.is_set() is False`."
    - "The output test separately records `info` and `bestmove` and proves only the replacement generation emits after preemption."
    - "`.venv/bin/python -m pytest tests/test_uci_generation.py tests/test_go_bestmove.py -q` exits non-zero against the shared global stop event/ungated callback implementation."
    - "Commit the test-only RED state atomically as `test(02-08): expose stale worker generation races (RED)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_uci_generation.py tests/test_go_bestmove.py -q; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate SearchJob cancellation and gate all output (GREEN)</name>
  <files>ance/uci/loop.py</files>
  <read_first>ance/uci/loop.py, tests/test_uci_generation.py, tests/test_go_bestmove.py, ance/search/negamax.py, ance/uci/protocol.py</read_first>
  <action>
    Replace the shared `stop_flag`/`movetime_timer` ownership with a module-local `SearchJob` dataclass containing `generation: int`, `stop_event: threading.Event`, `thread: threading.Thread | None`, and `timer: threading.Timer | None`; retain one `active_job: SearchJob | None`. Allocate a fresh `threading.Event()` in every `handle_go`, bind a movetime Timer only to that event, and pass the event directly to `_run_search`.

    Refactor `_stop_active_worker(timeout=..., invalidate_on_timeout=...)` to capture the current job, set its event, cancel its timer, and join its thread. Never call `clear()` on any event. If the thread survives and the caller is `position`, `ucinewgame`, or `quit`, invalidate that generation before returning so its later output is dropped; if it joined, preserve the existing stop-and-flush behavior. A new `go` continues to increment generation before preempting the old job. `handle_stop` sets only `active_job.stop_event` and does not advance generation.

    Add `generation_lock = threading.Lock()`. Change `_emit_info` to accept `my_generation`; hold the lock across current-generation comparison and `send_info_depth`. In `_run_search`, pass a callback that captures `my_generation`. Apply the same lock/check/send critical section to `send_bestmove`. Advance `search_generation` under the lock. Timer cleanup must only clear/cancel fields on the same `SearchJob`, never global state belonging to a replacement.

    Update the module docstring to state the actual per-generation token and output-gating policy. Preserve daemon workers, bounded joins, copied Positions, and immediate reader-thread handling.
  </action>
  <acceptance_criteria>
    - "`ance/uci/loop.py` defines `SearchJob` and `active_job`; it has no reusable module-level `stop_flag` and no unconditional Event `.clear()`."
    - "Every `handle_go` allocates a fresh Event and any Timer targets that exact Event's `set` method."
    - "Both `send_info_depth` and `send_bestmove` occur only inside generation-locked current-generation branches."
    - "`handle_stop` does not increment generation and still produces exactly one current bestmove."
    - "The focused concurrency tests and `.venv/bin/python -m pytest -m 'not slow' -q` exit 0."
    - "Commit the production fix atomically as `fix(02-08): isolate worker cancellation and generation-gate UCI output (GREEN)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_uci_generation.py tests/test_go_bestmove.py tests/test_uci_info.py -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
</task>

</tasks>

<verification>
- Forced 0.01-second join timeout leaves the stale generation's unique token set and the replacement token unset.
- Delayed stale callbacks emit neither info nor bestmove after generation replacement.
- `stop` remains responsive and returns exactly one current-generation bestmove.
- Full fast suite passes; this plan does not run slow strength tests.
</verification>

<success_criteria>
- No live worker can resume because another search cleared its cancellation signal.
- UCI output is generation-consistent for both thinking lines and final moves.
- Reader-thread responsiveness and bounded preemption are preserved.
- RED and GREEN are separate atomic commits.
</success_criteria>

<threat_model>
| Threat ID | Category | Component | Severity | Mitigation |
|---|---|---|---|---|
| T-02-08a | Denial of Service | stale search worker | blocker | Unique never-cleared cancellation token per job and bounded join. |
| T-02-08b | Tampering | UCI stdout generation | blocker | One lock protects generation advance and check-plus-emission for info/bestmove. |
| T-02-08c | Repudiation | concurrency regressions | high | Event/barrier-controlled tests prove ordering without scheduler-dependent sleeps. |
| T-02-08d | Denial of Service | reader thread | medium | No unbounded join and no search execution on the reader thread. |
</threat_model>

## Artifacts this phase produces

- `SearchJob` dataclass — `ance/uci/loop.py`.
- `active_job: SearchJob | None` field — `ance/uci/loop.py`.
- `generation_lock` field — `ance/uci/loop.py`.
- `tests/test_uci_generation.py` — new concurrency regression module.
- No new CLI option or external dependency.

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-08-SUMMARY.md` after execution.
</output>
