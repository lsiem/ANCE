---
phase: 02-core-alpha-beta-search
plan: 12
type: execute
wave: 8
gap_closure: true
depends_on:
  - 02-10
files_modified:
  - ance/uci/loop.py
  - tests/test_uci_generation.py
  - tests/test_go_bestmove.py
autonomous: true
requirements:
  - UCI-06
must_haves:
  truths:
    - "Every `go` command yields exactly one generation-gated `bestmove` line even when the worker thread raises an unexpected exception during search, evaluation, or info callback."
    - "Worker failures are logged to stderr (via `ance.debug` when enabled, or explicit stderr write) without crashing the reader loop."
    - "Fallback bestmove policy is explicit: first legal root move when legal moves exist; `bestmove (none)` when the position has zero legal moves."
    - "Stale/superseded generations still drop output; only the current generation may emit the exception fallback."
  artifacts:
    - path: ance/uci/loop.py
      provides: exception-safe _run_search with generation-gated fallback bestmove
      contains: "except"
    - path: tests/test_uci_generation.py
      provides: raising-search worker regression
    - path: tests/test_go_bestmove.py
      provides: subprocess-level one-bestmove-on-failure contract (optional extension)
  key_links:
    - from: ance/uci/loop.py::_run_search
      to: send_bestmove
      via: except handler + generation_lock gate
      pattern: "send_bestmove"
    - from: exception handler
      to: Position.legal_moves
      via: deterministic fallback move selection
      pattern: "legal_moves"
---

<objective>
Close the UCI worker exception gap: unexpected failures in `_run_search` must not violate UCI-06's one-`bestmove`-per-`go` contract.

Purpose: Prevent GUIs from hanging indefinitely when search or evaluation raises; preserve the generation-gating model from Plan 02-08 on failure paths.
Output: Exception handler in `ance/uci/loop.py` with deterministic fallback policy and in-process regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md (Confirmed Gap 3)
@.planning/phases/02-core-alpha-beta-search/02-REVIEW.md (W4 — advisory source, confirmed by verification)
@ance/uci/loop.py
@tests/test_uci_generation.py
@tests/test_go_bestmove.py
@tests/conftest.py
</context>

<deferred_advisories>
Review-only findings **not** in scope (same rationale as Plan 02-11):

- Collector descendant cleanup and artifact identity — evidence harness concerns, not UCI-06.
- Random-opponent RNG lifecycle — Phase 3 statistical concern.
</deferred_advisories>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Raising worker emits no bestmove today (RED)</name>
  <files>tests/test_uci_generation.py</files>
  <read_first>ance/uci/loop.py, tests/test_uci_generation.py, tests/test_go_bestmove.py, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md</read_first>
  <behavior>
    - Patch `search_root` to raise `RuntimeError("probe")` synchronously.
    - Run `_run_search` (or `handle_go` + thread join) for a startpos with legal moves.
    - Assert exactly one generation-gated `send_bestmove` call with a legal UCI move (not empty list).
    - Assert the exception is logged (capture `debug.log` or stderr monkeypatch).
    - Assert superseded generation: if `search_generation` advances before fallback emission, stale worker must emit nothing.
  </behavior>
  <acceptance_criteria>
    - New test `test_worker_exception_emits_generation_gated_fallback_bestmove` (or equivalent) in `tests/test_uci_generation.py`.
    - Test uses synchronization (Events/barriers), not scheduler-dependent sleeps, and completes in under 2 seconds.
    - `.venv/bin/python -m pytest tests/test_uci_generation.py -q` exits non-zero against current `loop.py` (zero bestmove captured on raise).
    - Commit the test-only RED state atomically as `test(02-12): expose missing worker exception fallback (RED)`.
  </acceptance_criteria>
  <action>
    Extend `tests/test_uci_generation.py` with a raising-search regression mirroring verification probe 3. Patch `send_bestmove` to append records; patch `search_root` to raise immediately. Optionally add a companion test for zero-legal-move position asserting fallback `(none)`. Include a stale-generation control: bump `search_generation` before the handler would emit and assert no record.
  </action>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_uci_generation.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED test proves raising worker currently emits zero bestmove lines</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Catch worker failures and emit one fallback bestmove (GREEN)</name>
  <files>ance/uci/loop.py</files>
  <read_first>ance/uci/loop.py, tests/test_uci_generation.py, ance/debug.py, ance/board/position.py</read_first>
  <acceptance_criteria>
    - `_run_search` wraps `search_root` (and result handling) in `try`/`except Exception` (do not catch `SearchAborted` if it propagates — treat only unexpected failures).
    - On exception: log via `debug.log(f"ERROR: search worker failed: {exc}")` or equivalent stderr-visible message; set `move = None` then compute fallback from the **copied** `pos` passed to the worker.
    - Fallback policy: if `pos.has_no_legal_moves()` → `move = None` (`bestmove (none)`); else `move = pos.legal_moves()[0]` (first legal move, deterministic tie-break per D-10 spirit).
    - `finally` block retains timer/job cleanup from Plan 02-08.
    - After exception or normal completion, `send_bestmove` runs inside existing `generation_lock` / current-generation check — exactly one line per worker invocation.
    - Normal success path unchanged: search result's `best_move` still used when no exception.
    - `.venv/bin/python -m pytest tests/test_uci_generation.py tests/test_go_bestmove.py -q` exits 0.
    - `.venv/bin/python -m pytest -m "not slow" -q` exits 0.
    - Commit the production fix atomically as `fix(02-12): generation-gated fallback bestmove on worker failure (GREEN)`.
  </acceptance_criteria>
  <action>
    Refactor `_run_search` to initialize `move = None` before `try`, catch unexpected exceptions from `search_root` and the info callback chain, log the failure, and derive fallback from the worker's `pos` copy. Preserve generation-gated emission in the existing lock block after `finally` timer cleanup. Update module docstring to document the exception fallback policy alongside the per-generation output rules. Do not swallow exceptions silently — always log before fallback.
  </action>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_uci_generation.py tests/test_go_bestmove.py -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>UCI-06 one-bestmove-per-go contract holds on worker failure; verification probe emits exactly one legal fallback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker→stdout | GUI depends on exactly one bestmove per go |
| worker→stderr | Failures must be diagnosable without breaking the reader loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-02-12a | Denial of Service | GUI hang on worker crash | blocker | Catch-all exception handler + mandatory fallback bestmove |
| T-02-12b | Tampering | stale fallback after preemption | blocker | Same generation_lock gate as normal bestmove path |
| T-02-12c | Repudiation | silent worker failure | high | stderr/debug log on every caught exception |
| T-02-12d | Tampering | illegal fallback move | medium | Fallback chosen only from `pos.legal_moves()` |
</threat_model>

<verification>
- Verification probe: raising `search_root` yields exactly one generation-gated legal `bestmove`, not zero lines and not an unhandled `RuntimeError` to the test harness.
- Stale-generation control: superseded worker emits no fallback after generation bump.
- Full fast suite passes; no regression in Plan 02-08 concurrency tests.
</verification>

<success_criteria>
- UCI-06 satisfied on the worker failure path: every `go` completes with exactly one `bestmove`.
- Reader thread remains alive and responsive after worker exceptions.
- RED and GREEN are separate atomic commits.
</success_criteria>

## Artifacts this phase produces

- Exception-safe `_run_search` with documented fallback policy — `ance/uci/loop.py`
- `_fallback_root_move(pos: Position) -> chess.Move | None` helper (inline or module-local) — `ance/uci/loop.py`
- `test_worker_exception_emits_generation_gated_fallback_bestmove` — `tests/test_uci_generation.py`
- Optional `test_worker_exception_fallback_none_on_mate` — `tests/test_uci_generation.py`

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-12-SUMMARY.md` when done.
</output>
