---
phase: 02-core-alpha-beta-search
plan: 11
type: execute
wave: 8
gap_closure: true
depends_on:
  - 02-10
files_modified:
  - ance/search/negamax.py
  - tests/test_quiescence.py
  - tests/test_iterative_deepening.py
autonomous: true
requirements:
  - SRCH-04
  - SRCH-07
must_haves:
  truths:
    - "Every quiescence node applies the same draw detection as main negamax: game-history repetition, search-path twofold, 50-move rule, and insufficient material all score 0 before tactical expansion."
    - "Quiescence search maintains balanced path_keys push/pop across every recursive descent so capture/check sequences cannot bypass twofold-in-search detection."
    - "Terminal resolution (zero legal moves → mate or stalemate; in-check → evasions only) runs before any quiet-node MAX_QDEPTH static-evaluation return."
    - "A checkmated position reached at MAX_QDEPTH scores mate, not clamped evaluator centipawns."
  artifacts:
    - path: ance/search/negamax.py
      provides: draw-aware, terminal-first quiescence_search
      contains: "_is_draw_position"
    - path: tests/test_quiescence.py
      provides: capped checkmate and in-check-at-cap regressions
    - path: tests/test_iterative_deepening.py
      provides: qsearch-descendant draw regressions
  key_links:
    - from: quiescence_search entry
      to: _is_draw_position
      via: draw cut before expansion
      pattern: "_is_draw_position"
    - from: quiescence_search recursive calls
      to: ctx.path_keys
      via: balanced append/pop around each child
      pattern: "path_keys"
    - from: quiescence_search MAX_QDEPTH branch
      to: pos.is_check / legal_moves
      via: terminal resolution precedes static eval
      pattern: "is_check"
---

<objective>
Close the two confirmed quiescence correctness gaps from Phase 2 verification: qsearch descendants must inherit draw detection (SRCH-07), and the MAX_QDEPTH cap must not static-evaluate unresolved check/terminal positions (SRCH-04).

Purpose: Restore parity between main negamax and quiescence for draw/terminal contracts without changing main-search move ordering or ID behavior.
Output: Corrected `quiescence_search` in `negamax.py` with focused regressions proving historical/path draws and capped checkmate handling.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md
@.planning/phases/02-core-alpha-beta-search/02-CONTEXT.md (D-05 through D-08)
@.planning/phases/02-core-alpha-beta-search/02-RESEARCH.md
@ance/search/negamax.py
@tests/test_quiescence.py
@tests/test_iterative_deepening.py
</context>

<deferred_advisories>
The following review-only findings are explicitly **not** in scope for this plan (verification did not count them as Phase 2 requirement gaps):

- **Collector descendant cleanup** (`phase2_deterministic_evidence.py`) — tooling robustness; defer to a future evidence-harness plan.
- **Collector artifact reuse without source identity hash** — tooling integrity; defer to Phase 3 or a dedicated harness plan.
- **Random-opponent RNG lifecycle** — Phase 3 statistical concern; Plan 02-10 already defers statistical gauntlet claims.
</deferred_advisories>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing qsearch draw and capped-terminal regressions (RED)</name>
  <files>tests/test_quiescence.py, tests/test_iterative_deepening.py</files>
  <read_first>ance/search/negamax.py, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md (Confirmed Gaps 1 and 2), tests/test_iterative_deepening.py</read_first>
  <behavior>
    - Test A (SRCH-07 / gap 1): Direct `quiescence_search` call with `game_history_keys` containing the current position's zobrist key and a `_ConstantEval(123)` stub must return `0`, not `123`.
    - Test B (SRCH-07 / gap 1): A qsearch continuation that reaches a search-path repetition (capture sequence returning to a key already on `path_keys`) must score `0`, not a positive tactical eval.
    - Test C (SRCH-04 / gap 2): Checkmated FEN (fool's mate) probed through `quiescence_search` with `qdepth=MAX_QDEPTH` must return a mate score (`<= -(MATE - ply_margin)`), not evaluator centipawns `123`.
    - Test D (SRCH-04 / gap 2): In-check position with legal evasions at `qdepth=MAX_QDEPTH` must not return stand-pat static eval; it must find an evasion or mate when no evasions exist.
  </behavior>
  <acceptance_criteria>
    - At least four new test functions across `tests/test_quiescence.py` and/or `tests/test_iterative_deepening.py` target the verification probes directly.
    - Tests use `_ConstantEval` returning `123` where the verification probe expects draw/mate discrimination from static eval.
    - `.venv/bin/python -m pytest tests/test_quiescence.py tests/test_iterative_deepening.py -q` exits non-zero against current `negamax.py`.
    - Commit the test-only RED state atomically as `test(02-11): expose qsearch draw and capped-terminal gaps (RED)`.
  </acceptance_criteria>
  <action>
    Add pinned-FEN and direct-call regressions mirroring the verification probes in `02-VERIFICATION.md`. For Test B, construct a minimal capture/check line inside qsearch that revisits a path key (document FEN and move sequence in the test docstring). For Test C, call `quiescence_search(..., qdepth=MAX_QDEPTH)` on the fool's mate FEN with `_ConstantEval(123)`. Reuse existing helpers (`_make_ctx`, `_ConstantEval`) where possible.
  </action>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_quiescence.py tests/test_iterative_deepening.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED tests reproduce verification gaps: historical draw returns 123; capped checkmate returns 123</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Draw-aware, terminal-first quiescence_search (GREEN)</name>
  <files>ance/search/negamax.py</files>
  <read_first>ance/search/negamax.py, tests/test_quiescence.py, tests/test_iterative_deepening.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md (D-05, D-06)</read_first>
  <acceptance_criteria>
    - `quiescence_search` calls `_is_draw_position` at entry (before expansion) and returns `0` on draw.
    - `quiescence_search` uses balanced `path_keys.append` / `path_keys.pop` (try/finally) around its body, pushing before child recursion and popping on unwind — same contract as `negamax`.
    - `negamax` depth-0 handoff does not double-push the current key: refactor so `quiescence_search` owns path-key push/pop for the qsearch subtree (e.g., call qsearch before `negamax`'s path append, or pop parent push on qsearch entry and re-push consistently). Document the chosen handoff in a one-line comment.
    - Zero legal moves: checkmate scores `-(MATE - ctx.ply)`; stalemate scores `0` — evaluated before any quiet `MAX_QDEPTH` static return.
    - `qdepth >= MAX_QDEPTH` static evaluation applies **only** to quiet (not-in-check) nodes with legal moves; in-check nodes always search evasions (incrementing `qdepth`) regardless of cap.
    - Existing qsearch behavior preserved: stand-pat + delta pruning on quiet nodes; MVV-LVA captures+queen promos only; no main-search ordering added.
    - `.venv/bin/python -m pytest tests/test_quiescence.py tests/test_iterative_deepening.py -q` exits 0.
    - `.venv/bin/python -m pytest -m "not slow" -q` exits 0.
    - Commit the production fix atomically as `fix(02-11): qsearch draw detection and terminal-before-cap (GREEN)`.
  </acceptance_criteria>
  <action>
    Restructure `quiescence_search` to mirror `negamax`'s draw/terminal/path-key discipline:

    1. Poll stop, then `_is_draw_position` → return 0.
    2. Append current zobrist key; `try`/`finally` pop.
    3. Resolve terminals: no legal moves → mate or stalemate per D-05.
    4. If in check → search all evasions (no stand-pat), recurse with `qdepth + 1`.
    5. Else if `qdepth >= MAX_QDEPTH` → return `_clamped_eval` (quiet cap only).
    6. Else quiet branch: stand-pat, delta-pruned captures, recurse with balanced path keys on each child.

    Adjust the `negamax` depth-0 call site so path-key semantics remain consistent (no false twofold on qsearch entry). Do not change `MAX_QDEPTH`, `DELTA_MARGIN`, or main-search move ordering.
  </action>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_quiescence.py tests/test_iterative_deepening.py tests/test_alpha_beta.py -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q</automated>
  </verify>
  <done>SRCH-04 and SRCH-07 qsearch gaps closed; verification probes return 0 and mate respectively</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qsearch→draw detection | Qsearch must not claim wins in drawn positions reachable via captures |
| qsearch→terminal scoring | Mate/stalemate must not be replaced by static eval at depth cap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation |
|-----------|----------|-----------|----------|------------|
| T-02-11a | Tampering | qsearch draw bypass | blocker | `_is_draw_position` + balanced `path_keys` at every qsearch node |
| T-02-11b | Tampering | capped static eval in check | blocker | Terminal/in-check resolution before quiet `MAX_QDEPTH` return |
| T-02-11c | Denial of Service | qsearch explosion | medium | Preserve existing `MAX_QDEPTH`, delta pruning, and stop polling |
| T-02-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
- Verification probe A: qsearch with historical key returns `0`, not `123`.
- Verification probe B: capped checkmate returns mate score, not `123`.
- Full fast suite (`pytest -m "not slow" -q`) passes.
- No regression in existing quiescence horizon and in-check tests from Plan 02-02.
</verification>

<success_criteria>
- Quiescence descendants inherit the same draw correctness as main negamax (SRCH-07).
- Quiescence depth cap cannot mask checkmate or force static eval while in check (SRCH-04).
- RED and GREEN are separate atomic commits.
</success_criteria>

## Artifacts this phase produces

- Draw-aware `quiescence_search` with balanced `path_keys` — `ance/search/negamax.py`
- Negamax depth-0 / qsearch path-key handoff (documented inline) — `ance/search/negamax.py`
- `test_qsearch_game_history_repetition_scores_draw` (or equivalent) — `tests/test_quiescence.py` or `tests/test_iterative_deepening.py`
- `test_qsearch_path_repetition_scores_draw` (or equivalent) — `tests/test_iterative_deepening.py`
- `test_qsearch_at_cap_checkmate_scores_mate` — `tests/test_quiescence.py`
- `test_qsearch_at_cap_in_check_searches_evasions` — `tests/test_quiescence.py`

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-11-SUMMARY.md` when done.
</output>
