---
phase: 02-core-alpha-beta-search
plan: 10
type: execute
wave: 8
depends_on:
  - 02-07
  - 02-08
  - 02-09
files_modified:
  - ance/tools/random_mover_gauntlet.py
  - ance/tools/depth_vs_depth_match.py
  - ance/tools/phase2_strength_evidence.py
  - tests/test_random_mover_gauntlet.py
  - tests/test_depth_vs_depth.py
  - tests/test_phase2_strength_evidence.py
  - .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json
autonomous: true
gap_closure: true
requirements:
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-07
  - UCI-11
must_haves:
  truths:
    - "Fast deterministic tests validate report classification, minimum sample enforcement, seed/deadline forwarding, per-game atomic checkpointing, resume skips, and budget/interruption failure without replaying any long match."
    - "The corrected depth-vs-depth proof runs exactly 30 reproducible opening-varied games at depth 2 versus depth 3, max 80 halfmoves, and passes only when deeper score_rate >= 0.50 (D-14 original sample bound satisfied)."
    - "The depth-4 random-mover replacement runs exactly 30 reproducible games at max 80 halfmoves, passes only with losses == 0, and reports the one-sided 95% upper loss-rate bound."
    - "The evidence JSON explicitly records why D-01's original 100 depth-4 games within 600 seconds is infeasible (3 games measured at 1566 seconds projects to about 14.5 hours) and identifies the 30-game/80-halfmove/7200-second executable replacement rather than silently weakening the decision."
    - "One shared monotonic deadline is derived from the 7200-second budget, checked between games and plies, and passed into every depth-match and gauntlet search together with one shared cancellation Event."
    - "The evidence state is atomically checkpointed after every game and suite; rerunning the same command resumes compatible state and skips every completed game and completed suite."
    - "Deadline expiry, SIGINT/SIGTERM, KeyboardInterrupt, and the outer process hard timeout leave status failed with completion incomplete and return non-zero; no partial run can be classified as passed."
    - "The combined slow evidence command is supervised by a real 7260-second process timeout with a 30-second graceful-cancellation window and a forced-kill checkpoint fallback."
  artifacts:
    - path: ance/tools/phase2_strength_evidence.py
      provides: "resumable per-game checkpointing CLI, shared monotonic budget/cancellation control, and JSON classification"
      contains: "def main"
    - path: .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json
      provides: "machine-readable parameters, timing, outcomes, confidence bound, decision replacement, and pass/fail status"
    - path: tests/test_phase2_strength_evidence.py
      provides: "fast mocked classification, deadline, interruption, checkpoint, and resume contract tests"
    - path: ance/tools/random_mover_gauntlet.py
      provides: "seed/max-halfmove controls plus deadline, cancellation, resume offset, and per-game callback forwarding"
    - path: ance/tools/depth_vs_depth_match.py
      provides: "deadline, cancellation, resume offset, and per-game callback forwarding for corrected depth matches"
  key_links:
    - from: ance/tools/phase2_strength_evidence.py
      to: ance/tools/depth_vs_depth_match.py::run_depth_match
      via: "30-game depth 2 vs 3 opening-varied proof"
      pattern: "run_depth_match"
    - from: ance/tools/phase2_strength_evidence.py
      to: ance/tools/random_mover_gauntlet.py::run_gauntlet
      via: "30-game depth-4 zero-loss replacement proof"
      pattern: "run_gauntlet"
    - from: ance/tools/phase2_strength_evidence.py
      to: ance/search/negamax.py::search_root
      via: "one shared absolute monotonic deadline and cancellation Event forwarded through both game harnesses into every search"
      pattern: "deadline=deadline"
    - from: ance/tools/phase2_strength_evidence.py
      to: 02-STRENGTH-EVIDENCE.json
      via: "per-game/per-suite callback writes through fsynced temporary file and atomic replace"
      pattern: "os.replace"
---

<objective>
Replace the invalid undersized/over-budget strength claims with one reproducible, realistically budgeted evidence run after all harness and search defects are fixed.

Purpose: Satisfy D-14's original 30–50 game requirement, preserve D-01's depth-4 zero-loss intent through a documented executable replacement, and prevent another expensive run from being accepted when its sample, budget, or harness semantics are wrong.
Output: A fast-tested evidence CLI and one machine-readable 30+30 game result under an explicit two-hour budget.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify bounded evidence and report classification (RED)</name>
  <files>tests/test_random_mover_gauntlet.py, tests/test_depth_vs_depth.py, tests/test_phase2_strength_evidence.py</files>
  <read_first>ance/tools/random_mover_gauntlet.py, ance/tools/depth_vs_depth_match.py, tests/test_random_mover_gauntlet.py, tests/test_depth_vs_depth.py, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md, .planning/phases/02-core-alpha-beta-search/02-UAT.md</read_first>
  <action>
    Extend the random-gauntlet and depth-match fast tests to require their suite runners to forward seeds `BASE..BASE+n_games-1`, the exact halfmove limit, the identical absolute `deadline` float, and the identical shared `stop_event` object to every game while retaining alternating colors. At game level, spy on every `search_root` call and assert it receives that same deadline/event. Use a fake monotonic clock to prove both harnesses refuse to begin another game/ply after expiry and check expiry again immediately after a search returns, so an expired search result is never pushed.

    Create `tests/test_phase2_strength_evidence.py` with all expensive functions monkeypatched. Test the planned CLI/report builder for: exact forwarding of 30 depth games, 30 random games, seed, depths 2/3/4, max_halfmoves 80, one shared deadline equal to `start_monotonic + remaining_budget`, and one shared cancellation Event; refusal of either sample below 30; pass classification only for depth score rate >=0.50 and random losses==0; failure on either threshold; failure when elapsed seconds reaches/exceeds budget; one-sided 95% zero-loss upper bound `1 - 0.05**(1/30)`; and JSON containing `decision_replacement.original`, `.projection`, `.replacement`, parameters, raw per-game outcomes, elapsed seconds, suite state, completion, and final status.

    Add callback/checkpoint tests proving a sibling temporary file is flushed/fsynced and atomically replaced after every completed game and after each suite transition. Checkpoint state must contain each suite's `status` (`pending|running|completed`), `completed_games`, ordered per-game records keyed by game index/seed, aggregate counters, consumed elapsed seconds, top-level `status`, and `completion`.

    Add resume tests using a prebuilt compatible checkpoint: one with depth games 0–9 complete must invoke only 10–29; one with the depth suite complete and random games 0–6 complete must never call the depth harness and must invoke only random games 7–29; a fully completed failed/passed suite must be reclassified/no-op without replay. Assert parameter/schema mismatch fails before play unless `--restart` is explicit, and duplicate/out-of-order game callbacks are rejected rather than double-counted.

    Add deterministic expiry/interruption tests for a timeout during a game, `KeyboardInterrupt`, and the SIGINT/SIGTERM handler path. Each must set the shared event, atomically preserve only completed games, write `status="failed"` and `completion="incomplete"` with an explicit reason, and exit non-zero. Test `--mark-interrupted REASON` performs the same atomic transition without invoking a harness; this is the supervisor's forced-kill fallback.
  </action>
  <acceptance_criteria>
    - "Fast tests make no real search call and complete in under 2 seconds."
    - "Tests reject `depth_games < 30` or `random_games < 30` before invoking either harness."
    - "Tests assert every mocked search sees the same absolute monotonic deadline and Event, and no post-deadline move/game begins."
    - "Tests prove checkpoints occur after each game and suite and that resume never invokes completed game indices or completed suites."
    - "Tests assert the exact zero-loss upper bound within floating tolerance and require non-zero exit plus failed/incomplete persisted state for timeout or interruption."
    - "`.venv/bin/python -m pytest tests/test_random_mover_gauntlet.py tests/test_depth_vs_depth.py tests/test_phase2_strength_evidence.py -m 'not slow' -q` exits non-zero before implementation."
    - "Commit the test-only RED state atomically as `test(02-10): specify bounded strength evidence contract (RED)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_random_mover_gauntlet.py tests/test_depth_vs_depth.py tests/test_phase2_strength_evidence.py -m "not slow" -q; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reproducible evidence CLI (GREEN)</name>
  <files>ance/tools/random_mover_gauntlet.py, ance/tools/depth_vs_depth_match.py, ance/tools/phase2_strength_evidence.py</files>
  <read_first>ance/tools/random_mover_gauntlet.py, ance/tools/depth_vs_depth_match.py, ance/search/negamax.py, tests/test_random_mover_gauntlet.py, tests/test_depth_vs_depth.py, tests/test_phase2_strength_evidence.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md</read_first>
  <action>
    Extend `play_game` and `play_depth_match_game` with keyword-only `deadline: float | None = None` and `stop_event: threading.Event | None = None`. Use the supplied shared Event (allocate only when omitted for backward compatibility). Before each ply, immediately after each `search_root`, and before pushing the returned move, raise a dedicated harness cancellation/timeout exception when the event is set or `time.monotonic() >= deadline`. Pass `stop_flag=stop_event` and `deadline=deadline` into every `search_root`; this combines Plan 02-07's in-tree polling with game-boundary checks.

    Extend `run_gauntlet` and `run_depth_match` with backward-compatible keyword controls: `seed: int = 0`, `max_halfmoves: int = 300`, `start_game: int = 0`, prior ordered `game_records`, shared `deadline`, shared `stop_event`, and `on_game_complete(index, record, aggregate)`. Iterate only `range(start_game, n_games)`, use global index `i` for color and `seed+i`, check cancellation/deadline before each game, tally exactly once, and invoke the callback only after a full game is classified. Validate prior records are contiguous indices `[0,start_game)`. Reject non-positive counts and invalid offsets.

    Create `ance/tools/phase2_strength_evidence.py` with an argparse entry point. Expose options `--output`, `--seed` (default 20260710), `--depth-games` (default 30), `--random-games` (default 30), `--max-halfmoves` (default 80), `--budget-seconds` (default 7200), `--restart`, and supervisor recovery option `--mark-interrupted REASON`. Fixed evidence depths are shallow 2, deep 3, random-gauntlet 4; print and record them before starting.

    On a fresh run, write an initial atomic checkpoint with `status="running"`, `completion="incomplete"`, both suites pending, empty game records, zero consumed elapsed, and current-attempt monotonic/UTC start metadata. On default rerun, load and validate schema plus all evidence parameters; `--restart` is the only way to discard incompatible or completed state. Preserve completed records/suites, derive `remaining_budget = budget_seconds - checkpoint.elapsed_seconds.total`, then establish exactly one new-process absolute deadline as `time.monotonic() + remaining_budget` and one shared Event. A passed checkpoint exits 0 without play; a complete failed checkpoint exits 1 without play.

    Resume the first incomplete suite by passing its `completed_games`, records, shared deadline/Event, and callback to its suite runner; skip any suite marked completed. The callback must append exactly the next global game index, update aggregate/elapsed/remaining values, and atomically replace the JSON after every game. Atomically checkpoint a suite as completed before starting the next. The shared deadline is never reset between games or suites in one process. Across resumes, persisted consumed monotonic elapsed reduces the new remaining budget, so interruption cannot restore spent time.

    Install SIGINT and SIGTERM handlers that set the shared Event and raise a controlled interruption handled on the main thread. Catch harness deadline/cancellation, `KeyboardInterrupt`, and controlled signals around both suites; in every path add current-attempt monotonic elapsed, preserve completed games, atomically write `status="failed"`, `completion="incomplete"`, and an explicit reason, then exit non-zero. `--mark-interrupted` must atomically make that transition without playing, preserve all completed records, and charge elapsed time since the checkpointed attempt start (use the monotonic delta when valid and a conservative UTC delta fallback) so forced kill cannot restore budget. Use `try/finally` so no timeout/interruption path reaches success classification.

    Final status is `passed` only when both suite states are completed, each completed count equals its requested sample, depth `score_rate >=0.50`, random `losses ==0`, and total consumed elapsed is strictly within the budget. Otherwise status is `failed`, with `completion="complete"` only when both samples finished, explicit reasons, and exit 1. Calculate the one-sided 95% zero-loss upper rate as `1 - 0.05 ** (1 / random_games)` when losses are zero.

    Write JSON via `<output>.tmp` plus flush/fsync and `os.replace`. Include schema version, UTC timestamp, git commit when available, parameters, suite states and ordered per-game records, aggregate raw outcomes, completed-game counts, per-suite and total consumed elapsed seconds, remaining budget, confidence bound, status/completion/reasons, and `decision_replacement`: original=`100 depth-4 games, max 600s`; measured=`3 games in 1566s`; projection=`about 52200s/14.5h for 100`; replacement=`30 games, depth 4, max 80 halfmoves, combined 7200s budget, losses==0`.
  </action>
  <acceptance_criteria>
    - "`python -m ance.tools.phase2_strength_evidence --help` lists the six evidence options plus `--restart` and `--mark-interrupted`."
    - "Both harnesses forward the same Event/deadline through every game into every `search_root`, and remain backward-compatible for existing positional callers."
    - "The JSON writer uses temporary-file flush/fsync plus `os.replace`."
    - "Every completed game and suite transition is atomically persisted; compatible resume starts at the first missing game and never invokes a completed suite."
    - "The runner exits 1 with persisted failed/incomplete state for insufficient samples, deadline expiry, cancellation, SIGINT/SIGTERM, or KeyboardInterrupt; no such run is labeled passed."
    - "A suite-complete threshold failure is failed/complete and reruns do not replay it; only explicit `--restart` starts over."
    - "Focused fast tests and the full fast suite exit 0."
    - "Commit the implementation atomically as `feat(02-10): add budgeted Phase 2 strength evidence runner (GREEN)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_random_mover_gauntlet.py tests/test_depth_vs_depth.py tests/test_phase2_strength_evidence.py -m "not slow" -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q &amp;&amp; .venv/bin/python -m ance.tools.phase2_strength_evidence --help</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Execute the corrected 30+30 evidence run once</name>
  <files>.planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json</files>
  <read_first>ance/tools/phase2_strength_evidence.py, ance/tools/depth_vs_depth_match.py, ance/tools/random_mover_gauntlet.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md</read_first>
  <action>
    Run the evidence CLI after Tasks 1–2 and Plans 02-07/08/09 are green, under an external process supervisor: launch the exact CLI below with `subprocess.Popen`; wait at most 7260 seconds; on timeout send SIGINT, wait up to 30 seconds for its atomic failed/incomplete checkpoint, then SIGKILL if necessary and invoke the same module with `--output ... --mark-interrupted "process hard timeout after 7260s"` to atomically mark the last checkpoint failed/incomplete. The supervisor must exit 124 after either timeout path. This external bound is deliberately above the internal 7200-second shared budget but is a real hard ceiling if a search/evaluator stops cooperating.

    The supervised child command is:
    `.venv/bin/python -m ance.tools.phase2_strength_evidence --output .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json --seed 20260710 --depth-games 30 --random-games 30 --max-halfmoves 80 --budget-seconds 7200`.

    Do not separately run either slow pytest or either harness. If the child exits non-zero, retain the valid JSON and stop the plan as failed rather than changing thresholds, samples, depths, or budget. If the operator reruns the same supervised command after an ordinary interruption, default resume skips all checkpointed games and suites; never pass `--restart` merely to continue.
  </action>
  <acceptance_criteria>
    - "`02-STRENGTH-EVIDENCE.json` parses as JSON and records `depth_games=30`, `random_games=30`, `max_halfmoves=80`, `budget_seconds=7200`, and seed 20260710."
    - "The report records both suite states as completed, each with contiguous unique game indices 0–29; restart/resume history contains no duplicate game."
    - "The depth result records exactly 30 games and `score_rate >= 0.50`."
    - "The random result records exactly 30 games, depth 4, and `losses == 0`."
    - "Total consumed elapsed is <=7200 seconds, completion is `complete`, and status is `passed`; otherwise execution stops with the artifact marked failed (incomplete for timeout/interruption) and no acceptance claim."
    - "The execution is process-supervised at 7260 seconds with SIGINT then SIGKILL fallback; no single harness call can run indefinitely."
    - "The report contains the D-01 original measurement/projection/replacement rationale and the 95% upper loss-rate bound."
    - "Commit only the completed evidence artifact as `test(02-10): record corrected Phase 2 strength evidence`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python - &lt;&lt;'PY'
import signal
import subprocess
import sys

output = ".planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json"
cmd = [
    ".venv/bin/python", "-m", "ance.tools.phase2_strength_evidence",
    "--output", output, "--seed", "20260710", "--depth-games", "30",
    "--random-games", "30", "--max-halfmoves", "80",
    "--budget-seconds", "7200",
]
child = subprocess.Popen(cmd)
try:
    code = child.wait(timeout=7260)
except subprocess.TimeoutExpired:
    child.send_signal(signal.SIGINT)
    try:
        child.wait(timeout=30)
    except subprocess.TimeoutExpired:
        child.kill()
        child.wait()
        subprocess.run(
            cmd[:3] + ["--output", output, "--mark-interrupted",
                       "process hard timeout after 7260s"],
            check=False,
        )
    raise SystemExit(124)
raise SystemExit(code)
PY
rc=$?; test "$rc" -eq 0 || exit "$rc"
.venv/bin/python -c "import json; p='.planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json'; d=json.load(open(p)); assert d['status']=='passed' and d['completion']=='complete'; assert d['parameters']['depth_games']==30 and d['parameters']['random_games']==30; assert d['suites']['depth_match']['status']=='completed' and d['suites']['depth_match']['completed_games']==30; assert d['suites']['random_gauntlet']['status']=='completed' and d['suites']['random_gauntlet']['completed_games']==30; assert [g['index'] for g in d['suites']['depth_match']['games']]==list(range(30)); assert [g['index'] for g in d['suites']['random_gauntlet']['games']]==list(range(30)); assert d['suites']['depth_match']['aggregate']['score_rate'] &gt;= .5; assert d['suites']['random_gauntlet']['aggregate']['losses']==0; assert d['elapsed_seconds']['total'] &lt;= 7200"</automated>
  </verify>
</task>

</tasks>

<verification>
- Fast mocked tests prove classification, one shared deadline/Event, per-game atomic persistence, interruption failure, and exact resume skips without costly play.
- Full fast suite passes after all three Wave 7 fixes.
- One process-supervised resumable command produces 30 unique depth-match and 30 unique random-gauntlet games without replaying completed work.
- JSON parameters, suite/game completion, raw outcomes, consumed elapsed times, confidence bound, and replacement rationale are independently inspectable.
</verification>

<success_criteria>
- D-14 is supported by the originally required minimum of 30 corrected, varied games.
- D-01's impossible 100-games/10-minute combination is not falsely claimed; a reproducible 30-game bounded replacement preserves depth 4 and the zero-loss invariant with a stated confidence bound.
- Evidence is accepted only when both suites are complete inside one consumed 7200-second budget; timeout/interruption is persisted as failed/incomplete.
- Every game and suite is checkpointed atomically, and compatible resume skips completed games/suites.
- The shared in-search deadline and 7260-second outer process timeout bound both cooperative and non-cooperative overruns.
- RED, GREEN, and evidence are separate atomic commits; no redundant multi-hour replay occurs.
</success_criteria>

<threat_model>
| Threat ID | Category | Component | Severity | Mitigation |
|---|---|---|---|---|
| T-02-10a | Repudiation | strength result | high | Record parameters, seed, raw counters, timing, commit, confidence bound, and rationale in JSON. |
| T-02-10b | Tampering | weakened acceptance | high | Enforce minimum samples and fixed thresholds in code before harness invocation. |
| T-02-10c | Denial of Service | slow evidence | high | Shared monotonic deadline reaches every search/game, plus a 7260-second process supervisor with graceful and forced cancellation. |
| T-02-10d | Tampering | partial report | medium | Per-game/per-suite fsync temporary JSON and atomic replace; interruption is failed/incomplete and malformed partial output cannot be accepted. |
| T-02-10e | Repudiation | resumed evidence | high | Ordered indexed game records, compatible-parameter validation, and completed-suite skips prevent duplicate/replayed samples. |
</threat_model>

## Artifacts this phase produces

- `ance/tools/phase2_strength_evidence.py` — new resumable, deadline-aware evidence-runner module.
- `main(argv=None) -> int` — evidence CLI entry point.
- CLI options: `--output`, `--seed`, `--depth-games`, `--random-games`, `--max-halfmoves`, `--budget-seconds`, `--restart`, `--mark-interrupted`.
- Deadline/cancellation/resume callback extensions on both depth-match and random-gauntlet harnesses.
- `tests/test_phase2_strength_evidence.py` — new fast evidence, checkpoint, interruption, and resume contract test module.
- `.planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json` — new machine-readable slow evidence artifact.
- New `seed` and `max_halfmoves` options on `run_gauntlet`.

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md` after execution.
</output>
