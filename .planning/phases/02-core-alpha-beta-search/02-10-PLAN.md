---
phase: 02-core-alpha-beta-search
plan: 10
type: execute
wave: 8
depends_on:
  - 02-07
  - 02-08
  - 02-09
files_modified:
  - ance/tools/random_mover_gauntlet.py
  - ance/tools/depth_vs_depth_match.py
  - ance/tools/phase2_strength_evidence.py
  - ance/tools/phase2_deterministic_evidence.py
  - tests/test_random_mover_gauntlet.py
  - tests/test_depth_vs_depth.py
  - tests/test_phase2_strength_evidence.py
  - tests/test_phase2_deterministic_evidence.py
  - .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json
  - .planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md
autonomous: true
gap_closure: true
resume:
  from_task: 3
  completed_immutable:
    - task: 1
      commit: ab996ce
    - task: 2
      commit: 99888cb
requirements:
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-07
  - UCI-11
must_haves:
  truths:
    - "The completed RED commit ab996ce and GREEN commit 99888cb remain intact: fast tests continue to prove result normalization, seed/deadline forwarding, per-game atomic checkpointing, compatible resume skips, and timeout/interruption failure without replaying a long match."
    - "The partial run is retained only as runtime calibration: 2 depth-match games consumed 1205.6582282920135 seconds, projecting the 30-game depth suite alone to 18084.8734243802 seconds (~5.02 hours), so neither it nor the unstarted random gauntlet is classified as strength evidence."
    - "Bounded deterministic evidence covers fixed mate, tactic, and horizon FENs at specified shallow and deeper search depths with exact expected best moves and exact expected scores."
    - "Relative-depth evidence means only that each requested iteration completes, preserves the tactical oracle at the deeper depth, and performs deterministic nondecreasing node work; it does not claim Elo, win rate, or statistical superiority."
    - "Exact synthetic telemetry checks require cumulative nodes [10, 20, 30], NPS [10, 6, 5], completed depths [1, 2, 3], and final nodes 30; deadline-abort checks require retention of the last completed iteration."
    - "The evidence JSON records the runtime infeasibility, deterministic case inputs/expected/observed values, focused harness-contract tests, full fast-suite result, elapsed times, git commit, and explicit Phase 3 deferral."
    - "The committed deterministic evidence supervisor uses one absolute monotonic hard deadline 870 seconds after supervisor start, reserves the final 60 seconds for process-group termination, atomic failure reporting, and validation, and bounds every wait by the remaining absolute time."
    - "Long statistical depth-vs-depth Elo and random-mover gauntlet evidence is explicitly deferred to Phase 3's optimized search and cutechess harness and is not marked passed in Phase 2."
  artifacts:
    - path: ance/tools/phase2_strength_evidence.py
      provides: "completed resumable/checkpoint runner contract retained for harness semantics and interruption testing; its infeasible 30+30 execution is not resumed"
      contains: "def main"
    - path: .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json
      provides: "machine-readable runtime calibration, deterministic tactical/depth evidence, contract-test results, deferral, and pass/fail status"
    - path: tests/test_phase2_strength_evidence.py
      provides: "fast mocked classification, deadline, interruption, checkpoint, and resume contract tests"
    - path: ance/tools/phase2_deterministic_evidence.py
      provides: "committed one-shot collector and absolute-deadline process-group supervisor"
      contains: "def main"
    - path: tests/test_phase2_deterministic_evidence.py
      provides: "six fast supervisor, timeout, interruption, atomic-output, and rerun-safety tests"
    - path: .planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md
      provides: "terminal execution record with commit refs, commands/results, deferral, and artifact status"
    - path: ance/tools/random_mover_gauntlet.py
      provides: "seed/max-halfmove controls plus deadline, cancellation, resume offset, and per-game callback forwarding"
    - path: ance/tools/depth_vs_depth_match.py
      provides: "deadline, cancellation, resume offset, and per-game callback forwarding for corrected depth matches"
  key_links:
    - from: deterministic fixed-FEN evidence
      to: ance/search/negamax.py::search_root
      via: "same tactical oracle executed at explicit shallow and deeper max_depth values with exact move/score assertions"
      pattern: "search_root"
    - from: tests/test_phase2_strength_evidence.py
      to: ance/tools/phase2_strength_evidence.py
      via: "checkpoint, resume, deadline, SIGINT/SIGTERM, KeyboardInterrupt, and forced-kill fallback semantics"
      pattern: "test_resume_skips_completed_games_and_suites"
    - from: deterministic evidence collector
      to: 02-STRENGTH-EVIDENCE.json
      via: "fsynced sibling temporary file and atomic replace after complete classification or bounded failure"
      pattern: "os.replace"
    - from: deterministic evidence collector
      to: 02-10-SUMMARY.md
      via: "terminal summary generated and atomically replaced from the same classified evidence state"
      pattern: "artifact_status"
---

<objective>
Replace the invalid undersized/over-budget strength claims with reproducible deterministic correctness and relative-depth evidence after all harness and search defects are fixed.

Purpose: Prove Phase 2 search and harness semantics honestly within this machine's practical runtime, preserve the measured infeasibility, and defer statistical strength measurement until Phase 3 provides optimized search and a cutechess harness.
Output: The completed RED/GREEN evidence-runner contracts plus one machine-readable deterministic evidence result under an exact 870-second hard wall.
</objective>

<execution_state>
Resume at Task 3 only. Task 1 is completed and immutable at `ab996ce`; Task 2 is completed and immutable at `99888cb`. Do not rerun either task's RED/GREEN workflow, do not recreate or amend either commit, and do not recommit their files as Task 3 work. Before Task 3 mutates anything, require both commits to be ancestors of HEAD and require the Task 1/2 tracked files to match HEAD. The existing uncommitted `02-STRENGTH-EVIDENCE.json` is input calibration state, not a completed task and not a resumable 30+30 result. Task 3 may replace it only through the committed collector's atomic state transition.
</execution_state>

<tasks>

<task type="auto" tdd="true" status="completed" immutable="true" commit="ab996ce">
  <name>Task 1 [COMPLETED — DO NOT RERUN/RECOMMIT]: Specify bounded evidence and report classification (RED)</name>
  <files>tests/test_random_mover_gauntlet.py, tests/test_depth_vs_depth.py, tests/test_phase2_strength_evidence.py</files>
  <read_first>ance/tools/random_mover_gauntlet.py, ance/tools/depth_vs_depth_match.py, tests/test_random_mover_gauntlet.py, tests/test_depth_vs_depth.py, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md, .planning/phases/02-core-alpha-beta-search/02-UAT.md</read_first>
  <action>
    Extend the random-gauntlet and depth-match fast tests to require their suite runners to forward seeds `BASE..BASE+n_games-1`, the exact halfmove limit, the identical absolute `deadline` float, and the identical shared `stop_event` object to every game while retaining alternating colors. At game level, spy on every `search_root` call and assert it receives that same deadline/event. Use a fake monotonic clock to prove both harnesses refuse to begin another game/ply after expiry and check expiry again immediately after a search returns, so an expired search result is never pushed.

    Create `tests/test_phase2_strength_evidence.py` with all expensive functions monkeypatched. Test the planned CLI/report builder for: exact forwarding of 30 depth games, 30 random games, seed, depths 2/3/4, max_halfmoves 80, one shared deadline equal to `start_monotonic + remaining_budget`, and one shared cancellation Event; refusal of either sample below 30; pass classification only for depth score rate >=0.50 and random losses==0; failure on either threshold; failure when elapsed seconds reaches/exceeds budget; one-sided 95% zero-loss upper bound `1 - 0.05**(1/30)`; and JSON containing `decision_replacement.original`, `.projection`, `.replacement`, parameters, raw per-game outcomes, elapsed seconds, suite state, completion, and final status.

    Add callback/checkpoint tests proving a sibling temporary file is flushed/fsynced and atomically replaced after every completed game and after each suite transition. Checkpoint state must contain each suite's `status` (`pending|running|completed`), `completed_games`, ordered per-game records keyed by game index/seed, aggregate counters, consumed elapsed seconds, top-level `status`, and `completion`.

    Add resume tests using a prebuilt compatible checkpoint: one with depth games 0–9 complete must invoke only 10–29; one with the depth suite complete and random games 0–6 complete must never call the depth harness and must invoke only random games 7–29; a fully completed failed/passed suite must be reclassified/no-op without replay. Assert parameter/schema mismatch fails before play unless `--restart` is explicit, and duplicate/out-of-order game callbacks are rejected rather than double-counted.

    Add deterministic expiry/interruption tests for a timeout during a game, `KeyboardInterrupt`, and the SIGINT/SIGTERM handler path. Each must set the shared event, atomically preserve only completed games, write `status="failed"` and `completion="incomplete"` with an explicit reason, and exit non-zero. Test `--mark-interrupted REASON` performs the same atomic transition without invoking a harness; this is the supervisor's forced-kill fallback.
  </action>
  <acceptance_criteria>
    - "Fast tests make no real search call and complete in under 2 seconds."
    - "Tests reject `depth_games &lt; 30` or `random_games &lt; 30` before invoking either harness."
    - "Tests assert every mocked search sees the same absolute monotonic deadline and Event, and no post-deadline move/game begins."
    - "Tests prove checkpoints occur after each game and suite and that resume never invokes completed game indices or completed suites."
    - "Tests assert the exact zero-loss upper bound within floating tolerance and require non-zero exit plus failed/incomplete persisted state for timeout or interruption."
    - "`.venv/bin/python -m pytest tests/test_random_mover_gauntlet.py tests/test_depth_vs_depth.py tests/test_phase2_strength_evidence.py -m 'not slow' -q` exits non-zero before implementation."
    - "Commit the test-only RED state atomically as `test(02-10): specify bounded strength evidence contract (RED)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_random_mover_gauntlet.py tests/test_depth_vs_depth.py tests/test_phase2_strength_evidence.py -m "not slow" -q; test $? -ne 0</automated>
  </verify>
</task>

<task type="auto" tdd="true" status="completed" immutable="true" commit="99888cb">
  <name>Task 2 [COMPLETED — DO NOT RERUN/RECOMMIT]: Implement reproducible evidence CLI (GREEN)</name>
  <files>ance/tools/random_mover_gauntlet.py, ance/tools/depth_vs_depth_match.py, ance/tools/phase2_strength_evidence.py</files>
  <read_first>ance/tools/random_mover_gauntlet.py, ance/tools/depth_vs_depth_match.py, ance/search/negamax.py, tests/test_random_mover_gauntlet.py, tests/test_depth_vs_depth.py, tests/test_phase2_strength_evidence.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md</read_first>
  <action>
    Extend `play_game` and `play_depth_match_game` with keyword-only `deadline: float | None = None` and `stop_event: threading.Event | None = None`. Use the supplied shared Event (allocate only when omitted for backward compatibility). Before each ply, immediately after each `search_root`, and before pushing the returned move, raise a dedicated harness cancellation/timeout exception when the event is set or `time.monotonic() >= deadline`. Pass `stop_flag=stop_event` and `deadline=deadline` into every `search_root`; this combines Plan 02-07's in-tree polling with game-boundary checks.

    Extend `run_gauntlet` and `run_depth_match` with backward-compatible keyword controls: `seed: int = 0`, `max_halfmoves: int = 300`, `start_game: int = 0`, prior ordered `game_records`, shared `deadline`, shared `stop_event`, and `on_game_complete(index, record, aggregate)`. Iterate only `range(start_game, n_games)`, use global index `i` for color and `seed+i`, check cancellation/deadline before each game, tally exactly once, and invoke the callback only after a full game is classified. Validate prior records are contiguous indices `[0,start_game)`. Reject non-positive counts and invalid offsets.

    Create `ance/tools/phase2_strength_evidence.py` with an argparse entry point. Expose options `--output`, `--seed` (default 20260710), `--depth-games` (default 30), `--random-games` (default 30), `--max-halfmoves` (default 80), `--budget-seconds` (default 7200), `--restart`, and supervisor recovery option `--mark-interrupted REASON`. Fixed evidence depths are shallow 2, deep 3, random-gauntlet 4; print and record them before starting.

    On a fresh run, write an initial atomic checkpoint with `status="running"`, `completion="incomplete"`, both suites pending, empty game records, zero consumed elapsed, and current-attempt monotonic/UTC start metadata. On default rerun, load and validate schema plus all evidence parameters; `--restart` is the only way to discard incompatible or completed state. Preserve completed records/suites, derive `remaining_budget = budget_seconds - checkpoint.elapsed_seconds.total`, then establish exactly one new-process absolute deadline as `time.monotonic() + remaining_budget` and one shared Event. A passed checkpoint exits 0 without play; a complete failed checkpoint exits 1 without play.

    Resume the first incomplete suite by passing its `completed_games`, records, shared deadline/Event, and callback to its suite runner; skip any suite marked completed. The callback must append exactly the next global game index, update aggregate/elapsed/remaining values, and atomically replace the JSON after every game. Atomically checkpoint a suite as completed before starting the next. The shared deadline is never reset between games or suites in one process. Across resumes, persisted consumed monotonic elapsed reduces the new remaining budget, so interruption cannot restore spent time.

    Install SIGINT and SIGTERM handlers that set the shared Event and raise a controlled interruption handled on the main thread. Catch harness deadline/cancellation, `KeyboardInterrupt`, and controlled signals around both suites; in every path add current-attempt monotonic elapsed, preserve completed games, atomically write `status="failed"`, `completion="incomplete"`, and an explicit reason, then exit non-zero. `--mark-interrupted` must atomically make that transition without playing, preserve all completed records, and charge elapsed time since the checkpointed attempt start (use the monotonic delta when valid and a conservative UTC delta fallback) so forced kill cannot restore budget. Use `try/finally` so no timeout/interruption path reaches success classification.

    Final status is `passed` only when both suite states are completed, each completed count equals its requested sample, depth `score_rate >=0.50`, random `losses ==0`, and total consumed elapsed is strictly within the budget. Otherwise status is `failed`, with `completion="complete"` only when both samples finished, explicit reasons, and exit 1. Calculate the one-sided 95% zero-loss upper rate as `1 - 0.05 ** (1 / random_games)` when losses are zero.

    Write JSON via `&lt;output&gt;.tmp` plus flush/fsync and `os.replace`. Include schema version, UTC timestamp, git commit when available, parameters, suite states and ordered per-game records, aggregate raw outcomes, completed-game counts, per-suite and total consumed elapsed seconds, remaining budget, confidence bound, status/completion/reasons, and `decision_replacement`: original=`100 depth-4 games, max 600s`; measured=`3 games in 1566s`; projection=`about 52200s/14.5h for 100`; replacement=`30 games, depth 4, max 80 halfmoves, combined 7200s budget, losses==0`.
  </action>
  <acceptance_criteria>
    - "`python -m ance.tools.phase2_strength_evidence --help` lists the six evidence options plus `--restart` and `--mark-interrupted`."
    - "Both harnesses forward the same Event/deadline through every game into every `search_root`, and remain backward-compatible for existing positional callers."
    - "The JSON writer uses temporary-file flush/fsync plus `os.replace`."
    - "Every completed game and suite transition is atomically persisted; compatible resume starts at the first missing game and never invokes a completed suite."
    - "The runner exits 1 with persisted failed/incomplete state for insufficient samples, deadline expiry, cancellation, SIGINT/SIGTERM, or KeyboardInterrupt; no such run is labeled passed."
    - "A suite-complete threshold failure is failed/complete and reruns do not replay it; only explicit `--restart` starts over."
    - "Focused fast tests and the full fast suite exit 0."
    - "Commit the implementation atomically as `feat(02-10): add budgeted Phase 2 strength evidence runner (GREEN)`."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; .venv/bin/python -m pytest tests/test_random_mover_gauntlet.py tests/test_depth_vs_depth.py tests/test_phase2_strength_evidence.py -m "not slow" -q &amp;&amp; .venv/bin/python -m pytest -m "not slow" -q &amp;&amp; .venv/bin/python -m ance.tools.phase2_strength_evidence --help</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Record bounded deterministic search and harness evidence</name>
  <files>ance/tools/phase2_deterministic_evidence.py, tests/test_phase2_deterministic_evidence.py, .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json, .planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md</files>
  <read_first>.planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json, .planning/phases/02-core-alpha-beta-search/02-07-SUMMARY.md, .planning/phases/02-core-alpha-beta-search/02-08-SUMMARY.md, .planning/phases/02-core-alpha-beta-search/02-09-SUMMARY.md, ance/search/negamax.py, ance/tools/phase2_strength_evidence.py, ance/tools/depth_vs_depth_match.py, ance/tools/random_mover_gauntlet.py, tests/test_tactical_search.py, tests/test_iterative_deepening.py, tests/test_search_telemetry.py, tests/test_search_deadline.py, tests/test_uci_generation.py, tests/test_go_bestmove.py, tests/test_phase2_strength_evidence.py, tests/test_depth_vs_depth.py, tests/test_random_mover_gauntlet.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md, .planning/phases/02-core-alpha-beta-search/02-VERIFICATION.md</read_first>
  <action>
    Resume execution at Task 3. First assert `git merge-base --is-ancestor ab996ce HEAD` and `git merge-base --is-ancestor 99888cb HEAD`. Refuse to continue if either fails. Treat all Task 1/2 code and tests as immutable inputs: do not edit them, rerun their RED phases, amend their commits, recreate their commits, or include their tracked files in either Task 3 commit.

    Do not resume, restart, or classify the 30+30 runner. Read the current partial JSON first and preserve its measured facts in the replacement report under `runtime_calibration`: exactly 2 completed depth games, 1205.6582282920135 consumed seconds, approximately 602.829 seconds/game, a linear 30-depth-game projection of 18084.8734243802 seconds (~5.02 hours), zero random games started, the received SIGTERM, and the conclusion that the depth suite alone cannot fit the old 7200-second combined budget. Label both recorded game outcomes as a non-statistical smoke/calibration sample; do not calculate or report confidence, Elo, win-rate acceptance, or a D-14 pass from two games.

    Implement and commit `ance/tools/phase2_deterministic_evidence.py` plus exactly six fast tests in `tests/test_phase2_deterministic_evidence.py` before executing evidence. The committed module is both collector and supervisor; it exposes the exact CLI:
    `.venv/bin/python -m ance.tools.phase2_deterministic_evidence --output .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json --summary .planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md --hard-wall-seconds 870 --reporting-margin-seconds 60`.
    Its six tests are: absolute-deadline/margin arithmetic and remaining-time wait bounds; timeout process-group termination plus atomic failed output; external interruption process-group termination plus atomic failed output; completed-artifact rerun refusal; fixed-oracle schema/exact expectations; and atomic terminal-summary content. Require exactly `6 passed`. Commit only these two new files as `feat(02-10): add bounded deterministic evidence collector`. Capture that commit with `git rev-parse HEAD` for the evidence and summary.

    The supervisor sets `supervisor_start = time.monotonic()` and one immutable `hard_deadline = supervisor_start + 870`. Reserve the final 60 seconds: `work_deadline = hard_deadline - 60`, so evidence work has at most 810 seconds. Spawn the collector child with `start_new_session=True`; every pytest subprocess remains in that child process group. Pass the absolute work deadline to the child, and give every `communicate`, `wait`, and nested subprocess call `timeout=max(0, deadline - time.monotonic())` against the applicable immutable deadline—never a fresh relative budget.

    If the child is still live at `work_deadline`, send SIGINT to its whole process group with `os.killpg(os.getpgid(child.pid), signal.SIGINT)`. Graceful wait is at most 15 seconds and also capped by `hard_deadline - time.monotonic()`. If still live, send SIGKILL to the whole process group; reap wait is at most 5 seconds and likewise capped by remaining absolute time. The remaining reserved margin is for atomic failed/incomplete JSON and summary writes plus validation. Install a supervisor watchdog that kills the child process group and exits 124 at the immutable hard deadline. No cleanup, report write, validation, or retry may extend or reset 870 seconds. All timeout/interruption paths must attempt the atomic failed artifacts before the hard deadline and must never report passed.

    The child runs no slow-marked test and no real game harness. It runs and records these three commands with exact counts and monotonic elapsed seconds:
    1. Collector self-tests: `pytest tests/test_phase2_deterministic_evidence.py -m "not slow" -q` → exactly 6 passed.
    2. Requirement/focused contracts: `pytest tests/test_phase2_strength_evidence.py tests/test_depth_vs_depth.py tests/test_random_mover_gauntlet.py tests/test_search_deadline.py tests/test_search_telemetry.py tests/test_iterative_deepening.py tests/test_uci_generation.py tests/test_go_bestmove.py -m "not slow" -q` → exactly 73 passed, 2 deselected.
    3. Full fast suite after adding six collector tests: `pytest -m "not slow" -q` → exactly 139 passed, 2 deselected.
    Each command must exit 0, record start/end monotonic values with nonnegative elapsed time, finish before `work_deadline`, and retain stdout/stderr in the evidence report.

    The focused command must include these exact Plan 02-07/08/09 requirement nodes:
    - SRCH-07 / Plan 02-07: `tests/test_iterative_deepening.py::test_build_game_history_keys_reconstructs_every_prior_position` and `::test_real_game_history_repetition_from_root_child_scores_draw`.
    - SRCH-03 / Plan 02-07: `tests/test_search_deadline.py::test_negamax_aborts_at_poll_boundary_for_expired_deadline`, `::test_quiescence_aborts_at_poll_boundary_for_expired_deadline`, and `::test_deadline_during_iteration_retains_last_completed_depth`.
    - UCI-11 / Plan 02-07: `tests/test_search_telemetry.py::test_completed_iterations_report_exact_cumulative_nodes_and_nps`.
    - UCI-11 / Plan 02-08: `tests/test_uci_generation.py::test_timed_out_worker_keeps_unique_cancel_token_and_cannot_emit_after_replacement`, `::test_info_gate_rechecks_generation_after_waiting_for_lock`, `tests/test_go_bestmove.py::test_stop_signals_current_search_and_emits_exactly_one_legal_bestmove`, and `::test_stale_generation_worker_never_emits_bestmove_after_being_superseded`.
    - Plan 02-09 harness semantics: `tests/test_depth_vs_depth.py::test_game_outcome_is_from_deeper_side_perspective`, `::test_halfmove_cap_is_a_deeper_perspective_draw`, `::test_two_deeper_side_wins_tally_as_two_wins`, `::test_opening_selection_is_reproducible_and_varies_by_seed`, and `::test_every_configured_opening_is_legal_and_four_plies`.
    - Plan 02-10 runner safety: checkpoint, resume, timeout/KeyboardInterrupt, SIGINT/SIGTERM, and mark-interrupted tests in `tests/test_phase2_strength_evidence.py`.

    Run the direct fixed-FEN oracle suite through `search_root` with a fresh unset Event per call. Record and assert these exact `(case, depth, move, score, nodes)` observations:
    - rook mate: `(1, a1a8, 29999, 29)` and `(2, a1a8, 29999, 273)` for `6k1/5ppp/8/8/8/8/8/R3K3 w - - 0 1`, MaterialEval.
    - queen mate: `(1, h1a8, 29999, 42)` and `(4, h1a8, 29999, 8249)` for `6k1/5ppp/8/8/8/8/8/6KQ w - - 0 1`, MaterialEval.
    - hanging queen: `(1, e1e5, 500, 30)` and `(3, e1e5, 500, 2890)` for `4k3/8/8/4q3/8/8/8/4R3 w - - 0 1`, MaterialEval.
    - knight fork: `(1, e5f7, 0, 25)` and `(3, e5f7, 0, 4729)` for `6k1/5q2/8/4N3/8/8/8/4K3 w - - 0 1`, HandcraftedEval.
    - horizon rook: `(1, e1e4, 500, 36)` and `(3, e1e4, 500, 2209)` for `4k3/8/8/8/4r3/8/8/4R2K w - - 0 1`, MaterialEval.
    Require observed depth to equal requested depth. Record each monotonic start/end/elapsed value and require nonnegative elapsed, end no later than `work_deadline`, and total child elapsed at most 810 seconds. Describe these results only as deterministic relative-depth stability. The exact synthetic accounting oracle remains callback nodes `[10,20,30]`, NPS `[10,6,5]`, depths `[1,2,3]`, final depth 3, final nodes 30; deadline retention remains depth 1 with callbacks `[1]`.

    Atomically replace both `02-STRENGTH-EVIDENCE.json` and `02-10-SUMMARY.md` via sibling temporary file, flush, fsync, and `os.replace`. JSON must include `evidence_contract="deterministic_correctness_not_statistical_strength"`, immutable timing constants and actual monotonic timings, runtime calibration, exact commands/counts/results, all ten exact FEN observations, node/deadline assertions, status/completion/reasons, collector commit, and `deferred_statistical_evidence` naming Phase 3 optimized search plus cutechess.

    Add `requirement_evidence` to JSON with every requirement key and explicit evidence IDs: `SRCH-02` → mate/tactical exact-score cases plus alpha-beta fast tests; `SRCH-03` → both completed-depth pairs and the deadline-retention test; `SRCH-04` → hanging-queen and horizon-rook cases plus quiescence fast tests; `SRCH-07` → the two exact real-history tests above; `UCI-11` → exact telemetry plus all four exact generation/isolation tests above. Add `supporting_contracts.plan_02_09` with the five exact harness tests. Every map entry has `status="passed"` only when all referenced observations/tests passed.

    Generate terminal `02-10-SUMMARY.md` from the same classified state. It must include Task 1 commit `ab996ce`, Task 2 commit `99888cb`, the collector commit, the exact collector command, all three commands and exact counts/results/timings, the fixed-FEN and requirement-map result, runtime calibration and infeasibility conclusion, Phase 3/cutechess statistical deferral, evidence path, and `Artifact status: passed|failed`. On success commit only JSON and summary as `test(02-10): record deterministic Phase 2 search evidence`. On failure leave atomic failed/incomplete JSON and summary uncommitted and stop. A completed passed artifact causes future collector invocations to validate and exit without executing tests/search again; no option may overwrite it or recommit Tasks 1/2.
  </action>
  <acceptance_criteria>
    - "Execution resumes at Task 3; ab996ce and 99888cb are verified ancestors, Tasks 1/2 are immutable, and neither is rerun, amended, recreated, or recommitted."
    - "The committed collector/supervisor script and exactly six tests exist; its exact CLI is used by Task 3 verification and collector tests report exactly 6 passed."
    - "`02-STRENGTH-EVIDENCE.json` parses and identifies its contract as deterministic correctness evidence, not statistical strength evidence."
    - "Runtime calibration records 2 games/1205.6582282920135 seconds and the 18084.8734243802-second 30-game depth-only projection; the two outcomes are explicitly non-statistical and produce no confidence/Elo/strength pass."
    - "All ten fixed-FEN observations match the exact moves, scores, depths, and node counts listed in the action; every observation and command has bounded monotonic start/end/elapsed timing."
    - "Focused harness/search contracts report exactly 73 passed/2 deselected, including named repetition, deadline, telemetry, UCI generation-isolation, Plan 02-09, and runner checkpoint/resume/interruption tests."
    - "The full fast suite reports exactly 139 passed/2 deselected after the six collector tests are added."
    - "JSON `requirement_evidence` maps SRCH-02, SRCH-03, SRCH-04, SRCH-07, and UCI-11 to explicit passed evidence IDs; Plan 02-09's five tests appear under supporting contracts."
    - "The report explicitly defers depth-vs-depth Elo and random-gauntlet statistical evidence to Phase 3's optimized search/cutechess harness; D-01/D-14 statistical acceptance is not claimed."
    - "One absolute monotonic supervisor deadline covers work, process-group cleanup, atomic reporting, and validation: work &lt;=810 seconds, reporting reserve 60 seconds, graceful group wait &lt;=15 seconds, forced reap &lt;=5 seconds, and total &lt;=870 seconds."
    - "Timeout and interruption tests prove whole-process-group termination, every wait is capped by remaining absolute time, and atomic JSON/summary output is failed/incomplete with no torn temporary file."
    - "`02-10-SUMMARY.md` exists and contains all three prior commit refs, exact commands/counts/results/timings, runtime infeasibility, Phase 3 deferral, evidence path/status, and terminal `Artifact status: passed`."
    - "Collector/tests are one atomic commit; successful JSON/summary are a second atomic commit. No Task 1/2 file is included, amended, or recommitted."
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lasse/Development/Projects/ANCE &amp;&amp; git merge-base --is-ancestor ab996ce HEAD &amp;&amp; git merge-base --is-ancestor 99888cb HEAD &amp;&amp; git diff --quiet 99888cb -- ance/tools/random_mover_gauntlet.py ance/tools/depth_vs_depth_match.py ance/tools/phase2_strength_evidence.py tests/test_random_mover_gauntlet.py tests/test_depth_vs_depth.py tests/test_phase2_strength_evidence.py &amp;&amp; .venv/bin/python -m ance.tools.phase2_deterministic_evidence --output .planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json --summary .planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md --hard-wall-seconds 870 --reporting-margin-seconds 60 &amp;&amp; .venv/bin/python - &lt;&lt;'PY'
import json
import subprocess
from pathlib import Path

d = json.loads(Path(".planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json").read_text())
s = Path(".planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md")
assert d["status"] == "passed" and d["completion"] == "complete"
assert d["evidence_contract"] == "deterministic_correctness_not_statistical_strength"
assert d["runtime_bound"]["work_seconds"] == 810
assert d["runtime_bound"]["reporting_margin_seconds"] == 60
assert d["runtime_bound"]["hard_wall_seconds"] == 870
assert d["runtime_bound"]["graceful_wait_seconds"] == 15
assert d["runtime_bound"]["forced_reap_seconds"] == 5
assert d["timings"]["supervisor_elapsed_seconds"] &lt;= 870
assert d["timings"]["child_elapsed_seconds"] &lt;= 810
cal = d["runtime_calibration"]
assert cal["completed_depth_games"] == 2
assert cal["elapsed_seconds"] == 1205.6582282920135
assert cal["projected_30_depth_games_seconds"] == 18084.8734243802
assert cal["statistical_claim"] is False
expected = [
    ("rook_mate", 1, "a1a8", 29999, 29), ("rook_mate", 2, "a1a8", 29999, 273),
    ("queen_mate", 1, "h1a8", 29999, 42), ("queen_mate", 4, "h1a8", 29999, 8249),
    ("hanging_queen", 1, "e1e5", 500, 30), ("hanging_queen", 3, "e1e5", 500, 2890),
    ("knight_fork", 1, "e5f7", 0, 25), ("knight_fork", 3, "e5f7", 0, 4729),
    ("horizon_rook", 1, "e1e4", 500, 36), ("horizon_rook", 3, "e1e4", 500, 2209),
]
actual = [(c["id"], c["observed_depth"], c["observed_move"], c["observed_score"], c["observed_nodes"]) for c in d["deterministic_cases"]]
assert actual == expected
assert all(c["passed"] and c["elapsed_seconds"] &gt;= 0 for c in d["deterministic_cases"])
assert d["node_accounting"]["callback_nodes"] == [10, 20, 30]
assert d["node_accounting"]["callback_nps"] == [10, 6, 5]
assert d["node_accounting"]["completed_depths"] == [1, 2, 3]
assert d["node_accounting"]["final_nodes"] == 30
assert d["deadline_retention"] == {"final_depth": 1, "completed_callbacks": [1]}
assert d["commands"]["collector_tests"]["passed"] == 6
assert d["commands"]["focused"]["passed"] == 73 and d["commands"]["focused"]["deselected"] == 2
assert d["commands"]["full_fast"]["passed"] == 139 and d["commands"]["full_fast"]["deselected"] == 2
collector_files = set(subprocess.check_output(
    ["git", "show", "--format=", "--name-only", d["collector_commit"]], text=True
).split())
assert collector_files == {
    "ance/tools/phase2_deterministic_evidence.py",
    "tests/test_phase2_deterministic_evidence.py",
}
assert all(v["status"] == "passed" and v["evidence_ids"] for v in d["requirement_evidence"].values())
assert set(d["requirement_evidence"]) == {"SRCH-02", "SRCH-03", "SRCH-04", "SRCH-07", "UCI-11"}
assert len(d["supporting_contracts"]["plan_02_09"]["tests"]) == 5
assert d["supervisor_tests"]["timeout_process_group_terminated"] is True
assert d["supervisor_tests"]["interrupt_process_group_terminated"] is True
assert d["supervisor_tests"]["atomic_failure_output"] is True
assert d["deferred_statistical_evidence"]["phase"] == 3
assert "cutechess" in d["deferred_statistical_evidence"]["harness"].lower()
assert not d.get("confidence")
assert s.is_file()
text = s.read_text()
for required in (
    "ab996ce", "99888cb", d["collector_commit"], "73 passed", "139 passed",
    "6 passed", "Monotonic timings", "Hard wall: 870 seconds",
    "Reporting margin: 60 seconds", "Phase 3", "cutechess",
    "02-STRENGTH-EVIDENCE.json", "Artifact status: passed",
):
    assert required in text
PY</automated>
  </verify>
</task>

</tasks>

<verification>
- Execution begins at Task 3 with ab996ce/99888cb verified and immutable; their RED/GREEN workflows and commits are never repeated.
- The committed collector's six supervisor tests prove absolute-deadline arithmetic, whole-process-group timeout/interruption termination, atomic failure output, summary output, and completed-artifact rerun refusal.
- The named Plan 02-07 repetition/deadline/telemetry tests, Plan 02-08 generation-isolation tests, Plan 02-09 harness tests, and Plan 02-10 checkpoint/resume/interruption tests all pass in the exact 73-test focused command.
- Ten fixed tactical/mate/horizon observations produce exact expected moves, scores, depths, and nodes.
- The full fast suite reports exactly 139 passed/2 deselected after collector tests are committed.
- One immutable monotonic deadline bounds work to 810 seconds and reserves 60 seconds for group cleanup, atomic JSON/summary reporting, and validation; supervisor elapsed never exceeds 870 seconds.
- JSON requirement mapping and terminal summary make runtime calibration, commands/counts/timings, commit refs, artifact status, and Phase 3 statistical deferral independently inspectable.
</verification>

<success_criteria>
- Phase 2 search correctness is supported by exact deterministic mate, tactic, horizon, telemetry, and deadline-retention oracles.
- Relative-depth behavior is described narrowly as stable oracle satisfaction and completed deeper iterations; no Elo or statistical superiority is inferred.
- The infeasible 30+30 run is not resumed or passed: its two games are retained only as runtime calibration proving the depth suite alone projects to ~5.02 hours.
- Existing runner checkpoint, compatible-resume, deadline/Event, and interruption semantics remain green through fast tests.
- Any failed oracle, test, interruption, or runtime breach leaves atomic evidence and summary failed/incomplete; successful work, process cleanup, reporting, and validation all complete within the same exact 870-second hard wall.
- SRCH-02, SRCH-03, SRCH-04, SRCH-07, and UCI-11 each have an explicit passed evidence-ID map; Plan 02-09 harness semantics are separately mapped as supporting contracts.
- D-01/D-14 long statistical Elo/gauntlet evidence is explicitly deferred to Phase 3 optimized search and cutechess rather than silently weakened.
- RED ab996ce and GREEN 99888cb remain immutable; collector/tests and terminal evidence/summary are separate atomic Task 3 commits, with no multi-hour replay.
- `02-10-SUMMARY.md` is terminal, validated, and records commit refs, exact commands/results/timings, deferral, evidence path, and artifact status.
</success_criteria>

<threat_model>
| Threat ID | Category | Component | Severity | Mitigation |
|---|---|---|---|---|
| T-02-10a | Repudiation | evidence result | high | Record FENs, evaluators, requested depths, exact expected/observed moves and scores, node counts, timings, test commands, and git commit in JSON. |
| T-02-10b | Tampering | statistical overclaim | high | Mark the two-game run as calibration only, emit no confidence/Elo acceptance, and state D-01/D-14 statistical work is deferred to Phase 3. |
| T-02-10c | Denial of Service | slow evidence | high | One immutable monotonic deadline; work stops at +810s, process-group SIGINT/SIGKILL waits consume only remaining time, and watchdog exits by +870s including reporting. |
| T-02-10d | Tampering | partial report | medium | Fsync sibling temporary JSON and summary files, atomically replace both, and classify interruption/failure as failed/incomplete. |
| T-02-10e | Repudiation | harness semantics | high | Retain and execute fast checkpoint/resume/interruption tests from ab996ce/99888cb instead of relying on uncompleted games. |
| T-02-10f | Tampering | brittle relative-depth claim | medium | Require exact tactical oracles at both depths and nondecreasing nodes, while explicitly limiting the conclusion to those fixed positions. |
| T-02-10g | Elevation of Privilege | descendant processes | high | Start a distinct child session and terminate the entire process group on timeout/interruption; never kill by child PID alone. |
| T-02-10h | Repudiation | requirement closure | high | Persist requirement-to-evidence IDs for all five Phase 2 requirements and supporting Plan 02-09 contracts. |
</threat_model>

## Artifacts this phase produces

- `ance/tools/phase2_strength_evidence.py` — completed resumable, deadline-aware runner retained as a tested harness contract; its 30+30 game path is not executed for Phase 2 acceptance.
- `main(argv=None) -> int` — evidence CLI entry point.
- CLI options: `--output`, `--seed`, `--depth-games`, `--random-games`, `--max-halfmoves`, `--budget-seconds`, `--restart`, `--mark-interrupted`.
- Deadline/cancellation/resume callback extensions on both depth-match and random-gauntlet harnesses.
- `tests/test_phase2_strength_evidence.py` — new fast evidence, checkpoint, interruption, and resume contract test module.
- `ance/tools/phase2_deterministic_evidence.py` — committed one-shot collector/supervisor with an immutable monotonic 870-second deadline, 60-second reporting reserve, process-group termination, atomic dual-artifact output, and completed-run refusal.
- `tests/test_phase2_deterministic_evidence.py` — exactly six fast deadline, process-group, atomic failure, rerun-safety, oracle-schema, and summary tests.
- `.planning/phases/02-core-alpha-beta-search/02-STRENGTH-EVIDENCE.json` — machine-readable deterministic correctness evidence, measured infeasibility record, and Phase 3 statistical deferral.
- `.planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md` — validated terminal record containing ab996ce, 99888cb, collector commit, exact commands/counts/timings, requirement results, deferral, evidence path, and artifact status.
- New `seed` and `max_halfmoves` options on `run_gauntlet`.

<output>
Task 3 must atomically create and validate `.planning/phases/02-core-alpha-beta-search/02-10-SUMMARY.md` before success or the terminal evidence commit. The summary is not optional post-processing.
</output>
