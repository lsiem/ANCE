---
phase: 02-core-alpha-beta-search
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - ance/search/negamax.py
  - ance/search/types.py
  - tests/test_iterative_deepening.py
autonomous: true
requirements:
  - SRCH-03
  - SRCH-07
must_haves:
  truths:
    - "search_root iterates depths 1..N and keeps best move from last completed depth on abort"
    - "Twofold repetition in search path or game history scores draw (0) immediately (D-06, D-07)"
    - "50-move rule and insufficient material detected via python-chess (D-08)"
    - "Previous iteration's best move is searched first at root (D-10)"
  artifacts:
    - path: ance/search/negamax.py
      provides: iterative deepening loop in search_root
      contains: "for depth in range"
    - path: tests/test_iterative_deepening.py
      provides: ID and draw-detection regression tests
  key_links:
    - from: search_root ID loop
      to: negamax
      via: completed-depth PV tracking and stop_flag/deadline checks between depths
      pattern: "last_completed"
---

<objective>
Add iterative deepening with draw detection so search scales depth safely and avoids repeating won positions.

Purpose: SRCH-03 + SRCH-07 with D-06–D-08 draw rules — core strength before UCI info wiring.
Output: ID loop in search_root, zobrist path stack for twofold-in-search, python-chess draw helpers.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-alpha-beta-search/02-CONTEXT.md
@.planning/phases/02-core-alpha-beta-search/02-RESEARCH.md
@ance/search/negamax.py
@ance/search/types.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing ID and draw-detection tests</name>
  <files>tests/test_iterative_deepening.py</files>
  <read_first>ance/search/negamax.py, ance/search/types.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md (D-06 through D-08, D-10)</read_first>
  <behavior>
    - Test 1: search_root with stop_flag set mid-depth-2 still returns best move from completed depth 1
    - Test 2: Twofold-in-search — position reachable by repeating a move in the search tree scores 0 when encountered again on path
    - Test 3: Game-history repetition — root position already occurred in board history scores 0 at interior node
    - Test 4: halfmove_clock >= 100 scores draw 0 inside search (use FEN with high halfmove clock)
    - Test 5: insufficient material position scores draw 0 (K vs K)
    - Test 6: Mate still beats draw — winning capture line preferred over repetition draw when both available
  </behavior>
  <acceptance_criteria>
    - tests/test_iterative_deepening.py has at least 5 test functions
    - pytest tests/test_iterative_deepening.py exits non-zero before implementation
  </acceptance_criteria>
  <action>
    Create tests/test_iterative_deepening.py. Use chess.polyglot.zobrist_hash expectations where needed. For ID abort test, set stop_flag after a short sleep or use depth 4 with early stop. Pin FENs for 50-move and insufficient material cases.
  </action>
  <verify>
    <automated>pytest tests/test_iterative_deepening.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED tests for ID retention and all draw rules</done>
</task>

<task type="auto">
  <name>Task 2: Iterative deepening + draw cuts + root move ordering</name>
  <files>ance/search/negamax.py, ance/search/types.py</files>
  <read_first>tests/test_iterative_deepening.py, .planning/phases/02-core-alpha-beta-search/02-RESEARCH.md</read_first>
  <acceptance_criteria>
    - search_root loops depth 1..max_depth (or until deadline/stop), storing last_completed_depth, last_completed_score, last_completed_pv, last_completed_best_move
    - On SearchAborted or stop between depths, returns SearchResult from last *completed* depth
    - negamax checks draw before expanding: zobrist key in ctx.path_keys or ctx.game_history_keys → return 0; board.is_fifty_moves() → 0; board.is_insufficient_material() → 0 (mate-beats-draw: only when no legal winning capture exists at this node — standard search continues if moves exist)
    - path_keys updated on push/pop in negamax; game_history_keys built once at root from board move stack
    - Root move loop orders previous iteration's best move first per D-10
    - SearchResult.depth reflects last completed iteration depth
    - pytest tests/test_iterative_deepening.py exits 0
    - pytest -m "not slow" -q exits 0
  </acceptance_criteria>
  <action>
    Extend SearchContext with last_completed_* tracking fields if needed. Implement ID loop in search_root per 02-RESEARCH.md. Use chess.polyglot.zobrist_hash(board) for repetition keys per D-06 transposition-key stack comparison. Draw score is plain 0 without contempt per D-07. Honor deadline from ctx.deadline via time.monotonic() between depths. MAX_PLY discretion: 64 for infinite mode prep (constant in types.py or negamax.py).
  </action>
  <verify>
    <automated>pytest tests/test_iterative_deepening.py tests/test_alpha_beta.py tests/test_quiescence.py -q</automated>
  </verify>
  <done>SRCH-03 and SRCH-07 satisfied; ID returns last completed depth on interrupt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draw detection | Interior nodes must not claim win when draw rule applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | draw score injection | mitigate | Draw only via explicit python-chess helpers + path key stack; tested FENs |
| T-02-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
ID abort test returns depth-1 move; draw FENs score 0; mate-in-N still found on tactical FENs.
</verification>

<success_criteria>
Iterative deepening operational with correct draw handling and last-completed-depth retention.
</success_criteria>

## Artifacts this phase produces

- `DEFAULT_BARE_GO_MOVETIME_MS = 2000` — `ance/search/types.py` or `negamax.py`
- `MAX_PLY = 64` — `ance/search/negamax.py`
- `game_history_keys` population at search_root — `ance/search/negamax.py`
- `path_keys` push/pop in negamax — `ance/search/negamax.py`

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-03-SUMMARY.md` when done
</output>
