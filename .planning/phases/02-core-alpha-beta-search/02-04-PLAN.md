---
phase: 02-core-alpha-beta-search
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - ance/uci/protocol.py
  - ance/uci/loop.py
  - tests/test_uci_info.py
  - tests/test_go_bestmove.py
  - tests/test_eval_seam.py
autonomous: true
requirements:
  - UCI-11
must_haves:
  truths:
    - "Non-blocking reader/worker threading preserved; stop/quit honored mid-search (D-00b)"
    - "Board state and movegen remain via python-chess Position adapter — no rewrite of UCI loop structure (D-00c)"
    - "Each completed ID depth emits info depth score nodes nps pv with pv[0] matching eventual bestmove (D-11)"
    - "Bare go uses ~2s soft movetime budget iterating deeper until deadline (D-09)"
    - "go infinite deepens until stop up to MAX_PLY — no post-search idle hang (D-12, supersedes D-16)"
    - "Aborted partial depths emit no info line"
  artifacts:
    - path: ance/uci/protocol.py
      provides: send_info_depth formatter
      contains: "send_info"
    - path: ance/uci/loop.py
      provides: go mode dispatch with movetime/infinite/deadline wiring
    - path: tests/test_uci_info.py
      provides: subprocess UCI info line contract tests
  key_links:
    - from: ance/uci/loop.py _run_search
      to: search_root
      via: info_callback emitting send_info_depth per completed depth
      pattern: "send_info"
---

<objective>
Wire UCI `info` output and modernize `go` dispatch so GUIs see thinking lines and Unlimited mode works.

Purpose: UCI-11 + D-09/D-11/D-12 — user-visible search progress and En Croissant Unlimited fix.
Output: `send_info_depth`, loop.go modes, removal of Phase 1 infinite idle and RNG reseed tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-alpha-beta-search/02-CONTEXT.md
@.planning/phases/02-core-alpha-beta-search/02-RESEARCH.md
@ance/uci/loop.py
@ance/uci/protocol.py
@ance/search/negamax.py
@tests/test_go_bestmove.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing UCI info and go-mode tests</name>
  <files>tests/test_uci_info.py, tests/test_go_bestmove.py</files>
  <read_first>ance/uci/loop.py, ance/uci/protocol.py, tests/conftest.py, tests/test_go_bestmove.py</read_first>
  <behavior>
    - Test 1: go depth 2 emits at least one info line matching info depth 2 score (cp|mate) nodes nps pv before bestmove
    - Test 2: pv first token in final info line equals bestmove move UCI
    - Test 3: bare go completes within ~3s and emits multiple info depth lines (iterative deepening under movetime budget)
    - Test 4: go infinite emits increasing depth info lines until stop sent, then bestmove from last completed depth
    - Test 5: go infinite does NOT hang silently after first depth — must emit info then respond to stop within timeout
  </behavior>
  <acceptance_criteria>
    - tests/test_uci_info.py exists with subprocess tests via EngineProcess
    - pytest tests/test_uci_info.py exits non-zero before implementation
    - test_go_bestmove.py bare_go test updated: wall-clock bound relaxed to 3.0s (movetime budget replaces sub-1s fixed depth expectation) OR test moved to assert completion under DEFAULT_BARE_GO_MOVETIME_MS + margin
  </acceptance_criteria>
  <action>
    Create tests/test_uci_info.py with regex patterns for UCI info lines. Update test_bare_go_* in test_go_bestmove.py for D-09 movetime behavior. Remove or rewrite test_ucinewgame_reseeds_tie_break_rng — RNG reseed no longer relevant per D-10 (ucinewgame may still reseed nothing or remove rng entirely from loop.py).
  </action>
  <verify>
    <automated>pytest tests/test_uci_info.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED UCI info and go-mode tests exist; stale RNG reseed test removed/rewritten</done>
</task>

<task type="auto">
  <name>Task 2: Implement info emission and go dispatch</name>
  <files>ance/uci/protocol.py, ance/uci/loop.py</files>
  <read_first>ance/search/negamax.py, ance/search/types.py, tests/test_uci_info.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md</read_first>
  <acceptance_criteria>
    - send_info_depth(depth, score, nodes, nps, pv_uci_list) in protocol.py formats score cp for non-mate and score mate N for |score|>=MATE-MAX_PLY threshold using same mate-distance formula as search
    - _run_search calls search_root with deadline for bare go (DEFAULT_BARE_GO_MOVETIME_MS ~2000), max_depth for go depth, infinite loop until stop for go infinite (max MAX_PLY), movetime timer for go movetime
    - info_callback passed into search_root emits one line per completed depth only (D-11)
    - go infinite no longer calls stop_flag.wait() after single search (D-12 replaces D-16 idle)
    - Remove rng from loop.py module state and handle_ucinewgame reseed logic per D-10
    - search_generation gating and _stop_active_worker preemption unchanged per D-00b
    - Position.copy() before worker spawn unchanged per D-00c
    - pytest tests/test_uci_info.py and pytest tests/test_go_bestmove.py -m "not slow" exit 0
  </acceptance_criteria>
  <action>
    Add send_info_depth to protocol.py with flush=True. Refactor handle_go/_run_search: parse GoCommand modes — bare go sets monotonic deadline now+2s; infinite sets max_depth=MAX_PLY and loops ID until stop_flag; depth/movetime preserved from Phase 1. Wire info_callback from search_root ID completions to send_info_depth. Compute nps as nodes*1000//max(elapsed_ms,1). PV as space-separated UCI moves. Update gauntlet play_game to use SearchResult.best_move without rng.
  </action>
  <verify>
    <automated>pytest tests/test_uci_info.py tests/test_go_bestmove.py -m "not slow" -q</automated>
  </verify>
  <done>UCI-11 satisfied; bare go and infinite behave per D-09/D-11/D-12</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdout→GUI | Unflushed or malformed info lines break GUIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | info line format | mitigate | Regex-tested subprocess contract in test_uci_info.py |
| T-02-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
Subprocess tests prove info depth lines, pv[0]==bestmove, infinite responds to stop.
</verification>

<success_criteria>
GUI-visible thinking lines; En Croissant Unlimited works; bare go uses soft 2s budget.
</success_criteria>

## Artifacts this phase produces

- `send_info_depth(depth, score, nodes, nps, pv)` — `ance/uci/protocol.py`
- `info_callback` wiring in `_run_search` — `ance/uci/loop.py`
- Removal of module-level `rng` — `ance/uci/loop.py`

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-04-SUMMARY.md` when done
</output>
