---
phase: 02-core-alpha-beta-search
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - ance/search/negamax.py
  - tests/test_quiescence.py
autonomous: true
requirements:
  - SRCH-04
must_haves:
  truths:
    - "Quiet depth-0 nodes run quiescence over captures and queen promotions only (D-02)"
    - "Qsearch uses stand-pat plus delta pruning (D-03)"
    - "Captures in qsearch are sorted MVV-LVA; main search has no move ordering (D-04)"
    - "In-check nodes search all evasions with no stand-pat; zero evasions score mate (D-05)"
  artifacts:
    - path: ance/search/negamax.py
      provides: quiescence_search function integrated at depth-0 boundary
      contains: "quiescence"
    - path: tests/test_quiescence.py
      provides: horizon and in-check qsearch regression tests
  key_links:
    - from: negamax depth==0 branch
      to: quiescence_search
      via: quiet-node check vs in-check evasion path
      pattern: "quiescence"
---

<objective>
Add quiescence search at the horizon so the engine stops hanging pieces to the depth limit.

Purpose: SRCH-04 / D-02–D-05 — tactical stability before iterative deepening amplifies depth.
Output: `quiescence_search` wired into negamax depth-0 boundary with MVV-LVA inside qsearch only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-alpha-beta-search/02-CONTEXT.md
@.planning/phases/02-core-alpha-beta-search/02-RESEARCH.md
@ance/search/negamax.py
@ance/search/types.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing quiescence horizon tests</name>
  <files>tests/test_quiescence.py</files>
  <read_first>ance/search/negamax.py, .planning/phases/02-core-alpha-beta-search/02-CONTEXT.md (D-02 through D-05)</read_first>
  <behavior>
    - Test 1: FEN where hanging queen is captured — search at depth 1 chooses capture/recapture line over blundering (score does not collapse vs stand-pat blunder)
    - Test 2: Quiet depth-0 node uses stand-pat — position with no profitable captures returns eval without expanding quiet children
    - Test 3: Delta pruning skips low-value capture when stand-pat already wins by more than delta margin
    - Test 4: In-check at depth 0 searches evasions only — position in check finds evasion, not stand-pat
    - Test 5: In-check with no evasions returns mate score
  </behavior>
  <acceptance_criteria>
    - tests/test_quiescence.py has at least 4 test functions using search_root or direct negamax
    - pytest tests/test_quiescence.py exits non-zero before implementation
  </acceptance_criteria>
  <action>
    Create tests/test_quiescence.py with pinned FENs for each D-02–D-05 behavior. Use HandcraftedEval or MaterialEval. Assert chosen move UCI and/or score thresholds. Document FEN rationale in test docstrings.
  </action>
  <verify>
    <automated>pytest tests/test_quiescence.py -q; test $? -ne 0</automated>
  </verify>
  <done>RED quiescence tests exist covering captures, stand-pat, delta, in-check paths</done>
</task>

<task type="auto">
  <name>Task 2: Implement quiescence search</name>
  <files>ance/search/negamax.py</files>
  <read_first>ance/search/negamax.py, tests/test_quiescence.py, .planning/phases/02-core-alpha-beta-search/02-RESEARCH.md</read_first>
  <acceptance_criteria>
    - quiescence_search(pos, alpha, beta, ctx) exists in negamax.py
    - Quiet nodes: stand-pat eval, generate captures+queen promotions only, MVV-LVA sort, delta prune when stand_pat - capture_value + margin >= beta
    - In-check nodes: no stand-pat; all legal evasions searched; zero moves returns mate score
    - Main negamax move loop at depth>0 remains unordered (no MVV-LVA in main search per D-04)
    - Internal qsearch depth cap applied (discretion: MAX_QDEPTH=8 documented as module constant)
    - pytest tests/test_quiescence.py exits 0
    - pytest -m "not slow" -q exits 0
  </acceptance_criteria>
  <action>
    At negamax depth==0: if pos.is_check() search all evasions without stand-pat per D-05; else call quiescence_search per D-02/D-03. Implement MVV-LVA ordering helper used only inside qsearch per D-04. Queen promotion detection: move.promotion == chess.QUEEN. Delta margin constant DELTA_MARGIN centipawns (discretion: 200). Do not add hash/killer/history ordering to main search loop.
  </action>
  <verify>
    <automated>pytest tests/test_quiescence.py tests/test_alpha_beta.py -q</automated>
  </verify>
  <done>SRCH-04 satisfied; qsearch bounded; main search still unordered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qsearch→movegen | Only legal captures/promotions generated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | qsearch explosion | mitigate | MAX_QDEPTH cap + delta pruning; node counter + stop_flag polling preserved |
| T-02-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
pytest tests/test_quiescence.py green; node counts stay bounded on tactical FENs.
</verification>

<success_criteria>
Engine no longer hangs pieces at fixed depth on horizon test FENs; qsearch rules match D-02–D-05.
</success_criteria>

## Artifacts this phase produces

- `quiescence_search(pos, alpha, beta, ctx) -> int` — `ance/search/negamax.py`
- `_mvv_lva_sort(moves, board) -> list[chess.Move]` — `ance/search/negamax.py` (qsearch-only)
- `MAX_QDEPTH` constant — `ance/search/negamax.py`
- `DELTA_MARGIN` constant — `ance/search/negamax.py`

<output>
Create `.planning/phases/02-core-alpha-beta-search/02-02-SUMMARY.md` when done
</output>
